# Daily Standup — 2026-08-10

- Branch: `feat/premium-ui-overhaul` @ `834e6d1c`
- Upstream: `origin/feat/premium-ui-overhaul` — local is **0 ahead / 0 behind**. Fully pushed at run start; no stranded loop commits on this branch this cycle.
- vs `main`: **38 ahead / 71 behind** (was 37 / 67 at the 08-09 run start). Ahead +1 = the 08-09 standup doc (`834e6d1c`). Behind +4 = the four squash merges that landed on `main` overnight (PRs #207→#210, see DONE).
- Ahead-set composition (unchanged shape, now sharper): of the 38, **27 are docs** (21 daily-standup docs + ~6 research/other docs) and **11 are the regime-study research lane** (7 `feat` + 3 `fix` + 1 `test`) whose *content already shipped to `main`* via the squash merges #199/#200 — squash gives new SHAs, so the granular history still counts as "ahead" despite being duplicated on `main`. The only genuinely-unshipped product cargo remains the single UI commit `b061bef3` (2026-07-16, "unify premium assets with emerald brand", likely superseded by the TC-244…TC-253 work now on `main`). Net unique value = `b061bef3` + the status-doc ledger.
- `origin/main` movement (last 24h): **+4 — the evidence-surface split shipped to production.** Tip moved `ed333035` → `5cdc54d0`. Two focused arcs: TC-252 (observed vs modeled track record) and TC-253 (anti-survivorship strategy study catalog).
- Stashes: **2 — new this cycle** (08-09 reported none): `stash@{0}` `tc254-restack-after-tc253-final-main` and `stash@{1}` `tc253-restack-20260810`. Both are **documented recovery stashes** from the TC-253/TC-254 worktree sessions — do not drop casually. In-progress merge/rebase: none (`.git/MERGE_HEAD` / `rebase-merge` / `rebase-apply` absent). Concurrent-writer check: HEAD re-verified `834e6d1c` immediately before commit.
- Run window: scheduled unattended, 09:30 GMT+8. Provenance stamp: 2026-08-10 09:38 MPST (UTC+0800), UTC `2026-08-10T01:38:01Z`.
- Orchestration note: `ultracode` requested. Holds as before (verified 07-23 → 08-10): the dynamic-workflow review gate blocks headless Workflow fan-out for a daily standup, and status+sync over a tree shared with ~60 parallel worktrees is correctly done inline — reconcile + safety-guard skills, direct git reads, single mutation in the main loop. Fan-out adds no coverage here.

## DONE (last 24h)
- **Evidence-surface split went spec → live production overnight.** Four PRs merged to `main` `ed333035` → `5cdc54d0`:
  - `#207` (`0c069e51`): **separate observed track record from the modeled study.** `/track-record` is now count-first and carries only the source-backed OHLCV outcome record (no equity-model import/fetch/render); `/track-record/study` owns the explicitly hypothetical fixed-fractional, cost-adjusted sequential simulation.
  - `#208` (`528aa99d`): close TC-252 release state.
  - `#209` (`b90bf743`): **publish the anti-survivorship strategy-study catalog** — seven featured study cards, defaulting to the D1 slow-gate evidence-ranked paper-pass, all 11 committed JSON artifacts exposed in a reverse-chronological shelf, adverse aggregate simulation kept on-page.
  - `#210` (`5cdc54d0`): close TC-253 release state; this is the current `main` tip.
- **Production is live and verified (per owner-authorized release records):** TC-252 replaced via Railway deploy `dd91da6f` (`SUCCESS`), TC-253 via Railway deploy `8c424b7d` (`SUCCESS`, image `sha256:1af375e4…`). `/track-record/study` retains the adverse `-100%` result, `-100%` max drawdown, and `-0.52R` net expectancy rather than hiding them; TC-253 renders positive-but-failed strategies (Funding Carry) in neutral grey, never pass-green, and never uses return as a sort/promotion field.
- **Brand alignment of both arcs:** radical transparency and anti-cherry-picking made structural — observed evidence is walled off from modeled evidence, losses are retained, and survivorship bias is countered by keeping all 11 artifacts (winners and losers) one click away. No trading rule, signal generation, allocation, execution, schema, cron, billing, or auth changed — these are presentation-only.
- On this branch: 08-09 daily standup committed and pushed (`834e6d1c`) — fourteen consecutive clean daily writes (07-28 → 08-10).
- No product code advanced on this branch since `b061bef3` (2026-07-16). All forward motion this window happened on `main` in isolated worktrees — the healthy direction.

## PENDING (not finished)
- **TC-254 frozen prospective D1 alpha ledger — built locally, NOT published (the live open item).** Per the release record it is complete in worktree `C:\Ai\tradeclaw-d1-alpha-ledger-20260810` (branch `agent/d1-alpha-ledger`, base `5cdc54d0`): migration `054` defines an immutable, hash-chained BTC/ETH ledger; version `d1-slow-gate-v1-2026-08-09`; rule hash `a9c222a3…`; artifact hash `1a6b28e4…`. It is **uncommitted, unpushed, undeployed, has zero prospective rows, and migration 054 has never touched any DB** (Docker/psql unavailable locally — static contract coverage only). Publishing it (protected PR + migration) is a **separate owner authorization** that starts the prospective epoch. Status stays `collecting-evidence` until the full minimum-observation gate; a passing performance gate yields only `eligible-for-review`, never automatic promotion.
- **Uncommitted drift, owned by parallel sessions — LEFT UNTOUCHED: 106 tracked files (+7801 / −2691).** Byte-identical to the 08-09 end-state — the one-cycle bump on 08-08→08-09 (`scripts/research/*`) has **re-stabilized**; tracked drift is flat again this cycle. Still under the 110-file flag threshold. File-level attribution is unrecoverable (standups record counts, not inventories).
- Untracked: **33 porcelain rows / 62 files** (was ~34 rows / 62 files on 08-09 — flat). Every previously-flagged stranded lane re-confirmed present: `product-i18n` (≈25 files incl. tests + switcher + e2e spec), the slow-gate research helper lane (`slow-gate-cli/assembly`, `recost-report` + tests + the 07-18 plan doc + the `f4.json` artifact), the two regime-study screenshots, orphaned standups 07-13/14/15 (retired; double-commit guard holds), editorial art assets, plus untracked `CLAUDE.md`/`.gitattributes` and a `tmp/` dir.
- Unpushed-to-`main` feature commit `b061bef3` — the redundancy case is now the strongest it has been: TC-244…TC-253 shipped successive evidence-surface and artwork work to `main`, so this one UI commit is almost certainly superseded. The stranded branch's unique cargo is now `b061bef3` + loop docs + unhomed drift.
- Open PRs: **7** (unchanged set): #198 (ia-consolidation "front door match the product", MERGEABLE), #184 (CONFLICTING), #174 (draft), #170 (CONFLICTING), #160 (rescue stranded standups, MERGEABLE), #149 (MERGEABLE), #143 (draft). The TC-252/TC-253 PRs (#207→#210) opened and merged from their own worktree branches without appearing in this loop's snapshot window. None target this branch.
- Committed-standup gap 07-24 → 07-27 remains a closed hole; no backfill.

## NEXT
- **Owner: TC-254 D1 alpha ledger publication decision (new top item).** Authorize the protected PR + migration `054` to start the prospective epoch — but first run migration `054` against isolated PostgreSQL in CI or before production deploy (it currently has static contract coverage only). Do not tune from prospective observations, backfill the ledger, auto-promote the candidate, or delete the losing observed archive. Keep the recovery stash `stash@{0}` until handoff acceptance.
- **Owner: retire-or-integrate `feat/premium-ui-overhaul` — case unchanged, sharper.** 71 behind; research cargo shipped to `main` (squash-duplicated here); only unique unshipped value = `b061bef3` (one UI commit, now very likely superseded by TC-244…TC-253) + the loop-doc ledger + 106-file drift needing per-lane homes. Do not rebase this branch onto a 71-ahead `main`.
- **Owner: post-activation watch on the live D1 slow-gate.** It is `active` in production but has still emitted zero real D1 rows (no qualifying newest-bar transition yet). The first non-simulated `d1-slow-gate` signal is the thing to verify lands correctly and shows in the public tracked record with its simulated-history exclusion intact.
- **Owner: PR #198** (ia-consolidation, MERGEABLE) remains the only green product PR still open — decide merge or close.
- Home the two coherent untracked lanes when the owner picks branch points: `product-i18n` (≈25 files, test-carrying) and the slow-gate research helpers (mostly superseded by #202→#206; re-scope to just the untracked research helpers).
- Parallel-writer watch: tracked **106** (byte-flat, re-stabilized), untracked **33 rows / 62 files**. Flag if tracked breaks **110** or untracked breaks **40 rows**.
- Refresh graphify in an interactive session — the extraction is now **35 days stale**.

## graphify highlights (from graphify-out/GRAPH_REPORT.md, 2026-07-06 extraction — 35 days stale)
- Incremental `--update` intentionally skipped again this run: `GEMINI_API_KEY` is unset (probed via `node -e`, not the shell so no value is echoed), so `--update` on changed files would spawn gateguard-blocked LLM subagents and would ingest the other sessions' 106-file uncommitted drift into the graph. Highlights embedded from the last committed extraction (11,497 nodes · 18,049 edges · 845 communities).
- God Nodes: `readSessionFromRequest()` 114 edges, `execute()` 62, `query()` 60, `getUserById()` 49, `requireCronAuth()` 46, `getTrackedSignals()` 42, `queryOne()` 41, `getTrackedSignalsForRequest()` 34, `getUserTier()` 33, `getOHLCV()` 32. Auth/session + DB access still dominate the coupling surface; `getTrackedSignals()`/`getTrackedSignalsForRequest()` are exactly the read paths that TC-252's observed-vs-modeled `/track-record` split now sits on top of, and `getOHLCV()` is the source path both the D1 slow-gate cron and the frozen TC-254 ledger depend on — the coupling is load-bearing in production.
- Surprising connections: backtest UI props (`ComparisonChartProps`/`MetricsTableProps`) bind directly to `BacktestResult` in `packages/strategies/src/run-backtest.ts`; `refreshAtrCalibration()` couples the web ATR cache to `packages/signals/src/atr-calibration.ts` (the ATR path the slow-gate stops rely on). No *new* connections this run — the graph has not been re-extracted.
- Import cycles worth breaking: `packages/agent/src/channels/base.ts` ↔ `webhook.ts`/`discord.ts`/`telegram.ts` (three cycles through the channel base), `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`.

## Sync result
- Committed this standup on `feat/premium-ui-overhaul` by explicit path (standup doc only; 106 tracked + 33-row untracked parallel-session drift untouched; both recovery stashes left intact).
- Pushed to `origin/feat/premium-ui-overhaul` after re-verifying HEAD; remote-tip advance confirmed via `git ls-remote`.
- No commit or merge to `main`. Still deliberately not opening a feat→main PR from this 71-behind status branch: a 38-commit PR (11 of them squash-duplicates of already-merged research) would violate surgical-change discipline and pre-empt the owner-gated retire-or-integrate decision. Valuable cargo has consistently shipped via its own focused PRs (#199…#210), which is the vindicated pattern.

## Lessons / escalations (this run)
- LESSON: **the evidence-surface refactors (TC-252/TC-253) are the correct structural expression of the brand.** Neither touched the trading rule, signal generation, allocation, or execution — they only walled observed evidence off from modeled evidence and kept every adverse result (`-100%`, `-0.52R`, positive-but-failed strategies in neutral grey). "Every trade verified" is enforced by *architecture* (separate routes, retained artifacts) rather than copy.
- LESSON: **TC-254 shows the frozen-prospective-ledger discipline in practice** — build the ledger and freeze the rule/gate/artifact hashes, but zero prospective rows, no backfill, no auto-promotion, and publication behind a separate owner gate. This is the anti-survivorship contract made operational: the epoch cannot start until the first post-deployment row commits, and status cannot advance past `collecting-evidence` until the full minimum-observation gate clears.
- LESSON: **two documented recovery stashes appeared without warning this cycle** (`tc254-restack`, `tc253-restack`) from parallel worktree sessions. On a ~60-worktree host, a fresh `git stash list` at reconcile time is not noise — an unfamiliar stash is a live parallel lane's safety net; classify before assuming it is droppable.
- ESCALATION (owner, new, decision-ready): **authorize or defer TC-254 D1 alpha-ledger publication.** Complete locally; needs a protected PR + migration `054` (run against isolated PostgreSQL first) to start the prospective epoch. Nothing auto-promotes; keep the recovery stash until handoff acceptance.
- ESCALATION (owner, standing, decision-ready): **retire or integrate `feat/premium-ui-overhaul`.** Unique unshipped value narrowed to one very-likely-superseded UI commit (`b061bef3`) + the loop-doc ledger. Confirm `b061bef3` is superseded by TC-244…TC-253 → retire; do not rebase.
- ESCALATION (owner, standing): **post-activation watch on the live D1 slow-gate** — verify the first real (non-simulated) `d1-slow-gate` signal lands correctly and displays with simulated-history exclusion intact. Nothing to do until a qualifying bar transition occurs.
- ESCALATION (owner, standing): the drift lanes (`product-i18n` ≈25 files, slow-gate research helpers, deploy/self-host cleanup, premium-UI tracked set) still have no committed home — 18 days open, size- and churn-stable. Per-lane commit-and-PR or documented discard.
- NOTE: the launcher's global state file remains sensitive-path-blocked from the loop; durable lessons/escalations live here in the committed standup doc, as before.
