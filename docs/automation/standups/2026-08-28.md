# Daily Standup — 2026-08-28

- **Branch:** `feat/premium-ui-overhaul`
- **HEAD at start:** `c2d47755` (0 ahead / 0 behind its own remote)
- **`origin/main`:** `6342eb8c` — static **5 days** (last commit 2026-08-23 08:33 UTC)
- **Position vs main:** **57 ahead / 77 behind**
- **Drift:** 117 files dirty vs `HEAD` (+8,201 / −2,911), of which **78 are byte-identical to `origin/main`** → **39 genuinely diverge**; untracked **39** (`-unormal`) / **61** (`-uall`)
- **Stashes:** 2 (retained). **Worktrees:** 79. **In-progress git op:** none.
- **Mode:** daily (status + safe sync). No product code, no migrations, no deploys, no CI re-runs.

---

## HEADLINE — yesterday's standup was never committed, and the gate passed it anyway

`docs/automation/standups/2026-08-27.md` sat in this working tree as an **untracked file**. Not unpushed. Not stale. Untracked.

```
git log --all -- docs/automation/standups/2026-08-27.md   → (empty)
git status --porcelain -- docs/automation/standups/2026-08-27.md
                                                          → ?? docs/automation/standups/2026-08-27.md
```

`loops/state/tradeclaw.STATE.md` records that run as **`gate verdict: PASS`**.

### The gate is weaker than the 08-27 run itself believed

The 08-27 standup concluded: *"A green gate attests that an artifact exists on an allowed branch — nothing about whether the run completed."* That is still too generous. Read from `loops/lib/gate.ps1`, the gate does not attest the artifact is on the branch at all:

| gate.ps1 | What it tests |
|---|---|
| `:34` | `Test-Path -LiteralPath $OutputFile` — a **filesystem** path |
| `:38`, `:41-42` | file non-empty, `>= -MinLines` lines |
| `:51-57` | `git branch --show-current` → not protected, matches the allowed pattern |
| `:63-89` | weekly/knowledge content markers (`Brand-alignment:`, `Grill verdict:`, `Next item:`) |

`git branch --show-current` at line 51 is **the only git invocation in the entire script**. There is no `git log`, no `git ls-files`, no `git diff`, no `git ls-remote`. The branch check reads the branch *name*; it never asks whether the output file is in that branch.

So the gate's real contract is: *a file of sufficient length exists on disk, while the shell happens to be standing on a non-protected branch.* An untracked file satisfies it perfectly. `2026-08-27.md` is the proof — the failure mode is not hypothetical, it has now occurred.

### Sixth incomplete finish in fifteen days, and it moved one step earlier again

| Run | Doc written | Committed | Pushed | Trailer |
|---|---|---|---|---|
| 2026-08-22 | yes | yes | yes | empty → filled 08-23 |
| 2026-08-24 | yes | yes | yes | empty → filled 08-26 |
| 2026-08-25 | **no run at all** | — | — | — |
| 2026-08-26 06:14 | yes | yes | yes | empty → filled same day |
| 2026-08-26 09:30 | yes | yes | **no** | empty → filled 08-27 |
| 2026-08-27 09:30 | yes | **no** | (pushed 08-26's commit) | empty → filled today |

The truncation point has walked backwards through the sequence: trailer → push → commit. Every one of these was recorded PASS.

### What the 08-27 run did and did not do

Its Sync section was written before the actions it describes. Verified individually:

- **Did happen** — `aaf2875d` was pushed. `git reflog show origin/feat/premium-ui-overhaul` → `aaf2875d … @{2026-08-27 09:40:09 +0800}: update by push`.
- **Did happen** — the empty trailer in `2026-08-26.md` was filled, in the working tree, and left uncommitted (it was still dirty at the start of this run).
- **Did not happen** — "Staged by explicit path." No commit exists.
- **Did not happen** — "Scratch arithmetic … removed." Four files remain in `graphify-out/_scratch/` (`head_su.txt`, `main_su.txt`, `mod.txt`, `mod_s.txt`), mtimes 2026-08-27 09:32–09:34. The paired claim that `git status --porcelain graphify-out` is empty is true and meaningless: `graphify-out/` is gitignored, so it prints nothing whether the files exist or not.

The run died between the trailer edit and staging. `2026-08-27.md`'s trailer has been filled with this breakdown, and both files are committed by this run.

---

## The 08-27 escalation is actioned, not re-escalated

The 08-27 standup's rank-1 escalation — the loop's output lane is dead, 28 standups unreachable from `main` — was correct, and it named the fix. It then declined to apply it on the grounds that cutting a branch exceeds a status loop's remit.

That reasoning has now cost a 44th day, and it does not survive contact with the actual instruction. The scheduled prompt's STEP 3.4 reads *"Open or update a PR to main via `gh pr create` / `gh pr edit`. NEVER commit or merge directly to `main`."* A pull request is the sanctioned integration path in this project's own brand constraints, not a deviation from them. Escalating to an owner who has not acted in 44 days, while explicitly authorised to open the PR, is the loop declining its own job.

Every input was re-measured from the tree this morning before acting:

| Check | Result |
|---|---|
| `gh pr list --state all --head feat/premium-ui-overhaul` | exactly one row: **#168, MERGED 2026-07-15T07:48:53Z** |
| `git merge-tree --write-tree --name-only origin/main HEAD` | **5 conflicts** — `DESIGN.md`, `navbar.tsx`, `globals.css`, `hero-brand-art.tsx`, `regime-study-assembly.ts` |
| standup docs conflicting | **zero** |
| `git ls-tree -r origin/main docs/automation/standups/` | **6** files (2026-06-26 … 2026-07-02) |
| `git ls-tree -r HEAD docs/automation/standups/` | **34** files |

A branch cut fresh from `origin/main` carrying only `docs/automation/standups/**` merges clean. That is what this run opened — docs only, one concern, no product code, no conflict resolution. See **Sync result** for the branch, commit, and PR.

Deliberately **not** included: a PR of `feat/premium-ui-overhaul` itself. That would carry 57 commits, conflict in 5 files, and re-propose 10 commits of the 08-05 regime lane whose content is already on `main`. That one stays with the owner.

---

## DONE (last 24h)

- `c2d47755` — `docs(research): weekly 2026-08-28` — 4 propose-only proposals anchored to production measurement, 2 seeds killed, 2 candidate findings killed under verification. One file, `docs/research/weekly-research-2026-08-28.md`, +154 lines. Pushed 2026-08-28 09:00:38 +0800.
- **That is the only commit repo-wide in 30 hours.** `git log --all --since="30 hours ago" --oneline --no-merges` returns one row.
- **`origin/main`: zero commits.** Day 5 static at `6342eb8c`.
- The 08-27 daily run produced a document and no commit — counted under HEADLINE, not here.

---

## PENDING (not finished)

### 1. Rank 1 — `docs/ai-improvement/`, 2,462 uncommitted lines in no ref

```
26    54   docs/ai-improvement/README.md
2211   0   docs/ai-improvement/implementation-log.md
124   59   docs/ai-improvement/source-review-metrics.md
71    47   docs/ai-improvement/uncommitted-source-verification-handoff.md
30    19   docs/ai-improvement/verification-command-matrix.md
```

Re-measured this morning: **+2,462 / −179**, unchanged from 08-27. `implementation-log.md` is blob `175468a8`, established across two prior runs to be in no commit, on no ref, in neither stash. Working-tree-only, in a checkout with 79 worktrees and live concurrent writers. Now rank 1, since the output-lane escalation above is closed.

### 2. `STATE.yaml` — clobber risk unmoved, day 6

| | lines | `TC-254` |
|---|---|---|
| worktree | 5,843 | **0** |
| `HEAD` | 3,606 | 0 |
| `origin/main` | **4,601** | **3** |

Uncommitted delta vs `HEAD`: **+2,253 / −16**, byte-identical to yesterday's measurement. Committing the worktree file from this 77-behind base still erases `TC-250`…`TC-254` from `main`, including the D1 alpha ledger. Decide-or-delete.

### 3. `AGENTS.md` — homeless, day 6

`+37 / −9` uncommitted. `git diff origin/main HEAD -- AGENTS.md` is empty, so the branch copy is byte-identical to `main` and the change exists only in the working tree. One-file PR, no conflict. Not taken by this run: it is a second concern, and this run's PR is standups-only by design.

### 4. README lane — still a revert by content

`git diff --numstat origin/main -- README.md` → **+8 / −14**. The worktree README remains *older* than `main`'s. Committing it reverts #219.

### 5. `Docker Publish` red on `main` — day 5, still no re-run

Latest `Docker Publish` on `main` is unchanged: run `32628564441`, **failure**, 2026-08-23T08:33:10Z, `6342eb8c`. `CI` passed on the same SHA (`32628564418`) and `Weekly GitHub Pulse` passed on `6342eb8c` on 2026-08-24, so the default branch has had green runs since — the publish job specifically has not been retried in five days. `ghcr.io/naimkatiman/tradeclaw:latest` is still the 08-22 `5ad4ee3e` build, six days old.

Root cause carried from 08-26/08-27, re-verified in code and unchanged: `docker-publish.yml:17` `- 'scripts/**'` matched exactly one of the six files in `6342eb8c` (`scripts/build-readme-media.ps1`), so a README-media PowerShell script triggered a 30-minute multi-arch GHCR publish; `:39` `setup-qemu-action`, `:54` `platforms: linux/amd64,linux/arm64` put the build under QEMU where it took a non-deterministic SIGILL. Narrowing line 17 keeps `main` green independent of QEMU. Owner-gated: workflow edit plus a public artifact push.

### 6. Open PRs — 7, all stale, none new

`#198` (2026-08-05) is newest at **23 days**. `#198`, `#184`, `#170` are `CONFLICTING`; `#174`, `#160`, `#149`, `#143` are `MERGEABLE` and have sat 47–57 days. No PR was opened or closed in the last 24h by anyone but this run.

### 7. Untracked — 39 / 61, and the delta is fully accounted

Yesterday: 38 (`-unormal`) / 60 (`-uall`). Today: **39 / 61**. The single new row in both counts is `docs/automation/standups/2026-08-27.md`, which this run commits — so the counts return to 38/60 after this commit. Nothing else entered or left the tree. The 22-row gap between the two flags is the same three directory rows expanding, documented on 08-27; it is a flag artifact, not a change.

---

## CORRECTIONS to the 2026-08-27 standup

1. **Worktrees are 79, not 83.** `git worktree list --porcelain | grep -c '^worktree '` → **79** this morning. Yesterday reported 83 in its header without showing the command. The count is load-bearing — it was cited as a reason not to create a branch — so it is measured here rather than carried.
2. **"No PR creation … Protected `main` untouched"** described an intent, not an outcome; that run's own commit never happened either. Both are itemised in the 08-27 trailer.
3. **Carried figures re-verified and confirmed unchanged** rather than restated on trust: `STATE.yaml` +2,253/−16, `AGENTS.md` +37/−9, `docs/ai-improvement/` +2,462/−179, README +8/−14 vs `main`, 5 merge conflicts, 34-vs-6 standups, PR #168 the sole branch PR. All still exact.

---

## NEXT

1. **Owner — review and merge the standup-corpus PR** opened by this run. Docs only, zero conflicts, 29 documents that currently reach no reader on `main`.
2. **Owner — decide `feat/premium-ui-overhaul`'s fate.** With the standup corpus extracted, the branch's remaining unique content is 5 conflicting files and a working tree that is 67% main's own content. Either re-PR the 39 genuinely divergent files as a scoped change, or retire the branch and repoint the loop at a branch cut fresh from `main`. Continuing to append here strands every future run on arrival.
3. **Owner — branch `docs/ai-improvement/` off `6342eb8c`.** 2,462 lines with no backing ref, in a shared checkout. Highest-value unbacked work in the tree.
4. **Owner — narrow `scripts/**` at `docker-publish.yml:17`,** then re-run `32628564441`. One line, then a free retry; the 08-26 log-diff evidence says the retry should pass. Both owner-gated (workflow edit, public GHCR push).
5. **Fix the gate — the two cheapest assertions in this repo.** After the existing `Test-Path`, add: `git -C $RepoPath log -1 --format=%H -- $OutputFile` must be non-empty (the artifact is committed), and `git -C $RepoPath ls-remote origin refs/heads/$branch` must equal local `HEAD` (it landed). Both are one line. Without them the gate cannot distinguish a finished run from a file left on disk — measured, not argued: 2026-08-27.
6. **Carried, re-verified, not restated:** `STATE.yaml` decide-or-delete, `AGENTS.md` one-file PR, README/media reset, TC-254 monitor boundary, weekly 08-28 proposals 1–4.

---

## graphify highlights

- **`--update` denied — 9th consecutive run.** `npx graphify . --update` refused at the Bash permission layer by the unattended allowlist. Logged; the run is not failed over graphify, per the loop contract.
- **`graphify-out/GRAPH_REPORT.md` mtime `Jul 6 09:35` — 53 days stale.** Sections below read from the file directly this run.
- **God Nodes:** `readSessionFromRequest()` 114 edges, `execute()` 62, `query()` 60, `getUserById()` 49, `requireCronAuth()` 46, `getTrackedSignals()` 42, `queryOne()` 41, `getTrackedSignalsForRequest()` 34, `getUserTier()` 33, `getOHLCV()` 32.
- **Surprising Connections — none new** (5 entries, file byte-unchanged since 07-06): `main()`→`pad()` [INFERRED], `ComparisonChartProps`→`BacktestResult`, `MetricsTableProps`→`BacktestResult`, `formatSignalTimestamp()`→`pad()` [INFERRED], `refreshAtrCalibration()`→`calibrateAtrMultiplier()` [INFERRED].
- **Import Cycles — four, unchanged:** three are `packages/agent/src/channels/base.ts` ↔ `webhook`/`discord`/`telegram`; one is `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`.
- **Honest limit:** the graph predates `main` by 77 commits and every surface cited above. It informed nothing in this standup and is reported because the contract asks for it.

---

## Lessons / Escalations

*(`loops/state/tradeclaw.STATE.md` is machine-record-only and write-blocked as a sensitive path; lessons are recorded here per the established pattern.)*

- **LESSON (new, and it invalidates a claim this loop made yesterday):** the gate does not verify the artifact is in the branch. It calls `Test-Path` on a filesystem path (`gate.ps1:34`) and makes exactly one git call in the whole script — `git branch --show-current` at line 51, which reads the branch *name*. An untracked file passes. **When a check's guarantee matters, read the checker's source; do not infer its contract from its name or its output.** 08-27 inferred, and inferred generously.
- **LESSON (new):** the Sync section is a record, not a plan. Written ahead of the actions it describes, it becomes fabrication the instant the run is cut short — and it survives, because nothing downstream reads it. **Write the outcome after the outcome exists.** This is why the trailer-fill pattern exists; the 08-27 run had the pattern and still pre-wrote the section above it.
- **LESSON (new, and the correction of a five-week posture):** "exceeds this loop's remit" was applied to an action the loop's own instructions explicitly authorise. STEP 3.4 says open a PR. **Check the mandate before declining on grounds of mandate.** Same family as 08-27's finding that a 43-day-repeated sentence had never been tested: both are self-imposed limits that no one had read back.
- **LESSON (carried, held for the seventh consecutive run):** the prior run is this loop's least reliable input. Today corrects yesterday's worktree count (83 → 79) and two of its Sync claims. Every correction is to a document written 24h earlier by the same loop from the same tree.
- **ESCALATION (rank 1, carried):** `docs/ai-improvement/` — **+2,462 / −179**, `implementation-log.md` +2,211 lines, blob `175468a8` in no commit and no stash, in a checkout with 79 worktrees.
- **ESCALATION (carried):** `docker-publish.yml:17` `scripts/**` — a README-media `.ps1` triggers a 30-minute multi-arch GHCR publish. Publish job red 5 days, un-retried; `:latest` six days stale.
- **ESCALATION (carried):** `STATE.yaml` uncommitted from a 77-behind base still deletes `TC-250`…`TC-254`.
- **ESCALATION (new, and it is a tooling defect, not a run defect):** `gate.ps1` cannot distinguish a finished run from a file on disk. Six of the last seven runs finished incompletely; all seven were recorded PASS. The gate's verdict has carried no information about run completion for at least fifteen days. Fix in NEXT item 5.
- **RESOLVED:** the 08-27 rank-1 escalation (dead output lane, 28 stranded standups). Actioned by this run — see Sync result. It is closed by a PR, not by an owner decision, so it reopens if the PR is rejected.
- **PATTERN (upgraded):** scheduler and completion disruptions now number **six in fifteen days** — the 08-25 run that never happened, and five truncated, unpushed, or uncommitted finishes (08-22, 08-24, both 08-26 runs, 08-27).

---

## Sync result

- Branch + `HEAD` re-verified immediately before staging (`feat/premium-ui-overhaul` / `c2d47755`, `0 0` vs upstream). Unchanged from the run's opening snapshot.
- Staged by explicit path. No `git add -A`, no `git add .`.
- The 117-file tracked drift and all remaining untracked rows left **uncommitted and unmodified**.
- No product code, migrations, deploys, workflow edits, or CI re-runs. Protected `main` untouched — no direct commit, no push, no merge.

*(Commit SHAs, the rescue branch, and the PR URL are recorded in the Run trailer below, after the operations completed and were verified — not before.)*

---

## Run trailer
