# Daily Standup — 2026-07-23

- Branch: `feat/premium-ui-overhaul` @ `614030a9`
- Upstream: `origin/feat/premium-ui-overhaul` — in sync (0 ahead / 0 behind; local HEAD `614030a9` == remote tip `614030a9469141e0260b404eda2b8a68cac65e0b`, verified via `git ls-remote`).
- vs `main`: 9 ahead / 38 behind. The 9-ahead set is 8 standup/research docs + one feature commit (`b061bef3 feat(ui): unify premium assets with emerald brand`).
- Stashes: none. In-progress merge/rebase: none (`.git/MERGE_HEAD` / `rebase-*` absent). Concurrent-writer check: HEAD unchanged across the run (re-verified immediately before commit).
- Run window: scheduled unattended, 09:30 GMT+8.
- Orchestration note: `ultracode` requested. I authored a read-only fan-out workflow (graphify highlights + prior-standup continuity + an independent ground-truth cross-check), but the dynamic-workflow gate required manual review — unavailable in an unattended run — so it did not execute. Fell back to the same read-only investigation inline. No loss of coverage: ground truth was already established twice (reconcile + direct git), and the git mutation is single-context by design (parallel writers to one working tree are unsafe).

## DONE (last 24h)
- One commit, and it is yesterday's own standup: `614030a9 docs(standup): daily standup 2026-07-22` (committed 2026-07-22 09:36:43 +0800) and pushed to `origin/feat/premium-ui-overhaul`. That push is why the ahead-of-`main` count ticked 8 → 9 overnight.
- No product code advanced on `feat/premium-ui-overhaul` since `b061bef3` (2026-07-16). This remains a status branch the loop is not driving forward. Nothing merged to `main` in the last 24h either — the newest merges are still PRs #177–#181, all dated 2026-07-19.

## PENDING (not finished)
- Uncommitted drift, owned by parallel sessions — LEFT UNTOUCHED this run (not this loop's work): 99 tracked files modified/deleted (+5312 / −2566), 46 untracked. Vs 07-22 (+5222 / −2562, 46 untracked): +90 net insertions, +4 deletions, untracked count now stable at 46 (it had jumped 26 → 46 two nights prior). Drift is still moving but slower, and the untracked set has stopped growing. Lanes:
  - Premium UI overhaul (dominant tracked lane): `apps/web/app/components/`, `apps/web/components/`, `apps/web/components/landing/`, `apps/web/lib/`, locale pages (`ar`/`es`/`ms`/`zh`), `dashboard`, `screener`, `track-record`, `layout.tsx`.
  - Deploy / self-host cleanup: working-tree deletions `Dockerfile.fly`, `apps/web/app/fly/FlyClient.tsx`, `apps/web/app/replit/ReplitClient.tsx`; modified `fly.toml`, `docker-compose.yml`, `.replit`, `.github/workflows/docker-test.yml`, `.env.example`, `.env.docker.example`, `scripts/init-db.sh`, `scripts/test-docker.sh`, `packages/create-tradeclaw/index.js`, `docs/FLYIO.md` / `docs/REPLIT.md` / `docs/QUICKSTART.md`.
  - WS / price lanes: `apps/web/lib/hooks/use-price-stream.ts`, `use-websocket-prices.ts`, `apps/web/lib/signals-live.ts`, `apps/web/middleware.ts`.
  - Recost-research lane: `scripts/research/recost-segment.ts` plus a new `scripts/research/__tests__/recost-segment-cli.test.ts`.
- New untracked subsystem (not present in earlier lane breakdowns as a named block): a full **`product-i18n` localization layer** — ~24 untracked files under `apps/web/lib/product-i18n/` (`app-shell`, `dashboard`, `dashboard-live`, `dashboard-evidence`, `screener`, `track-record`, `track-record-widgets`, `theme-state`, `marketing-nav`, `format`, `server-locale` + matching `__tests__`), plus `apps/web/app/components/product-locale-switcher.tsx` and `apps/web/tests/e2e/features/product-localization.spec.ts`. This is a substantial in-progress feature sitting entirely uncommitted.
- Untracked slow-gate research lane: `scripts/research/slow-gate-{assembly,cli}.ts` + `recost-report.ts` and their `__tests__`, `docs/research/experiments/slow-gate-BTCUSD_ETHUSD-D1-2017-09-01-2026-07-16-f4.json`, and the plan doc `docs/plans/2026-07-18-slow-regime-gate-sandbox.md` (still untracked).
- Untracked infra/test additions: `apps/web/app/api/health/route.test.ts`, `apps/web/app/__tests__/deployment-surface-integrity.test.ts`, `apps/web/lib/hooks/websocket-config.ts` (+test), `apps/web/lib/github-star-count.ts` (+test), `apps/ws-server/src/__tests__/auth.test.ts`, `packages/create-tradeclaw/index.test.js`, plus untracked `.gitattributes`, `CLAUDE.md`, and `tmp/imagegen/*.png`.
- Unpushed-to-`main` feature commit: `b061bef3 feat(ui): unify premium assets with emerald brand` — still the only non-doc commit in the 9-ahead set.
- Orphaned standups on disk, untracked here — NOT rescue-committed (verified permanently retired): `2026-07-13.md`, `2026-07-14.md`, `2026-07-15.md` each already live on other branches. Committing them here would create divergent duplicates; the rescue double-commit guard holds.
- Open PRs for this branch: none (`gh pr list --head feat/premium-ui-overhaul --state open` empty). This branch's own #168 merged 2026-07-15.
- Newest plan doc: `docs/plans/2026-07-18-slow-regime-gate-sandbox.md` (still untracked) — the active slow-regime-gate sandbox workstream.

## NEXT
- Owner decision (gated): integrate `feat/premium-ui-overhaul` → `main`. Branch is 9-ahead / 38-behind with one unmerged feature commit (`b061bef3`) plus the 99-file uncommitted drift. Needs a rebase-or-supersede call AND a disposition for the drift before any PR. Do not auto-open a `main` PR against a 38-behind base (stale-base-clobber risk), and do not fold unrelated parallel drift into a standup-branch PR.
- Decide the home for the untracked `product-i18n` layer and slow-gate research lane. Both are coherent, test-carrying feature sets stranded in the working tree with no committed branch — highest-value disposition target after the UI drift, because they are self-contained and would make clean PRs on their own.
- Advance the slow-regime-gate sandbox per `docs/plans/2026-07-18-slow-regime-gate-sandbox.md`. Research-surface + `currentVol` production wire-up already merged on `main`; keep observing the live vol scaler without retuning until a predeclared production sample is sufficient. No invented metrics — measured or "pending real data" only.
- Standup-gap 07-19/07-20 is closed on record as a permanent two-day hole; no backfill. If the launcher write-path root cause is still unaddressed, that is the recurring-failure item, not a per-day re-run.
- Refresh graphify: `GRAPH_REPORT.md` is the 2026-07-06 extraction — 17 days stale. A fresh `--update` stays deferred on scheduled runs because it spawns LLM subagents whose writes gateguard blocks on this host; queue it for an interactive session.

## graphify highlights (from graphify-out/GRAPH_REPORT.md, 2026-07-06 extraction)
- Incremental `--update` intentionally skipped on this status run (spawns gateguard-blocked LLM subagents; the drift it would ingest is other sessions' uncommitted work). Highlights embedded from the last extraction. Corpus at extraction: 11,497 nodes · 18,049 edges · 845 communities · 98% EXTRACTED.
- God Nodes (core abstractions to watch for coupling): `readSessionFromRequest()` 114 edges, `execute()` 62, `query()` 60, `getUserById()` 49, `requireCronAuth()` 46, `getTrackedSignals()` 42, `queryOne()` 41, `getTrackedSignalsForRequest()` 34, `getUserTier()` 33, `getOHLCV()` 32. Auth/session and DB access dominate the graph — a change to `readSessionFromRequest()` fans out across 114 edges.
- Surprising connections: `ComparisonChartProps` and `MetricsTableProps` (`apps/web/app/backtest`) both reference `BacktestResult` from `packages/strategies/src/run-backtest.ts` — backtest UI is tightly bound to the strategy runner's result shape, so a result-shape change breaks the UI. `refreshAtrCalibration()` → `calibrateAtrMultiplier()` couples `apps/web/app/lib/atr-calibration-cache.ts` to `packages/signals/src/atr-calibration.ts`.
- Import cycles (2-file, worth breaking): `packages/agent/src/channels/base.ts` ↔ `webhook.ts` / `discord.ts` / `telegram.ts` (three cycles through the channel base class); `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`.

## Sync result
- Committed this standup on `feat/premium-ui-overhaul` by explicit path (standup doc only; the 99-file drift + 46 untracked stay untouched — not this loop's work).
- Pushed to `origin/feat/premium-ui-overhaul` after re-verifying HEAD had not shifted, then confirmed the remote tip advanced to the new commit via `git ls-remote`.
- No commit or merge to `main`; feature→main integration remains owner-gated (deliberately not auto-opening a PR from a 38-behind status branch carrying unrelated parallel drift).

## Lessons / escalations (this run)
- ESCALATION (owner): the uncommitted drift on `feat/premium-ui-overhaul` is stabilizing in count (untracked flat at 46) but still has no committed home. It now clearly contains at least three separable, test-carrying feature sets — premium UI overhaul, the `product-i18n` localization layer, and the slow-gate research lane — plus deploy/self-host cleanup. These would make clean, independent PRs; leaving them as ambient working-tree state risks a stale-base clobber against a 38-behind base. Needs a deliberate per-lane commit-and-PR or a documented discard.
- ESCALATION (owner): feature→`main` integration is still gated. The 9-ahead set carries exactly one product commit (`b061bef3`); the rest are loop docs. A clean integration would cherry-pick or rebase `b061bef3` onto current `main`, not PR the whole status branch.
- LESSON: `ultracode` fan-out for a daily standup is blocked by the dynamic-workflow review gate under unattended permissions. A status+sync standup is single-context git work; the safe pattern is inline read-only investigation (git reads + seeded graphify report) with the one mutation kept in the main loop. Do not spend the turn re-authoring a workflow that cannot clear the gate headless.
- LESSON: `Glob` still times out on this repo (many `.claude/worktrees/*` + sibling `tradeclaw-*` worktrees); use `git diff --name-only` / `git ls-files --others` for file discovery. Shell guard also blocks `sed`, `env | grep`, unquoted `@{u}`, and PowerShell script blocks / `$()` here — use explicit refs and plain git output.
