# Daily Standup — 2026-07-21

- Branch: `feat/premium-ui-overhaul` @ `69a97b2e`
- Upstream: `origin/feat/premium-ui-overhaul` — in sync (0 ahead / 0 behind its own remote, pre-commit)
- vs `main`: 7 ahead / 38 behind
- Stashes: none. In-progress merge/rebase: none. Concurrent-writer check: HEAD unchanged across the run.
- Run window: scheduled unattended, 09:35 GMT+8.

## DONE (last 24h)
- Nothing committed on this branch in the last 24h. The loop has not advanced `feat/premium-ui-overhaul` since 2026-07-18 (`69a97b2e`, the 07-18 state-write-denied record).
- Prior two scheduled runs (07-19, 07-20) both recorded `FAIL(2)` in the loop state: their standup files were never written or committed. `2026-07-19.md` and `2026-07-20.md` do not exist on disk or on any branch — a real two-day gap, not a mislabel.

## PENDING (not finished)
- Uncommitted drift, unchanged in scope and owned by other sessions — LEFT UNTOUCHED this run: 99 tracked files modified/deleted (+5031 / −2560), 26 untracked. Concentrated lanes:
  - Premium UI overhaul: `apps/web/app/components/` (~17% of drift), `apps/web/components/` (~11%), `apps/web/components/landing/` (~5%), `apps/web/lib/` (~4%).
  - Deploy/self-host config: `fly.toml`, `docker-compose.yml`, `docker-entrypoint.sh`, `.replit`, `scripts/init-db.sh`, `scripts/test-docker.sh`, `packages/create-tradeclaw/index.js`, plus `docs/FLYIO.md` / `docs/REPLIT.md` / `docs/QUICKSTART.md`.
  - Deletions staged in working tree: `Dockerfile.fly`, `apps/web/app/fly/FlyClient.tsx`, `apps/web/app/replit/ReplitClient.tsx`.
  - AI-improvement docs: `docs/ai-improvement/implementation-log.md` (+1404), README, source-review-metrics, verification-command-matrix.
  - WS/price lanes: `apps/web/lib/hooks/use-price-stream.ts`, `use-websocket-prices.ts`, `apps/web/lib/signals-live.ts`.
- Unpushed feature commit on this branch not on main: `b061bef3 feat(ui): unify premium assets with emerald brand` (the other 6 of the "7 ahead" are standup/research docs).
- Orphaned standups on disk, untracked here: `2026-07-13.md`, `2026-07-14.md`, `2026-07-15.md`. NOT double-committed this run — per the rescue double-commit guard they may already live on `loop/standup-*` branches; needs `git log --all -- <path>` verification first.
- Open PRs (none for this branch): #174 (draft, remotion claw bg), #170 (daily intel 07-16), #160 (rescue 5 stranded standups 07-05..07-11), #149 (loop standup 07-04 + weekly 07-03), #143 (draft, signal-worker ADR). Branch's own #168 merged 2026-07-15.
- Newest plan doc: `docs/plans/2026-07-18-slow-regime-gate-sandbox.md` (untracked) — the active slow-regime-gate sandbox workstream.

## NEXT
- Owner decision (gated): integrate `feat/premium-ui-overhaul` → `main`. Branch is 8-ahead (post this commit) / 38-behind with one unmerged feature commit plus a 99-file uncommitted drift owned by parallel sessions. Needs a rebase-or-supersede call AND a disposition for the drift before any PR — do not auto-open a main PR against a 38-behind base.
- Reconcile the standup gap: confirm whether 07-19/07-20 are covered elsewhere or accept the two-day gap on record; verify orphaned 07-13/14/15 against `loop/standup-*` branches before any rescue commit (avoid divergent duplicates).
- Advance the slow-regime-gate sandbox per `docs/plans/2026-07-18-slow-regime-gate-sandbox.md` (research-surface + `currentVol` production wire-up already merged on main; keep observing the live vol scaler without retuning until a predeclared production sample is sufficient).
- Refresh graphify: `GRAPH_REPORT.md` is the 2026-07-06 extraction, now 15 days stale.

## graphify highlights (from graphify-out/GRAPH_REPORT.md, 2026-07-06 extraction)
- Full `--update` intentionally skipped on this status run: it spawns LLM subagents whose writes gateguard blocks on this host, and the drift under review is not this loop's work. Highlights embedded from the last extraction; a fresh extract is queued in NEXT.
- God Nodes (core abstractions to watch for coupling): `readSessionFromRequest()` 114 edges, `execute()` 62, `query()` 60, `getUserById()` 49, `requireCronAuth()` 46, `getTrackedSignals()` 42, `queryOne()` 41.
- Surprising connections: `ComparisonChartProps` and `MetricsTableProps` (apps/web/app/backtest) both reference `BacktestResult` from `packages/strategies/src/run-backtest.ts` — backtest UI is tightly bound to the strategy runner's result shape.
- Import cycles (2-file): `packages/agent/src/channels/base.ts` ↔ `webhook.ts` / `discord.ts` / `telegram.ts`; `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`.

## Sync result
- Committed this standup on `feat/premium-ui-overhaul` by explicit path (standup doc only; the 99-file drift stays untouched — not this loop's work).
- Pushed to `origin/feat/premium-ui-overhaul`. No commit or merge to `main`; feature→main integration remains owner-gated.

## Lessons / escalations (this run)
- ESCALATION (owner): two consecutive `FAIL(2)` runs (07-19, 07-20) produced no standup file at all — the daily loop is silently failing to write on some scheduled runs. Standups for those two dates are absent everywhere. Root cause not diagnosed this run; needs a look at the launcher's write path / permission state on those runs.
- ESCALATION (owner): the uncommitted drift on `feat/premium-ui-overhaul` has grown across many sessions (99 files now) with no committed home. It carries real product changes (premium UI, deploy config, WS hooks). It needs either a deliberate commit-and-PR or a documented discard decision — leaving it as ambient working-tree state risks a stale-base clobber.
- LESSON: `Glob` times out (>20s) on this repo because of the many `.claude/worktrees/*` and sibling `tradeclaw-*` worktrees; use `git ls-files` / `git ls-files --others` for file discovery here instead.
