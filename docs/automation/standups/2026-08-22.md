# Daily Standup — 2026-08-22

- Branch: `feat/premium-ui-overhaul` @ `e6d7be78`.
- Upstream: `origin/feat/premium-ui-overhaul` — **0 ahead / 0 behind** at run start. Remote tip verified `e6d7be78dbf0ebbad0c57e76bfc3d67e62a18e33` (`git ls-remote`) == local HEAD, so yesterday's push landed cleanly. Will become 1 ahead once this standup commits, until pushed.
- vs `main`: **48 ahead / 74 behind** (was 46 / 74 at the 08-21 run start). Ahead +2 = the two 08-21 status-doc commits (`7e4fa991` standup, `e6d7be78` sync trailer). Behind **unchanged at 74** — `main` has not moved.
- `origin/main` movement (since last standup): **none.** Tip held at `055107ad` (2026-08-11T04:20:04Z, PR #214 dark/light theme lane) — now **11 days static**. Zero owner merges. `git fetch` run this cycle; tip identical post-fetch. Open PRs (`gh pr list`) are the same seven stale June/July/early-Aug lanes (#198, #184, #174, #170, #160, #149, #143); none new, none merged, none target this branch head.
- Stashes: **2 — unchanged** since 08-10: `stash@{0}` `tc254-restack-after-tc253-final-main`, `stash@{1}` `tc253-restack-20260810`. Documented recovery stashes from the TC-253/TC-254 worktree sessions — do not drop until owner handoff acceptance. In-progress merge/rebase: **none** (`.git/MERGE_HEAD` / `rebase-merge` / `rebase-apply` absent). Concurrent-writer check: HEAD re-verified `e6d7be78` immediately before the write and again before commit; 80 worktrees are registered against this repo, so this check is not ceremonial.
- Run window: scheduled unattended, 09:30 GMT+8 (2026-08-22). **Loop cadence recovered** — this is the second consecutive daily run after the 08-14 → 08-19 dark period and the 08-20 triple FAIL(2). Orchestration: `ultracode` requested; holds as before (verified 07-23 → 08-22) — the dynamic-workflow review gate blocks headless Workflow fan-out for a daily standup, and status + safe sync over a tree shared with ~80 parallel worktrees is correctly done inline: `continuous-improvement:reconcile` + `safety-guard` skills, direct git reads, single doc mutation in the main loop. Fan-out adds no coverage here.

## DONE (last 24h)

- **08-21 daily standup closed out and pushed** (`7e4fa991` + trailer `e6d7be78`, both 2026-08-21 09:37 +0800 — the only two commits in the strict 24h window). The push landed `e040cd9b..7e4fa991` and then `e6d7be78`; remote tip re-verified this run, upstream now 0/0. The previously-stranded `c40f7184` (08-21 weekly research) is confirmed on the remote.
- **Verified three carried-forward weekly proposals against live code — one is already shipped.** The 08-21 weekly research (`c40f7184`) carried three grill-SURVIVED proposals forward as decision-ready. Read-only verification this run resolves one of them and sharpens a second (detail in PENDING/NEXT). This is the loop's verify-before-propose discipline applied to its own prior output: proposing work that already exists is the same failure class as cherry-picking a win.
- **Quiet cycle on product surfaces.** `origin/main` held at `055107ad` for the full window; no PRs merged, opened, or closed against `main`. No product code advanced on this branch since `b061bef3` (2026-07-16) — the healthy pattern holds: real forward motion happens on `main` in isolated worktrees, never in this shared premium-UI checkout.
- **TC-254 prospective D1 alpha ledger remains live and collecting** (shipped 08-10). No new publication event — monitoring-only. Gate thresholds re-verified from code on `main` this run (`apps/web/lib/d1-alpha-protocol.ts:12-14`): `D1_ALPHA_MIN_CALENDAR_DAYS = 365`, `D1_ALPHA_MIN_SNAPSHOTS = 365`, `D1_ALPHA_MIN_CLOSED_TRADES = 12`; `apps/web/lib/d1-alpha-ledger.ts:500-502` defaults the gate to `collecting-evidence` / `not-promoted`. Progress figures (0/365 days, 1/365 snapshots, 0/12 closed trades) are unchanged and stand on the owner verification record, not a fresh probe — see the verification note.

Verification note: `origin/main` tip `055107ad`, the empty merge window, the open-PR set, and the remote branch tip were confirmed this run from local git + `gh pr list` + `git ls-remote`. The three code findings below were confirmed by direct file reads in this checkout and against `origin/main` trees. A live production re-probe (`/api/health`, `/track-record/alpha`) was **not** run — headless external `curl` is approval-gated in the unattended allowlist (same class as the known Cloudflare headless block). TC-254 live counts therefore stand on the owner-authorized verification record (MemoryCore 2026-08-10 17:42 MPST).

## PENDING (not finished)

- **Weekly proposal 3 (public calibration surface) is ALREADY SHIPPED — retire it, do not build it.** The 08-21 proposal asked to publish `confidence-calibration.ts` → `/api/calibration` as a public reliability view showing band-vs-realized win-rate plus Brier/ECE, with an honest "pending real data" state below the 20-sample floor. All of that exists on `origin/main` **and** on this branch: `apps/web/app/calibration/page.tsx` + `apps/web/app/calibration/CalibrationClient.tsx` render the band chart (`CalibrationClient.tsx:105-137`), a Brier score tile (`:306`), an ECE tile with assessment (`:319`), and an `insufficientData` state (`:235,274`) driven by `MIN_STABLE_CALIBRATION_SIGNALS = 20` in `apps/web/app/api/calibration/route.ts:26`. The route carries **no auth or tier gate** (no `readSessionFromRequest` / `getUserTier` / 401 / 403 anywhere in the file) and is 5-min cached. It is fully discoverable: linked from `apps/web/app/components/site-footer.tsx:37`, `apps/web/components/PageNavBar.tsx:114`, `apps/web/app/how-it-works/HowItWorksClient.tsx:364`, `apps/web/app/open-data/page.tsx:120`, and `apps/web/app/sitemap.ts:29` (priority 0.85) — with `nav-routes.test.ts:42` and `sitemap-routes.test.ts:36` asserting both. The empty-bucket honesty the proposal wanted is already enforced in code: `winRate` is `null` when a bucket is empty, and the comment at `CalibrationClient.tsx:105-106` records that the API previously faked `winRate = midpoint` for empty buckets and no longer does.
- **Weekly proposal 2 (LOCAL-vs-production provenance badge) is genuinely open, and narrower than proposed.** A badge component already exists — `apps/web/components/data-provenance-badge.tsx` — but its axis is real-vs-simulated, not environment: `export type DataProvenance = 'live' | 'mixed' | 'simulated' | 'empty'` (`:12`), derived from archive counts (`:97-99`, `:115-117`). It is mounted on `/accuracy` (`AccuracyClient.tsx:199`) and `accuracy-stats-bar.tsx:215,232` — **not** on track-record. The LOCAL-vs-production distinction still lives only where the proposal said it did: `scripts/compute-strategy-decay.py:46`, verbatim `"LOCAL scanner/dev samples — NOT production Railway Postgres."`. So the work is "add an orthogonal environment axis to the existing badge and mount it on track-record", not "build a badge" — reuse-first, materially smaller than the proposal implies.
- **Weekly proposal 1 (decay delivery-gate) is open, and its stated prerequisite is weaker than recorded.** Yesterday's standup carried the graph's `alert-channels.ts` ↔ `email-sender.ts` import cycle forward as "break the cycle before wiring a suppressor there". Direct read: `apps/web/lib/email-sender.ts:3` imports back only as `import type { AlertSignal }` (erased at compile), and `apps/web/lib/alert-channels.ts:184` reaches the other direction through a **dynamic** `await import('./email-sender')` inside `sendEmail()`. The static extractor sees a 2-file cycle; at runtime there is no module-initialization hazard. The decay gate can hook `alert-channels.ts:206` (`case 'email':`) without a cycle-break landing first. Recorded so the prerequisite does not calcify into a phantom blocker.
- **Loop-scheduler incident (08-14 → 08-20) — still needs owner attention, now with a recovery signal.** Per `loops/state/tradeclaw.STATE.md`: no daily standup ran 08-14 → 08-19 (6-day gap after 08-13), then 08-20 produced three consecutive FAIL(2) runs (daily 06:06, weekly 06:12, daily 09:30; all `claude exit 1`). `docs/automation/standups/2026-08-20.md` is **still absent** from both tracked and untracked trees (re-checked this run), consistent with gate FAIL-on-missing-output. 08-21 weekly + daily both PASSED, and today's run is healthy — so the harness is functioning again, but the root cause of the 6-day dark period and the triple failure remains undiagnosed (run logs are outside the unattended read scope). Operational escalation, not a code defect.
- **Uncommitted drift, owned by parallel sessions — LEFT UNTOUCHED: 108 tracked files (+7974 / −2726) — byte-identical to 08-21 and 08-13.** Δ0 across every successful measurement since 08-11. Under the 110-file flag threshold. **Corrected split: 105 modified + 3 deleted = 108** per `git diff --name-status` (authoritative). `git status --porcelain` reports 106 `M` + 3 `D` = 109; the extra row is a stat-dirty CRLF-only entry that `git diff` correctly filters after normalization. Prior standups recorded "106 + 3 = 108", which does not add up — the diff-based split is the one to carry forward. Deletions unchanged: `Dockerfile.fly`, `apps/web/app/fly/FlyClient.tsx`, `apps/web/app/replit/ReplitClient.tsx`. File-level attribution of the modifications remains unrecoverable (standups record counts, not inventories).
- **Primary-tree theme churn overlaps SHIPPED code.** The in-place churn in this checkout (`theme-toggle.tsx`, `globals.css`, `layout.tsx`, navbar/mobile-nav) predates and is very likely redundant with PR #214, merged to `main` on 08-11. **Do not commit it from the primary tree** — it would duplicate or fork merged #214 work. Reconcile against `origin/main` before treating any of it as unhomed cargo.
- Untracked: **34 porcelain rows — flat** vs 08-21 and 08-13. Every stranded lane re-confirmed: `product-i18n` (lib modules + tests + `product-locale-switcher.tsx` + e2e specs — a large uncommitted localization feature), the slow-gate research helper lane (`slow-gate-cli/assembly`, `recost-report` + tests + the 07-18 plan doc + the `f4.json` artifact), the two regime-study screenshots, orphaned standups 07-13/14/15 (retired; double-commit guard holds), editorial art assets, plus untracked `CLAUDE.md` / `.gitattributes` and a `tmp/` dir.
- **`feat/premium-ui-overhaul` retire-or-integrate — decision-ready, re-confirmed from trees this run.** The branch is 74 behind and provably lacks the shipped TC-254 ledger: `git ls-tree` on this branch stops at `apps/web/migrations/053_drop_monetization.sql`, while `origin/main` carries `054_d1_alpha_ledger.sql`, `apps/web/lib/d1-alpha-ledger.ts`, `apps/web/lib/__tests__/d1-alpha-ledger.test.ts`, and `docs/plans/2026-08-10-d1-alpha-ledger.md` (PR #211, `25c30665`). Unique unshipped product cargo here = `b061bef3` only (one UI commit, near-certainly superseded by TC-244…TC-254 + #214) + the loop-doc ledger + the 108-file unhomed drift. Do not rebase this branch onto a 74-ahead `main`.
- **2 recovery stashes retained** (tc254, tc253 restack) — do not drop until owner handoff acceptance.
- graphify extraction is **47 days stale** (2026-07-06); `--update` skipped this run (see highlights).

## NEXT

- **Owner: strike weekly proposal 3 from the decision queue — it is already live.** `/calibration` ships band-vs-realized win-rate, Brier, ECE, and a below-floor honesty state, publicly and link-reachable, on `main` today. Re-reading it as "to build" costs owner attention and risks a duplicate implementation. If anything remains, it is a discoverability or copy question about a page that already exists, not a build.
- **Owner: re-scope weekly proposal 2 to "add an environment axis to the existing provenance badge, mount it on track-record."** `DataProvenanceBadge` already exists and is tested; it just answers a different question (real vs simulated) on different pages (`/accuracy`). The LOCAL-vs-production fact is still trapped in `compute-strategy-decay.py:46`. Smallest honest version: one new provenance value set (`production-verified` | `local-sample`) machine-derived from the data source, rendered on track-record. This is the highest-value remaining proposal — it converts a stated brand claim (LOCAL ≠ production) into a visible one.
- **Owner: weekly proposal 1 (decay delivery-gate) is the largest remaining lane and is unblocked.** The alert-channels ↔ email-sender cycle is type-only plus a dynamic import — not a runtime initialization hazard — so the suppressor can hook `alert-channels.ts:206` without a prior refactor. It must compute on production Postgres, not the LOCAL sample, and must keep demoted strategies visible-as-demoted (auto-demote is a transparency feature, not a hiding mechanism). Branch from `origin/main`, never this 74-behind tree.
- **Owner: investigate the 08-14 → 08-20 loop-scheduler gap + 08-20 triple FAIL(2).** Two consecutive healthy runs (08-21, 08-22) suggest the harness recovered on its own, which makes diagnosis time-sensitive — the evidence is in run logs that are outside the unattended read scope and may rotate. The repo is clean (drift byte-flat across the whole incident), so this is a scheduler/harness matter, not a code fix.
- **Owner / monitor: TC-254 is the live prospective candidate — monitoring only.** Verify a daily-cadence row lands for each closed D1 bar (epoch fixed at the 2026-08-09 bar). Investigate any missing row or integrity error **without rewriting history**. Do NOT tune from prospective observations, backfill/rewrite the ledger, auto-promote the candidate, delete the losing observed archive, or infer broker execution/performance. Gate: 0/365 days, 1/365 snapshots, 0/12 closed trades against the 365/365/12 thresholds now re-verified in code.
- **Owner: retire-or-integrate `feat/premium-ui-overhaul` — case unchanged, sharper each cycle.** 74 behind; research and theme cargo shipped to `main` (squash-duplicated here); only unique unshipped value = `b061bef3` (very likely superseded) + loop-doc ledger + 108-file drift needing per-lane homes. Either extract whatever `b061bef3` holds that is not already merged, or retire the branch.
- **Owner: the `product-i18n` localization lane is the largest stranded untracked feature** (20+ files with tests) — needs a branch-or-discard call so it stops riding as unhomed drift.
- Parallel-writer watch: tracked **108** (byte-flat, fifth consecutive measurement), untracked **34 rows** (flat). Flag if tracked breaks **110** or untracked breaks **40 rows**.
- Refresh graphify in an interactive session with `GEMINI_API_KEY` set — the extraction is now 47 days stale and predates TC-24x, the D1 ledger, and #214.

## graphify highlights (from graphify-out/GRAPH_REPORT.md, 2026-07-06 extraction — 47 days stale)

- `--update` **skipped this run.** `GEMINI_API_KEY` presence could not be verified — three separate probes (`env`, `Get-ChildItem Env:`, `[Environment]::GetEnvironmentVariable`) were each refused by the unattended allowlist. Given the standing config is unset and an unkeyed `--update` spawns heavy LLM subagents that gateguard blocks headless, the skip is the safe branch either way. Per the loop contract: logged, continued, never fail the run over graphify. Treat the numbers below as indicative, not current — they were read directly from the report file this run, but the underlying extraction predates TC-24x, the D1 ledger, and #214's theme structure.
- **God Nodes (core abstractions):** `readSessionFromRequest()` 114 edges, `execute()` 62, `query()` 60, `getUserById()` 49, `requireCronAuth()` 46, `getTrackedSignals()` 42, `queryOne()` 41, `getTrackedSignalsForRequest()` 34, `getUserTier()` 33, `getOHLCV()` 32. Auth/session + DB-query + signal-tracking remain the coupling hubs; `requireCronAuth()` this high aligns with the authenticated D1 ledger cron writer — a change there fans out widely.
- **Surprising connections:** backtest `comparison-chart.tsx` and `metrics-table.tsx` → `packages/strategies/src/run-backtest.ts` (both via `BacktestResult`, EXTRACTED); `atr-calibration-cache.ts` → `packages/signals/src/atr-calibration.ts` (INFERRED); two INFERRED `pad()` edges into `packages/agent/src/cli/cli.ts` that look like extractor name-collision noise rather than real coupling. No new connections — the file is unchanged since 07-06.
- **Import cycles:** four, unchanged — three in `packages/agent/src/channels/base.ts` ↔ `webhook` / `discord` / `telegram`, plus `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`. **The product-side cycle is a static artifact, not a runtime one** (type-only import one way, dynamic `await import()` the other — verified this run). That downgrades it from "blocker for the decay-gate lane" to "cosmetic graph noise"; the three `channels/base.ts` cycles were not re-verified and may or may not be real.

## Sync result

- **Standup doc committed to `feat/premium-ui-overhaul` by explicit path** (`docs/automation/standups/2026-08-22.md`, single file). No `feature → main` PR opened: consistent with 30+ prior daily runs — `feature → main` is owner-gated, the branch's unique cargo is redundant/stale (`b061bef3` + loop docs), and a PR of 48 mostly-duplicate commits would be noise, not signal. Recorded as owner-gated, not silently skipped. No open PR targets this branch head (checked this run via `gh pr list`).
- Commit SHA + push confirmation recorded in the run trailer below.

## Lessons / Escalations (state file is machine-record-only; lessons recorded here per the sensitive-path pattern)

- **State-file note:** `loops/state/tradeclaw.STATE.md` is readable and holds only the launcher's machine run-records (no lessons/escalations headings). It confirms 08-21 daily = PASS and the 08-20 triple FAIL(2). Loop STATE.md **writes** remain permission-denied here (sensitive path), so lessons are captured in this committed doc, matching established behavior. Continuity also holds via MemoryCore + the auto-memory index.
- **LESSON (new, methodological):** the daily loop must verify the weekly loop's carried-forward proposals against live code before re-listing them as decision-ready. Proposal 3 was already fully shipped — page, API, Brier/ECE, below-floor honesty state, nav + sitemap links, and route tests — and had been carried forward as work to do. Re-proposing shipped work is the same integrity failure class as cherry-picking a win: it misrepresents the true state of the product. Verification cost was four grep/read calls.
- **LESSON (new, measurement):** on this autocrlf tree, `git status --porcelain` over-reports the modified count by one versus `git diff --name-status` (106 vs 105). Use `git diff --name-status` for the modified/deleted split; the porcelain count double-counts a stat-dirty entry whose content is unchanged after normalization. This corrects the "106 + 3 = 108" split repeated in prior standups.
- **LESSON (new, graph hygiene):** a static import cycle reported by graphify is not automatically a runtime cycle. `alert-channels.ts` ↔ `email-sender.ts` is type-only in one direction and a dynamic `await import()` in the other. Verify direction and import kind before treating a graph cycle as a prerequisite blocker.
- **ESCALATION (operational, open):** the daily loop was dark 08-14 → 08-19 and FAILED all three 08-20 runs (gate FAIL(2), `claude exit 1`); `2026-08-20.md` was never written. Two healthy runs since (08-21, 08-22) with no repo damage (drift byte-flat throughout). Root cause still undiagnosed and the evidence may rotate — owner should pull the run logs soon.
- **ESCALATION (decision-ready, standing):** retire-or-integrate `feat/premium-ui-overhaul`. 48 ahead / 74 behind; the branch provably lacks the shipped TC-254 ledger (migrations stop at 053; 054 + `d1-alpha-ledger.ts` are on `main` via #211). Unique product cargo = `b061bef3` only, very likely fully superseded.
- **ESCALATION (open):** Pro-funnel instrumentation seed (weekly 08-07 P4) — measure whether the checkout-resume fix recovers conversions — still open.
- **ESCALATION (standing):** the `product-i18n` localization lane (20+ untracked files with tests) is the single largest stranded feature in the dirty checkout — needs an owner branch-or-discard call.
- **MONITOR (live boundary):** TC-254 prospective D1 ledger is `collecting-evidence` / `not-promoted` in production. Strict no-tune / no-backfill / no-promote / no-archive-deletion boundary. Any missing daily row is a monitoring event, not a repair opportunity.
- **LESSON (standing, re-confirmed):** headless production re-probe is approval-gated (external `curl` not in the unattended allowlist) — same class as the Cloudflare headless block. Do not claim fresh live probes in a scheduled run; cite the owner verification record and independently confirm what git + `gh` can prove (main tip, open/merged PRs, remote branch tip, file presence in trees).
- **OBSERVATION (this run):** git-state deltas were near-zero again (main static, drift byte-flat, untracked flat, stashes unchanged). The value this cycle came from spending the quiet window verifying the loop's own prior output against live code rather than restating it. That is the right use of a quiet cycle; scope still stayed at status + a single doc commit.

---
Run trailer:
- Commit: `abe6a244` on `feat/premium-ui-overhaul` (1 file, +74). Parent `e6d7be78`.
- Push: `e6d7be78..abe6a244` → `origin/feat/premium-ui-overhaul`; remote tip verified `abe6a2443852b8d90e4debf965bcfa3bd004ede8` == local HEAD (`git ls-remote`), upstream back to 0/0.
- feature → main: no PR opened (owner-gated; branch cargo redundant/stale). Protected `main` untouched (static at `055107ad`).
