# TradeClaw Daily Standup — 2026-07-11

Branch: `loop/standup-2026-06-26` · HEAD `acd2fee0` · **diverged 6 ahead / 28 behind** `origin/loop/standup-2026-06-26` (remote tip `2aa098ee`, unchanged since 07-03) · **6 ahead / 33 behind `origin/main`** (main tip `3abe45ff` = squash-merge of PR #153).
Scheduled unattended run, 09:30 GMT+8. Status + safe sync only — no product code, no migrations, no deploys.

## Ground truth
- Loop branch is NOT protected — safe to commit. No stashes, no in-progress merge/rebase. Branch + HEAD re-verified `acd2fee0` immediately before committing (concurrent-writer check passed — unchanged through the run).
- The "6 ahead" = weekly `5684434e` + dailies `3dff01e4`, `db0bf277`, `8b3d3484`, `38cf980d`, `acd2fee0`. All six already superseded to remote via `loop/standup-2026-07-04` + **PR #149** (lane tip advanced to `da6e6448` when the 07-08 standup was cherry-picked) — nothing stranded off remote.
- **Gap in the daily record:** 07-09 daily gate-FAILED and 07-10 was the weekly run (also gate-FAILED per the loop state file); neither produced a committed standup. This file resumes the daily cadence — last committed standup was 07-08 (`acd2fee0`).
- **State-file append still blocked** (`loops/state/tradeclaw.STATE.md` readable, write denied) — 8th run. Lessons + escalations carried in the memory chain.
- **Launcher cwd quirk persists** (harness cwd `C:\Windows\system32`; repo ops via `git -C` + absolute paths; `cd && git` trips the untrusted-hook prompt; only `git`/`gh`/`date`/`graphify` bash are allowlisted, so `cat`/`echo`/`head`/`find` are denied — used Read/Glob/Grep instead).
- Working-tree drift (owner-reserved, reported not captured): **10 modified (+2,106/−140)** — `implementation-log.md` append now +1,547 (~+400 in the window) — plus **6 untracked**. NEW since 07-08: `package.json` is now modified (+1), and `scripts/research/recost-segment.ts` + its `__tests__/recost-segment-cli.test.ts` now show as **untracked** (07-08 saw the `.ts` as tracked-drift; a concurrent writer appears to have changed its tracked state). Untracked source is product code — excluded from this docs-only commit per `neverDo`.
- Worktrees: main tree + 7 under `.claude/worktrees` + 2 external (10 total). Stale trio (`pr135-merge`, `pr144-fix`, `deploy-main` — all detached HEAD) plus `audit-port`, `zaky-recost`, `oss-pivot-1`, `btc-daily-sleeve` unchanged. Prune candidates carried.

## DONE (last ~24h)
- **PR #153 MERGED to main** (squash `3abe45ff`, 2026-07-10 14:51 UTC) — **Phase 2 transparency pivot**: the tier system is removed, everything is free, partner content goes dark. CI + Docker Smoke + Docker Publish all **green** on the merge commit (unlike #152, which merged with a red E2E). This is the `feat/oss-pivot-2-detier` branch that 07-08 flagged as staged-but-uncommitted in the `oss-pivot-1` worktree — it landed.
- Open-PR surface shrank to two: **#149** (loop-standup supersession lane, OPEN) and **#143** (DRAFT). #150/#152/#153 are all merged.
- No Stripe/Postgres mutations or migrations observed in the window from any lane.
- Only loop-branch commit in the strict 24h window is `acd2fee0` (the 07-08 standup) — the substantive change this cycle is the #153 merge on main, external to this branch.

## ESCALATIONS (production-facing — both carried from 07-08, both re-measured worse)
1. **Deploy gap is now the top brand contradiction — production still sells three paid tiers the repo has fully retracted.** Fresh probe this run (2026-07-11): `tradeclaw.win` homepage still serves the **Pro $29/mo** funnel verbatim — "Start 7-day Pro trial · Card required. Cancel anytime before day 8", "No delay — Pro signals fire the same second", "Private Pro group — invite arrives at checkout", "Full exit plan — TP1/TP2/TP3 + SL". `tradeclaw.win/pricing` still shows **Free / Pro $29/mo ("Most Popular") / Elite $99/mo ("For Power Users")** with annual billing. Phase 1 (#152, merged 07-06) deleted the pricing/Pro surfaces and Phase 2 (#153, merged 07-10) made everything free — **neither is deployed.** Production is ~5 days behind Phase 1 and ~1 day behind Phase 2. Every hour, the live site makes tiered paid offers the codebase has withdrawn — the sharpest possible violation of "radical transparency / measurable proof." **Deploy main or publish the deploy schedule.**
2. **All GitHub-Actions cron dispatchers still failing — ~8 continuous days.** Re-measured this run: `Push Signals` and `Signal Alerts` are red as recently as **2026-07-11 01:07 UTC** and **00:56 UTC**; `Telegram Broadcast` and `Signal Log` also red in the window. Root cause last confirmed 07-08 (HTTP 403 on `https://tradeclaw.win/api/cron/*`, lead hypothesis a `CRON_SECRET` mismatch between repo secrets and the deployed app — not re-pulled this run to conserve budget; the failure status is the measured fact). Brand-critical: **`Daily Track Record Post` is part of the public verified record and has not been auto-posting.** Whether signal delivery runs via a platform-native cron instead is still **pending owner confirmation** — "pipeline down" is not yet established, only "GH crons down."

## PENDING (not finished)
- **Two PRs open**: **#149** (MERGEABLE; carries 07-04/05/06/07/08 standups + stranded 07-03 weekly; today's standup folds in via the same lane) and **#143** (DRAFT since 06-28; signal-worker ADR + unwired flag scaffold).
- **Loop-branch disposition** still open and compounding (6/28 vs its own remote, **6/33 vs main** — grew from 5/32 as #153 landed). The whole loop-branch approach is now superseded by two merged pivots; the daily loop should re-anchor on a fresh branch off main. Decision packet `docs/ai-improvement/branch-sync-disposition-handoff.md` remains **untracked**, owner/Fatin.
- **Drift-lane collisions** (owner-reserved, unchanged in character): `apps/web/public/readme-banner.svg` local mod would revert an on-brand banner; the two 07-07 draft docs are redundant vs merged copies; `scripts/research/recost-segment.ts` + test are an untracked duplicate of a landed diagnostic. Wholesale `git add` would be actively harmful — hence path-scoped commit only.
- **07-03 weekly proposals still ungraded** (grill gate) — pending #149 landing.
- **The uuid E2E fixture** (`tests/e2e/api/signals.spec.ts`) merged-around in #152 still re-reds future PRs until repaired (owner, fresh branch off main).
- State-append block (8th run), stale-worktree cleanup, launcher cwd/allowlist quirk, graphify LLM-key gap — all carried.

## NEXT
1. **Owner: deploy main.** Phase 1 + Phase 2 are both merged and green; production is the only thing still selling the retracted tiers. This is the highest-leverage transparency action open.
2. **Owner: run down the cron 403s** — compare repo `CRON_SECRET` against the deployed value; confirm whether `Daily Track Record Post` / signal delivery run via platform cron or are genuinely down. If down, the public record has not auto-posted for ~8 days.
3. **Owner/Fatin: pick the branch-sync disposition** and re-anchor the daily loop on a fresh branch off main (6/33 and compounding; two pivots have obsoleted this branch).
4. **Owner: merge #149**, then decide #143 (draft since 06-28).
5. **Owner: fix the uuid E2E fixture** on a branch off main; reconcile the banner collision; drop the two redundant drafts and the recost duplicate; prune the stale detached worktrees (`pr135-merge`, `pr144-fix`, `deploy-main`).
6. **Operator: grant `loops/state/*.STATE.md` write** (or bless the memory chain as the durable record); set `GEMINI_API_KEY` for the launcher so graphify `--update` can semantically extract changed docs (this run's specific block).

## graphify highlights (NOT refreshed — standing 07-06 graph)
- One-line failure log per contract: `graphify /c/Ai/tradeclaw --update` ran the incremental scan (**22 code, 29 docs, 1 paper changed; 1 deleted; 1,718 unchanged** since the 07-06 graph — confirms the drift lane touches code, not just docs) then exited 1: **"no LLM API key found (30 doc/paper/image files need semantic extraction)"**. New, cleaner diagnosis than prior runs — not the PowerShell/cwd block (graphify 0.8.46 ran fine on PATH), the miss is `GEMINI_API_KEY`. Run not failed over graphify.
- Standing 07-06 graph: **11,497 nodes / 18,049 edges / 845 communities**, 98% EXTRACTED / 2% INFERRED (392 edges, avg confidence 0.8).
- **God Nodes (top-10, standing):** `readSessionFromRequest()` 114 · `execute()` 62 · `query()` 60 · `getUserById()` 49 · `requireCronAuth()` 46 · `getTrackedSignals()` 42 · `queryOne()` 41 · `getTrackedSignalsForRequest()` 34 · `getUserTier()` 33 · `getOHLCV()` 32. `requireCronAuth()` (46 edges) is the hub implicated in the cron-403 escalation; `getUserTier()` (33) should thin on the next successful refresh now that #153 removed the tier system.
- **Surprising connections (standing, no new):** two low-confidence INFERRED `pad()` edges into `packages/agent/src/cli/cli.ts` persist (name-collision artifact — watch, don't act).
- **Import cycles (4, unchanged since 06-30):** `packages/agent/src/channels/base.ts` ↔ `{webhook,telegram,discord}.ts`; `apps/web/lib/alert-channels.ts` ↔ `email-sender.ts`.

## Sync result
- Committing `docs/automation/standups/2026-07-11.md` **only**, by explicit path, on `loop/standup-2026-06-26`. No `git add -A`. Drift lane left uncaptured (owner-reserved; banner collision + untracked source make wholesale capture actively harmful and `neverDo`-prohibited).
- **Push of `loop/standup-2026-06-26` stays blocked** (6/28 non-fast-forward; rebase/reset/force prohibited by the disposition handoff and the loop safety contract).
- **Supersession:** today's standup cherry-picked onto `origin/loop/standup-2026-07-04` (PR #149's branch) via a throwaway detached worktree, FF-pushed, worktree removed. Exact SHAs in the PR #149 comment.
- **ultracode note:** the `ultracode` keyword opts this turn into Workflow orchestration, but `Workflow` is not in the launcher's allowlist (`git`/`gh`/`date`/`graphify` bash + Read/Write/Edit/Glob/Grep/WebFetch/WebSearch) and this is a non-interactive run — a fan-out would hit the approval wall, and every claim above is first-hand `git`/`gh`/WebFetch output, so a verification fleet would only re-derive verified facts. Fleet intentionally skipped, consistent with prior standups.
