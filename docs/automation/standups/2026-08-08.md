# Daily Standup — 2026-08-08

- Branch: `feat/premium-ui-overhaul` @ `756098a2`
- Upstream: `origin/feat/premium-ui-overhaul` — local is **0 ahead / 0 behind**. Fully pushed at run start; no stranded loop docs this cycle (the 08-07 run backed up the weekly doc, and nothing new landed on this branch since).
- vs `main`: **36 ahead / 61 behind** (was 35 / 59 on 08-07). Ahead +1 = the 08-07 standup doc (`756098a2`). Behind +2 = two squash merges landed on `main` yesterday afternoon (see DONE). Still exactly one product commit in the ahead set (`b061bef3`, 2026-07-16).
- `origin/main` movement (last 24h): **+2, meaningful.** Tip moved `81a5f3f3` → `58eee2b9`: `ac74bd82` = PR #199 (pre-registered regime-expectancy study) and `58eee2b9` = PR #200 (regime-kill published to the public research ledger). First main movement since 08-05, and it ships the weekly P1 proposal.
- Stashes: none. In-progress merge/rebase: none (`.git/MERGE_HEAD` / `rebase-merge` / `rebase-apply` absent). Concurrent-writer check: HEAD re-verified `756098a2` immediately before commit. The ~60-worktree fleet is unchanged and untouched; a new sibling `C:/Ai/tradeclaw-regime-study-pr` sits on `agent/d1-slow-gate-build-spec` (`5ea26489` = PR #202's branch).
- Run window: scheduled unattended, 09:30 GMT+8. Provenance stamp: 2026-08-08 09:36 MPST (UTC+0800).
- Orchestration note: `ultracode` requested. Holds as before (verified 07-23 → 08-08): the dynamic-workflow review gate blocks headless Workflow fan-out for a daily standup, and status+sync over a tree shared with parallel writers is correct done inline — reconcile + safety-guard skills, direct git reads, single mutation in the main loop. Fan-out would add no coverage here.

## DONE (last 24h)
- **Weekly P1 proposal SHIPPED to `main`.** PR #199 merged 2026-08-07 15:28 UTC (`ac74bd82`): the pre-registered regime-expectancy study — the 13-commit research lane that was stranded on this 59-behind branch — got exactly the focused `scripts/research` + `docs/research` PR the 08-06/08-07 standups escalated for. PR #200 merged 16:24 UTC (`58eee2b9`): the regime-filter kill is now on the public research ledger with its integrity gates and the 36.8% coverage limit stated. The standing "give the regime study a home toward main" escalation is **RESOLVED**; internal study → public kill-ledger entry is the exact brand posture (losses published, not hidden).
- **Weekly P2 opened as PR #201** (`agent/p2-reconstruction-coverage`, `61596752`): methodology-page disclosure that independent reconstruction covers 1,162/3,157 counted trades (36.8%), crypto-only; 1,995 trades across 20 non-crypto pairs excluded for lack of a candle feed, fail-closed. All checks green, MERGEABLE — awaiting owner merge.
- **D1 slow-gate build spec opened as PR #202** (`agent/d1-slow-gate-build-spec`, `5ea26489`): propose-only plan doc for the build the regime study commissioned; owner gates before any code and again before any activation. All checks green, MERGEABLE. Per the recorded weekly disposition (2026-08-08 escalations note): P1 merged, P2 = #201, P3 (QA-gates trust module) deferred, P4 (registered-report positioning) owner-halted, and #202 carries 4 owner decisions in its §7.
- 08-07 daily standup committed and pushed (`756098a2`) — twelve consecutive clean daily writes (07-28 → 08-08).
- No product code advanced on this branch since `b061bef3` (2026-07-16). All forward motion this window happened on `main` and the two agent PR branches — which is the healthy direction.

## PENDING (not finished)
- **PR #201 (coverage disclosure): green and mergeable — one owner click from public.** Highest-leverage open item; it is the P2 half of the transparency pair whose P1 half already shipped.
- **PR #202 (D1 slow-gate spec): green, propose-only.** The 4 owner decisions in its §7 gate the entire D1 slow-gate build. No code moves until they're made.
- Uncommitted drift, owned by parallel sessions — LEFT UNTOUCHED: **105 tracked files (+7710 / −2691)** — byte-for-byte flat vs 08-06 and 08-07, a **third consecutive identical reading**; the tracked lanes (premium UI, deploy/self-host cleanup, WS/price, AI-improvement/recost/regime) are paused, not churning.
- Untracked: **32 porcelain rows / 63 files — first movement in 5 cycles** (down from a flat 57 rows). Every previously-flagged stranded lane re-confirmed present: `product-i18n` (≈25 files incl. tests + switcher + e2e spec), slow-gate research lane (`slow-gate-cli/assembly`, `recost-report` + 3 tests + the 07-18 plan doc), orphaned standups 07-13/14/15 (retired, double-commit guard holds), editorial art assets. The −25 rows are consistent with regime-study/QA temp artifacts being tidied around the #199/#200 merges; exact file-level attribution is unrecoverable because prior standups recorded counts, not inventories (lesson below).
- Unpushed-to-`main` feature commit `b061bef3` — the redundancy case got **stronger**: the branch's other unique cargo (the regime study) has now shipped to `main` independently, so `b061bef3` + loop docs + unhomed drift is all that remains here.
- Open PRs now **9** (was 7): new #202, #201; carried #198 (ia-consolidation "front door match the product"), #184, #174, #170, #160, #149, #143. None target this branch.
- Committed-standup gap 07-24 → 07-27 remains a closed hole; no backfill.

## NEXT
- **Owner: merge call on PR #201** — green, mergeable, completes the P1+P2 transparency pair. Keep the published numbers exactly as measured (1,162/3,157, 36.8%, 1,995 excluded).
- **Owner: decide the 4 §7 gates in PR #202** to unblock the D1 slow-gate build — the only surviving lever per the study (horizon, not filtering). Until then the untracked slow-gate lane in this tree stays read-only research.
- P3 stays deferred and P4 owner-halted per the recorded disposition — no loop action.
- Owner decision (gated), now sharper: **retire or integrate `feat/premium-ui-overhaul`.** 61 behind, research cargo shipped via #199/#200, one likely-redundant product commit (`b061bef3` vs seven artwork releases on `main`), plus 105-file drift that needs per-lane homes regardless. The branch's reason-to-exist has narrowed to "status-doc ledger + drift host".
- Home the two coherent untracked lanes: `product-i18n` (≈25 files, test-carrying) and the slow-gate research lane — clean independent PRs once the owner picks branch points (slow-gate naturally waits on #202's §7).
- Watch parallel-writer churn with a **reset baseline**: tracked 105 (flat 3 cycles), untracked 32 rows. Flag if tracked breaks 110 or untracked breaks 40 rows.
- Refresh graphify in an interactive session — the extraction is now 33 days stale.

## graphify highlights (from graphify-out/GRAPH_REPORT.md, 2026-07-06 extraction — 33 days stale)
- Incremental `--update` intentionally skipped again this run (gateguard-blocked LLM subagents; would ingest other sessions' 105-file uncommitted drift; `GEMINI_API_KEY` presence probe again permission-blocked from the loop's allowlist). Highlights embedded from the last committed extraction (11,497 nodes · 18,049 edges · 845 communities).
- God Nodes: `readSessionFromRequest()` 114 edges, `execute()` 62, `query()` 60, `getUserById()` 49, `requireCronAuth()` 46, `getTrackedSignals()` 42, `queryOne()` 41, `getTrackedSignalsForRequest()` 34, `getUserTier()` 33, `getOHLCV()` 32. Auth/session + DB access dominate the coupling surface.
- Surprising connections: backtest UI props (`ComparisonChartProps`/`MetricsTableProps`) bind directly to `BacktestResult` in `packages/strategies/src/run-backtest.ts`; `refreshAtrCalibration()` couples the web ATR cache to `packages/signals/src/atr-calibration.ts`.
- Import cycles worth breaking: `packages/agent/src/channels/base.ts` ↔ `webhook.ts`/`discord.ts`/`telegram.ts` (three cycles through the channel base), `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`.

## Sync result
- Committed this standup on `feat/premium-ui-overhaul` by explicit path (standup doc only; 105 tracked + 32 untracked rows of parallel-session drift untouched).
- Pushed to `origin/feat/premium-ui-overhaul` after re-verifying HEAD; remote-tip advance confirmed via `git ls-remote`.
- No commit or merge to `main`. Still deliberately not opening a feat→main PR from this 61-behind status branch — yesterday's rationale is now vindicated: the valuable cargo (regime study) shipped via its own focused PRs #199/#200 instead of riding a junk 36-commit PR.

## Lessons / escalations (this run)
- LESSON: the escalate-and-wait pattern paid off in one cycle — the regime study stranded on this branch since 08-05 shipped to `main` via a focused PR (#199) plus a public disposition (#200) within 24h of the weekly proposals naming it. A status loop that keeps a clean, loud escalation beats one that forces integration itself.
- LESSON: untracked-row deltas are only lane-attributable, never file-attributable, because standups record counts + named-lane presence, not full inventories. That trade-off is accepted (a 60-file inventory per day is noise); the presence checks on the named stranded lanes are what make a count drop like 57→32 diagnosable as "temp artifacts tidied, lanes intact".
- ESCALATION (owner, new): **PR #201 is green and mergeable** — the coverage disclosure is one click from public. **PR #202's §7 holds 4 owner decisions** gating the D1 slow-gate build. Both are the direct continuation of the shipped P1.
- ESCALATION (owner, standing, sharpened): retire or integrate `feat/premium-ui-overhaul`. With the research cargo gone to `main`, remaining unique value = `b061bef3` (likely redundant vs TC-244..249) + loop-doc ledger + unhomed drift. Confirm or retire; do not rebase.
- ESCALATION (owner, standing): the drift lanes (`product-i18n` ≈25 files, slow-gate research lane, deploy/self-host cleanup, premium-UI tracked set) still have no committed home — 16 days open, size- and churn-stable. Per-lane commit-and-PR or documented discard.
- NOTE: the launcher's global state file remains sensitive-path-blocked from the loop; durable lessons/escalations live here in the committed standup doc, as before.
