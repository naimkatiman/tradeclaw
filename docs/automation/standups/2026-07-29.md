# Daily Standup — 2026-07-29

- Branch: `feat/premium-ui-overhaul` @ `8de16d43`
- Upstream: `origin/feat/premium-ui-overhaul` — in sync (0 ahead / 0 behind local; remote tip re-verified via `git ls-remote` = local HEAD `8de16d43b939…` before the commit below).
- vs `main`: 11 ahead / 40 behind. The 11-ahead set is 10 standup/research docs + one feature commit (`b061bef3 feat(ui): unify premium assets with emerald brand`, 2026-07-16). Verified: `b061bef3` is still the ONLY non-doc/non-research commit ahead of `main`.
- Stashes: none. In-progress merge/rebase: none (`.git/MERGE_HEAD` / `rebase-merge` / `rebase-apply` absent). Concurrent-writer check: HEAD unchanged across the run (re-verified immediately before commit). ~40 sibling/`.claude` worktrees exist on this host — none is this branch's tree; left untouched.
- Run window: scheduled unattended, 09:30 GMT+8.
- Branch tip ADVANCED since the 07-23 standup: `4ca71153` → `8de16d43`. Unlike the prior 5-day static stretch, the tip moved once — because yesterday's 07-28 standup committed and pushed cleanly. The vs-`main` counts moved 10→11 ahead (that new standup commit) / 39→40 behind (`origin/main` advanced one commit). No new product work on this branch.
- Orchestration note: `ultracode` requested. Per the established gate on this loop, an unattended `ultracode`/Workflow fan-out for a daily standup cannot clear the dynamic-workflow manual-review gate headless, so I did the read-only investigation inline and kept the single git mutation in the main loop. No coverage lost: ground truth was established twice (reconcile skill + direct git reads), and single-context git is the correct model when parallel writers share this one working tree.

## DONE (last 24h)
- One commit landed: `8de16d43 docs(standup): daily standup 2026-07-28` (`git log --since="24 hours ago" --no-merges` shows exactly this). This is the loop's own status doc — the 07-28 run wrote AND committed AND pushed successfully (gate `PASS`).
- Recovery signal: 07-28 is the first successful committed standup since 07-23. The write+commit path that failed on 07-27 (double `FAIL(2)`, no file written) worked on 07-28 — so that failure was intermittent, not a permanent harness break. The write path is currently healthy (this run is further proof).
- No product code advanced on `feat/premium-ui-overhaul` since `b061bef3` (2026-07-16). This remains a status branch the loop is not driving forward.
- Nothing merged to `main` in the last 24h attributable here. `origin/main` advanced by one commit (behind 39→40) — an owner/parallel-lane change, not this loop's. Newest owner merges on record remain PRs #177–#181 (2026-07-19).

## PENDING (not finished)
- Committed-standup GAP 07-24 → 07-27 — four missing days, now a CLOSED hole (not reopened by 07-28's success). No scheduled runs wrote files for 07-24/07-25/07-26; 07-27 fired twice (`06:15` and `09:30`), both exited 1 / gate `FAIL(2)`, neither wrote `2026-07-27.md` (absent on disk and in history). Same class as the 07-19/07-20 permanent hole. Treated as permanent; no backfill (fabricating past-day status would violate the no-invented-data rule). 07-28 recovering means the harness is not persistently broken — the holes are historical, the cause need not be re-chased per-day.
- Uncommitted drift, owned by parallel sessions — LEFT UNTOUCHED this run (not this loop's work): 99 tracked files modified/deleted (+5671 / −2563), 46 untracked. Vs the 07-28 record (+5562 / −2564, 46 untracked): +109 net insertions, 1 fewer deletion, untracked count identical at 46. Drift remains frozen at the file level — 99 tracked / 46 untracked unchanged for 6 days, only line-level churn. Lanes (unchanged from 07-28):
  - Premium UI overhaul (dominant tracked lane): `apps/web/app/components/`, `apps/web/components/`, `apps/web/components/landing/`, `apps/web/lib/`, locale pages (`ar`/`es`/`ms`/`zh`), `dashboard`, `screener`, `track-record`, `layout.tsx`.
  - Deploy / self-host cleanup: working-tree deletions `Dockerfile.fly`, `apps/web/app/fly/FlyClient.tsx`, `apps/web/app/replit/ReplitClient.tsx`; modified `fly.toml`, `docker-compose.yml`, `.replit`, `.github/workflows/docker-test.yml`, `.env.example`, `.env.docker.example`, `scripts/init-db.sh`, `scripts/test-docker.sh`, `packages/create-tradeclaw/index.js`, `docs/FLYIO.md` / `docs/REPLIT.md` / `docs/QUICKSTART.md`.
  - WS / price lanes: `apps/web/lib/hooks/use-price-stream.ts`, `use-websocket-prices.ts`, `apps/web/lib/signals-live.ts`, `apps/web/middleware.ts`.
  - AI-improvement / recost lane: `STATE.yaml`, `docs/ai-improvement/*`, `scripts/research/recost-segment.ts` + its `__tests__/recost-segment-cli.test.ts`.
- Untracked `product-i18n` localization layer — 21 files under `apps/web/lib/product-i18n/` (`app-shell`, `dashboard`, `dashboard-live`, `dashboard-evidence`, `screener`, `track-record`, `track-record-widgets`, `theme-state`, `marketing-nav`, `format`, `server-locale` + matching `__tests__`), plus `apps/web/app/components/product-locale-switcher.tsx` and `apps/web/tests/e2e/features/product-localization.spec.ts`. A substantial, test-carrying feature sitting entirely uncommitted with no branch home.
- Untracked slow-gate research lane: `scripts/research/slow-gate-{assembly,cli}.ts` + `recost-report.ts` and their `__tests__`, `docs/research/experiments/slow-gate-BTCUSD_ETHUSD-D1-2017-09-01-2026-07-16-f4.json`, and the plan doc `docs/plans/2026-07-18-slow-regime-gate-sandbox.md` (still untracked).
- Unpushed-to-`main` feature commit: `b061bef3 feat(ui): unify premium assets with emerald brand` — still the only non-doc commit in the 11-ahead set.
- Orphaned standups on disk, untracked here — NOT rescue-committed (verified permanently retired): `2026-07-13.md`, `2026-07-14.md`, `2026-07-15.md` each already live on other branches. Committing them here would create divergent duplicates; the rescue double-commit guard holds.
- Open PRs for this branch: none (`gh pr list --head feat/premium-ui-overhaul --state open` empty).
- Newest plan doc: `docs/plans/2026-07-18-slow-regime-gate-sandbox.md` (still untracked) — the active slow-regime-gate sandbox workstream.

## NEXT
- Owner decision (gated): integrate `feat/premium-ui-overhaul` → `main`. Branch is 11-ahead / 40-behind with one unmerged feature commit (`b061bef3`) plus the 99-file uncommitted drift. A clean integration cherry-picks or rebases `b061bef3` onto current `main`, NOT a PR of the whole 40-behind status branch (stale-base-clobber risk), and does not fold unrelated parallel drift into a standup-branch PR.
- Decide the home for the untracked `product-i18n` layer and slow-gate research lane. Both are coherent, test-carrying feature sets stranded in the working tree with no committed branch — they would make clean, independent PRs on their own and are the highest-value disposition targets after the UI drift.
- Advance the slow-regime-gate sandbox per `docs/plans/2026-07-18-slow-regime-gate-sandbox.md`. Research-surface + `currentVol` production wire-up already merged on `main`; keep observing the live vol scaler without retuning until a predeclared production sample is sufficient. No invented metrics — measured or "pending real data" only. LOCAL sandbox findings (EMA200 gate beats buy-and-hold on Calmar after costs in 3/4 folds; HMM sizing hurts) stay labelled research, never presented as the production verified record.
- The 07-27 double-`FAIL(2)` root-cause is DE-PRIORITIZED (not dropped): 07-28 and 07-29 both wrote+committed cleanly, so the harness write-path is currently healthy and the failure was intermittent. Re-open only if a fresh gap appears; do not schedule per-day catch-up re-runs (they would fabricate retroactive status).
- Refresh graphify: `GRAPH_REPORT.md` is the 2026-07-06 extraction — 23 days stale. A fresh `--update` stays deferred on scheduled runs because it spawns LLM subagents whose writes gateguard blocks on this host; queue it for an interactive session with `GEMINI_API_KEY` set.

## graphify highlights (from graphify-out/GRAPH_REPORT.md, 2026-07-06 extraction)
- Incremental `--update` intentionally skipped on this status run (spawns gateguard-blocked LLM subagents, and the drift it would ingest is other sessions' uncommitted work). Highlights embedded from the last committed extraction. Corpus at extraction: 11,497 nodes · 18,049 edges · 845 communities · 98% EXTRACTED.
- God Nodes (core abstractions to watch for coupling): `readSessionFromRequest()` 114 edges, `execute()` 62, `query()` 60, `getUserById()` 49, `requireCronAuth()` 46, `getTrackedSignals()` 42, `queryOne()` 41, `getTrackedSignalsForRequest()` 34, `getUserTier()` 33, `getOHLCV()` 32. Auth/session and DB access dominate the graph — a change to `readSessionFromRequest()` fans out across 114 edges.
- Surprising connections: `ComparisonChartProps` and `MetricsTableProps` (`apps/web/app/backtest`) both reference `BacktestResult` from `packages/strategies/src/run-backtest.ts` — backtest UI is tightly bound to the strategy runner's result shape, so a result-shape change breaks the UI. `refreshAtrCalibration()` → `calibrateAtrMultiplier()` couples `apps/web/app/lib/atr-calibration-cache.ts` to `packages/signals/src/atr-calibration.ts`.
- Import cycles (2-file, worth breaking): `packages/agent/src/channels/base.ts` ↔ `webhook.ts` / `discord.ts` / `telegram.ts` (three cycles through the channel base class); `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`.

## Sync result
- Committed this standup on `feat/premium-ui-overhaul` by explicit path (standup doc only; the 99-file drift + 46 untracked stay untouched — not this loop's work).
- Pushed to `origin/feat/premium-ui-overhaul` after re-verifying HEAD had not shifted, then confirmed the remote tip advanced to the new commit via `git ls-remote`.
- No commit or merge to `main`; feature→main integration remains owner-gated (deliberately not auto-opening a PR from a 40-behind status branch carrying unrelated parallel drift).

## Lessons / escalations (this run)
- LESSON: the harness write-path is currently healthy. 07-28 (after the 07-27 double `FAIL(2)`) and 07-29 both wrote, committed, and pushed cleanly. The write/commit failure that produced the 07-24→07-27 hole was intermittent, not a standing break — so the fix priority drops from "root-cause now" to "watch for recurrence". Do NOT re-chase a per-day cause while the path is green.
- ESCALATION (owner): the uncommitted drift on `feat/premium-ui-overhaul` is frozen (99 tracked / 46 untracked, unchanged file counts for 6 days) but still has no committed home. It contains at least three separable, test-carrying feature sets — premium UI overhaul, the `product-i18n` localization layer (21 files), and the slow-gate research lane — plus deploy/self-host cleanup. Leaving them as ambient working-tree state against a 40-behind base risks a stale-base clobber. Needs a deliberate per-lane commit-and-PR or a documented discard.
- ESCALATION (owner): feature→`main` integration is still gated and the base is drifting further (now 40 behind). The 11-ahead set carries exactly one product commit (`b061bef3`); the rest are loop docs. A clean integration cherry-picks or rebases `b061bef3` onto current `main`, not a PR of the whole status branch. The longer this waits, the larger the rebase surface.
- LESSON: `ultracode` fan-out for a daily standup is blocked by the dynamic-workflow review gate under unattended permissions. A status+sync standup is single-context git work; the safe pattern is inline read-only investigation (git reads + seeded graphify report) with the one mutation kept in the main loop. Do not spend the turn re-authoring a workflow that cannot clear the gate headless.
- LESSON: shell guard on this host blocks `env`/`printenv` on a secret name, `$VAR` expansion, unquoted `@{u}`, `%`-format `stat`, and compound `&&`/`||` chains; `Glob` times out against this repo (many `.claude/worktrees/*` + sibling `tradeclaw-*` worktrees). Use single-command git reads with explicit refs, `git ls-files --others`, and `Read` on known paths instead.
- NOTE: loop STATE file (`C:\Users\User\.claude\loops\state\tradeclaw.STATE.md`) is a sensitive path — direct edits are permission-denied under the loop sandbox, so durable lessons/escalations live here in the committed standup doc (the launcher still appends its machine run-record automatically).
