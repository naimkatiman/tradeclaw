# Daily Standup — 2026-07-22

- Branch: `feat/premium-ui-overhaul` @ `db9b0d42`
- Upstream: `origin/feat/premium-ui-overhaul` — in sync (0 ahead / 0 behind; local HEAD `db9b0d42` == remote tip, verified via `git ls-remote`).
- vs `main`: 8 ahead / 38 behind.
- Stashes: none. In-progress merge/rebase: none (`.git/MERGE_HEAD` / `rebase-*` absent). Concurrent-writer check: HEAD unchanged across the run.
- Run window: scheduled unattended, 09:30 GMT+8. Orchestration note: `ultracode` was available but not used — a deterministic single-context status+sync standup gains nothing from fan-out and it would only re-run git reads already done here.

## DONE (last 24h)
- One commit, and it is yesterday's own standup: `db9b0d42 docs(standup): daily standup 2026-07-21` (committed 2026-07-21 09:37:46 +0800). No product code advanced on `feat/premium-ui-overhaul` since `69a97b2e` (2026-07-18). This is a status branch that is not being driven forward by the loop.
- The 07-19 and 07-20 gap is now confirmed structural, not a mislabel: `git log --all -- 2026-07-19.md 2026-07-20.md` returns nothing — those standups exist on no branch and never will unless re-run. Two-day hole stands on record (both logged `FAIL(2)` in loop state).

## PENDING (not finished)
- Uncommitted drift, owned by parallel sessions — LEFT UNTOUCHED this run (not this loop's work): 99 tracked files modified/deleted (+5222 / −2562), 46 untracked. Still moving vs 07-21 (was +5031 / −2560, 26 untracked): +191 net insertions and the untracked set grew 26 → 46 overnight. Lanes unchanged from prior runs:
  - Premium UI overhaul: `apps/web/app/components/`, `apps/web/components/`, `apps/web/components/landing/`, `apps/web/lib/`.
  - Deploy/self-host config: `fly.toml`, `docker-compose.yml`, `docker-entrypoint.sh`, `.replit`, `scripts/init-db.sh`, `scripts/test-docker.sh`, `packages/create-tradeclaw/index.js`, `docs/FLYIO.md` / `docs/REPLIT.md` / `docs/QUICKSTART.md`. Working-tree deletions: `Dockerfile.fly`, `apps/web/app/fly/FlyClient.tsx`, `apps/web/app/replit/ReplitClient.tsx`.
  - WS/price lanes: `apps/web/lib/hooks/use-price-stream.ts`, `use-websocket-prices.ts`, `apps/web/lib/signals-live.ts`.
  - Recost-research lane (active this run): `scripts/research/recost-segment.ts` (+45 / −28 rewrite) plus a new `scripts/research/__tests__/recost-segment-cli.test.ts` (+44) — a segment-CLI test being added in the working tree.
- Unpushed feature commit not on `main`: `b061bef3 feat(ui): unify premium assets with emerald brand`. It is the only non-doc commit in the 8-ahead set; the other 7 are standup/research docs.
- Orphaned standups on disk, untracked here — NOT rescue-committed this run, and now verified why: `2026-07-13.md`, `2026-07-14.md`, `2026-07-15.md` each already live on other branches (`git log --all` finds `9da234a6`/`8dadd644` for 07-13, `d9b55496`/`8ec65a38` for 07-14, `755c639f`/`25420b5e` for 07-15). Committing them here would create divergent duplicates — the rescue double-commit guard held.
- Open PRs (none for this branch): #174 (draft, remotion claw bg), #170 (daily intel 07-16), #160 (rescue 5 stranded standups 07-05..07-11), #149 (loop standup 07-04 + weekly 07-03), #143 (draft, signal-worker ADR). This branch's own #168 merged 2026-07-15.
- Newest plan doc: `docs/plans/2026-07-18-slow-regime-gate-sandbox.md` (untracked) — the active slow-regime-gate sandbox workstream.

## NEXT
- Owner decision (gated): integrate `feat/premium-ui-overhaul` → `main`. Branch is 8-ahead / 38-behind with one unmerged feature commit (`b061bef3`) plus a 99-file uncommitted drift owned by parallel sessions. Needs a rebase-or-supersede call AND a disposition for the drift before any PR — do not auto-open a main PR against a 38-behind base (stale-base-clobber risk).
- Close the standup-gap question: 07-19/07-20 are gone for good; either accept the two-day hole on record or backfill by hand. Root-cause the launcher write failure that produced two silent `FAIL(2)` runs so it does not recur.
- The orphaned 07-13/14/15 need no rescue — they are already committed elsewhere; drop them from the "to-rescue" list.
- Advance the slow-regime-gate sandbox per `docs/plans/2026-07-18-slow-regime-gate-sandbox.md`. Research-surface + `currentVol` production wire-up already merged on `main`; keep observing the live vol scaler without retuning until a predeclared production sample is sufficient (no invented metrics).
- Refresh graphify: `GRAPH_REPORT.md` is the 2026-07-06 extraction — 16 days stale.

## graphify highlights (from graphify-out/GRAPH_REPORT.md, 2026-07-06 extraction)
- Full `--update` intentionally skipped on this status run: it spawns LLM subagents whose writes gateguard blocks on this host, and the 99-file drift it would ingest is other sessions' uncommitted work, not this loop's. Highlights embedded from the last extraction; a fresh extract is queued in NEXT. Corpus at extraction: 11,497 nodes · 18,049 edges · 845 communities.
- God Nodes (core abstractions to watch for coupling): `readSessionFromRequest()` 114 edges, `execute()` 62, `query()` 60, `getUserById()` 49, `requireCronAuth()` 46, `getTrackedSignals()` 42, `queryOne()` 41.
- Surprising connections: `ComparisonChartProps` and `MetricsTableProps` (apps/web/app/backtest) both reference `BacktestResult` from `packages/strategies/src/run-backtest.ts` — backtest UI is tightly bound to the strategy runner's result shape.
- Import cycles (2-file): `packages/agent/src/channels/base.ts` ↔ `webhook.ts` / `discord.ts` / `telegram.ts`; `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`.

## Sync result
- Committed this standup on `feat/premium-ui-overhaul` by explicit path (standup doc only; the 99-file drift stays untouched — not this loop's work).
- Pushed to `origin/feat/premium-ui-overhaul` after re-verifying HEAD had not shifted. No commit or merge to `main`; feature→main integration remains owner-gated.

## Lessons / escalations (this run)
- ESCALATION (owner): the uncommitted drift on `feat/premium-ui-overhaul` keeps growing under parallel writers (99 files, untracked 26 → 46 overnight) with no committed home. It carries real product changes (premium UI, deploy config, WS hooks, recost CLI). It needs a deliberate commit-and-PR or a documented discard — leaving it as ambient working-tree state risks a stale-base clobber against a 38-behind base.
- ESCALATION (owner): two consecutive silent `FAIL(2)` runs (07-19, 07-20) wrote no standup at all; the dates are absent on every branch. The daily loop is intermittently failing to write on scheduled runs — needs a look at the launcher write path / permission state, not a per-day re-run.
- LESSON (confirmed, close-out): the "rescue 07-13/14/15" item can be retired — `git log --all -- <path>` proves all three are already committed on `loop/standup-*` / rescue branches. The double-commit guard did its job; do not re-open this.
- LESSON: `Glob` times out (>20s) on this repo due to the many `.claude/worktrees/*` and sibling `tradeclaw-*` worktrees; use `git ls-files` / `git ls-files --others` for file discovery here.
