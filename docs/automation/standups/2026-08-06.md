# Daily Standup — 2026-08-06

- Branch: `feat/premium-ui-overhaul` @ `d7342ecf`
- Upstream: `origin/feat/premium-ui-overhaul` — local is **13 ahead / 0 behind** (was 0/0 at the 08-05 push). The 13-ahead is a full **regime-expectancy research study** committed by the parallel research session on top of the 08-05 standup push (`a721b15c`) and **not yet pushed** — a real push-hygiene item this cycle (resolved in Sync below).
- vs `main`: **33 ahead / 59 behind** (was 19 / 58 on 08-05). Ahead rose 19→33 (+14) = the 13 research commits + the 08-05 standup doc; still exactly one product commit (`b061bef3`, 2026-07-16) — the other 32 are loop docs + the research lane. Behind stepped 58→59 (+1): `main` took one owner merge in 24h.
- `origin/main` movement (last 24h): `81a5f3f3 chore(intel): append 2026-08-05 daily intel ideas`. One low-risk intel-log append; no product release this window (the six-release TC-247/248/249 burst was the 08-05 report).
- Stashes: none. In-progress merge/rebase: none (`.git/MERGE_HEAD` / `rebase-merge` / `rebase-apply` absent). Concurrent-writer check: HEAD re-verified `d7342ecf` immediately before commit. ~60 sibling / `.claude` / `.codex` / `.portfolio-worktrees` worktrees exist on this host (new `tradeclaw-ia-consolidation-20260805` since 08-05) — none is this branch's tree; left untouched.
- Run window: scheduled unattended, 09:30 GMT+8.
- Orchestration note: `ultracode` requested. Per the dynamic-workflow review gate verified 07-23 → 08-05 (holds today), an unattended `ultracode` / Workflow fan-out for a daily standup cannot clear the manual-review gate headless, so the read-only investigation ran inline (reconcile + safety-guard skills + direct git reads) with the single git mutation kept in the main loop. Status+sync is single-context git work — correct when parallel writers share one working tree.

## DONE (last 24h)
- **Regime-expectancy research study landed — 13 commits, pre-registered → committed verdict.** `git log --since="24 hours ago" --no-merges` shows the research session drove a full pre-registered study from hypothesis to result on this branch:
  - Pre-registration + executable plan: `817e66d6` (can any subset of the −0.49R track record make money — trend-aligned split, inversion test, cost-vs-stop-width curve, per-strategy split), `3b830b2f` (6-task TDD plan).
  - Build (TDD): `a7986a2a` dashboard-parity counted filter + R/cost math, `65954589` Wilder ADX + Kaufman efficiency ratio with property tests, `bf391e85` lookahead-safe EMA200/ADX/ER regime classification, `9117a59f` null-detector + strong-but-flat branch coverage, `555ae28e` bucket stats + inversion approximation + cost curve + reconciliation gate + CLI, `83a4f816` reconciliation-gated H1–H4 report with JSON artifact, `98ced17b` pre-registered 7-day bar staleness guard.
  - Result + hardening: `30044fe8` results doc (**H1 regime filter refuted, H2 inversion refuted; verdict: horizon is the only lever, next build = D1 slow-gate**), `a4732df9` reconciliation gate fails closed on non-finite aggregates, `072d8372` empty-stream candle guard + doc accuracy corrections, `d7342ecf` inline `emaSeries` so the branch is self-contained (drops the untracked `slow-gate-assembly` import).
- The study is disciplined-transparency work: pre-registered hypotheses, refuted its own H1/H2, and labels findings as research — no invented production metrics. Ran on 3,157 real trades (per the committed results doc + the [[regime-expectancy-study]] record); 1,995 non-crypto signals remain unclassifiable for lack of a candle feed, stated plainly rather than filled.
- The 08-05 daily sync **pushed cleanly** — `a721b15c` (08-05 standup) is at/below `origin/feat/premium-ui-overhaul`; no stranded standup commit. This 08-06 write makes **ten consecutive clean daily writes** (07-28 → 08-06 + the 07-31 weekly).
- No premium-UI product code advanced on this branch since `b061bef3` (2026-07-16). It remains a status branch the loop is not driving forward; the only forward motion this cycle is the research lane.

## PENDING (not finished)
- **13 unpushed research commits** on `feat/premium-ui-overhaul` (`817e66d6` → `d7342ecf`) — the regime study exists only in the local tree at run start. Pushed this run (see Sync); this is the first cycle the loop resolves a genuine push-hygiene backlog rather than a 0/0 branch.
- Uncommitted drift, owned by parallel sessions — LEFT UNTOUCHED (not this loop's work): **105 tracked files modified/deleted (+7710 / −2691)**, 57 untracked. Vs the 08-05 record (103 tracked +7457/−2685, 57 untracked): tracked **+2 files, +253 insertions / +6 deletions** in 24h; untracked **flat at 57** for a third consecutive cycle. Parallel writer still editing in-place (including further uncommitted edits on `scripts/research/regime-study-assembly.ts` + its test, on top of the now-committed lane). Lanes unchanged:
  - Premium UI overhaul (dominant tracked lane): `apps/web/app/components/`, `apps/web/components/`, `apps/web/components/landing/`, `apps/web/lib/`, locale pages (`ar`/`es`/`ms`/`zh`), `dashboard`, `screener`, `track-record`, `layout.tsx`, `globals.css`.
  - Deploy / self-host cleanup: working-tree deletions `Dockerfile.fly`, `apps/web/app/fly/FlyClient.tsx`, `apps/web/app/replit/ReplitClient.tsx`; modified `fly.toml`, `docker-compose.yml`, `.replit`, `.github/workflows/docker-test.yml`, `.env.example`, `.env.docker.example`, `packages/create-tradeclaw/index.js`, `docs/FLYIO.md` / `docs/REPLIT.md` / `docs/QUICKSTART.md`.
  - WS / price lanes: `apps/web/lib/hooks/use-price-stream.ts`, `use-websocket-prices.ts`, `apps/web/lib/signals-live.ts`, `apps/web/middleware.ts`.
  - AI-improvement / recost lane: `STATE.yaml`, `docs/ai-improvement/*`, `scripts/research/recost-segment.ts` (+ `__tests__/recost-segment-cli.test.ts`), `scripts/init-db.sh`, `scripts/test-docker.sh`.
- Untracked composition unchanged (57 total): `apps/web/lib` localization layer (`product-i18n` + hooks/tests ≈ 26 files), `tmp/imagegen`, `apps/web/public/brand/editorial`, `scripts/research` slow-gate lane (`slow-gate-cli.ts`, `slow-gate-assembly.ts`, `recost-report.ts` + 3 tests), `apps/web/tests/e2e` specs, `apps/ws-server` auth test, `packages/create-tradeclaw/index.test.js`, plan doc `docs/plans/2026-07-18-slow-regime-gate-sandbox.md`, experiment JSON under `docs/research/experiments/`, root `CLAUDE.md` / `.gitattributes`, orphaned standups 07-13/14/15, `tmp` logs.
- Untracked `product-i18n` localization layer — a substantial, test-carrying feature (≈26 files) still entirely uncommitted with no branch home. Re-confirmed present.
- Unpushed-to-`main` feature commit `b061bef3` — still the only non-doc commit in the 33-ahead set. Its premium-UI intent shipped via merged PR #168 and `main` has since moved seven artwork releases (TC-244..249) past it. Redundancy increasingly likely; owner call.
- Orphaned standups 07-13/14/15 on disk, untracked — NOT rescue-committed (verified retired: each already lives on other branches; the double-commit guard holds).
- Committed-standup GAP 07-24 → 07-27 — a CLOSED hole, treated permanent; no backfill (fabricating past-day status would violate the no-invented-data rule).
- Open PRs (**7**, none target this branch → `main`): **NEW `#198 agent/ia-consolidation-20260805 → main` "make the front door match the product"**; plus #184 daily-intel, #174 remotion draft (→ `release/premium-green-assets-20260716`), #170 daily-intel, #160 rescue-stranded-standups, #149 loop standup 07-04, #143 feature-flags ADR.
- Newest plan doc: `docs/plans/2026-07-18-slow-regime-gate-sandbox.md` (still untracked) — the slow-regime-gate sandbox workstream that the now-committed regime study explicitly commissions as its next build (D1 slow-gate).

## NEXT
- **The regime study's own verdict sets the next build:** H1 (regime filter) and H2 (inversion) are refuted; horizon is the only surviving lever. The committed results doc points to the **D1 slow-gate** — advance `docs/plans/2026-07-18-slow-regime-gate-sandbox.md`. Keep it LOCAL research: the EMA200-gate / slow-gate findings stay labelled sandbox, never the production verified record; measured or "pending real data" only.
- Give the 13-commit regime study a committed home toward `main`. It is now pushed to `origin/feat/premium-ui-overhaul` (backed up), but it rides a 59-behind status branch tangled with unrelated UI drift. Clean disposition = a focused research PR (scripts/research + docs/research only), not a rebase of the whole branch. Owner-gated.
- Owner decision (gated), still top: integrate or retire `feat/premium-ui-overhaul`. Now **33-ahead / 59-behind** with one possibly-redundant feature commit (`b061bef3`) + 105-file uncommitted drift; `main` has shipped seven artwork releases past its intent. Confirm `b061bef3` still adds value vs current `main`, else retire rather than rebase a 59-behind status branch.
- Decide the home for the untracked `product-i18n` layer (≈26 files) and the untracked slow-gate research lane. Both are coherent, test-carrying sets stranded with no committed branch — clean independent PRs, highest-value disposition after the UI drift.
- Watch parallel-writer churn: tracked 103→105, untracked flat at 57 for a third cycle, +253 lines/24h (down from +413). Flag if tracked breaks 110 or untracked breaks 70.
- Refresh graphify: `GRAPH_REPORT.md` is the 2026-07-06 extraction — now **31 days stale**. A fresh `--update` stays deferred on scheduled runs (spawns gateguard-blocked LLM subagents, and would ingest other sessions' uncommitted drift); env-var presence probes remain permission-blocked from the loop. Queue for an interactive session.

## graphify highlights (from graphify-out/GRAPH_REPORT.md, 2026-07-06 extraction — 31 days stale)
- Incremental `--update` intentionally skipped on this status run (gateguard-blocked LLM subagents + would ingest other sessions' uncommitted drift; `GEMINI_API_KEY` presence not probeable from the loop's permission layer). Highlights embedded from the last committed extraction (11,497 nodes · 18,049 edges · 845 communities).
- God Nodes (core abstractions to watch for coupling): `readSessionFromRequest()` 114 edges, `execute()` 62, `query()` 60, `getUserById()` 49, `requireCronAuth()` 46, `getTrackedSignals()` 42, `queryOne()` 41, `getTrackedSignalsForRequest()` 34, `getUserTier()` 33, `getOHLCV()` 32. Auth/session and DB access dominate — a change to `readSessionFromRequest()` fans out across 114 edges.
- Surprising connections: `ComparisonChartProps` / `MetricsTableProps` (`apps/web/app/backtest`) both reference `BacktestResult` from `packages/strategies/src/run-backtest.ts` — backtest UI is tightly bound to the strategy runner's result shape. `refreshAtrCalibration()` → `calibrateAtrMultiplier()` couples `apps/web/app/lib/atr-calibration-cache.ts` to `packages/signals/src/atr-calibration.ts`.
- Import cycles (2-file, worth breaking): `packages/agent/src/channels/base.ts` ↔ `webhook.ts` / `discord.ts` / `telegram.ts` (three cycles through the channel base class); `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`.

## Sync result
- Committed this standup on `feat/premium-ui-overhaul` by explicit path (standup doc only; the 105-file drift + 57 untracked stay untouched — not this loop's work).
- **Pushed the branch to `origin/feat/premium-ui-overhaul`** after re-verifying HEAD had not shifted — this backs up the 13-commit regime study (previously local-only) plus this standup. Push to the feature branch's own remote is safe hygiene, not a deploy (Railway ships only from `main`). Remote-tip advance confirmed via `git ls-remote`.
- No commit or merge to `main`; feature→main integration and the regime-study PR disposition remain owner-gated (deliberately not auto-opening a PR from a 59-behind status branch carrying unrelated parallel drift).

## Lessons / escalations (this run)
- LESSON: the loop's push job finally had real work — 13 committed-but-unpushed research commits, not a 0/0 branch. Pushing already-committed work to a feature branch's own remote is unambiguously in the daily "safe sync" scope and de-risks bus-factor loss; it is categorically distinct from the owner-gated feature→main integration. Do it; don't defer branch backup to the owner.
- LESSON: the research session models the brand correctly — pre-registered hypotheses, published its own refutations (H1 + H2 both dead), and labelled sandbox findings as research not production record. This is the transparency posture to protect; any surface that promotes slow-gate/EMA200 results must carry the LOCAL/research label.
- ESCALATION (owner, standing): the 13-commit regime study is a clean, self-contained, test-carrying research lane now stranded on a 59-behind status branch. Best home is a focused `scripts/research` + `docs/research` PR to `main`, independent of the premium-UI drift. Needs an owner call to cut it loose.
- ESCALATION (owner, standing): `feat/premium-ui-overhaul` is very likely redundant — `b061bef3`'s intent shipped via merged PR #168 and `main` moved seven artwork releases (TC-244..249) past it; the branch is 59 behind. Confirm value vs current `main` or retire — do not rebase a 59-behind status branch carrying unrelated drift.
- ESCALATION (owner, standing): uncommitted drift on this branch still has no committed home — 105 tracked / 57 untracked across ≥3 separable, test-carrying feature sets (premium UI, `product-i18n` ≈26 files, slow-gate research) plus deploy/self-host cleanup and artwork assets. Open 14 days; size-stable (untracked flat 57 for 3 cycles) but churning. Needs deliberate per-lane commit-and-PR or a documented discard.
- LESSON: `ultracode` fan-out for a daily standup remains blocked by the dynamic-workflow review gate under unattended permissions (verified 07-23 → 08-05, holds 08-06). Inline read-only investigation with the single mutation in the main loop stays the safe pattern.
- NOTE: the launcher-expected loop STATE file at `C:\Users\User\.claude\loops\state\tradeclaw.STATE.md` holds only the launcher-appended run log (no pending "In progress"/"Escalated" items at run start); edits to that global path are sensitive-path-blocked from the loop, so durable lessons/escalations live here in the committed standup doc.
