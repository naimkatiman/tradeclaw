# Intentionally non-deployable.
#
# The former Fly.io configuration started a web-only image without TradeClaw's
# PostgreSQL migrations and advertised obsolete storage and pricing behavior.
# Fly.io is not an end-to-end verified deployment target for the current app.
# See docs/FLYIO.md and use the supported workflow at /start instead.
