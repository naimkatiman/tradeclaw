# Daily Standup — 2026-08-23

- Branch: `feat/premium-ui-overhaul` @ `accb3ccb`.
- Upstream: `origin/feat/premium-ui-overhaul` — **0 ahead / 0 behind** at run start. Yesterday's push landed; will become 1 ahead once this standup commits, until pushed.
- vs `main`: **50 ahead / 76 behind** (was 48 / 74 at the 08-22 run start). Ahead +2 = the two 08-22 status-doc commits (`abe6a244`, `accb3ccb`). Behind **+2 — `main` moved for the first time in 11 days.**
- `origin/main` movement (since last standup): **two owner merges, both 2026-08-22.** `055107ad` → `e8b68e87` (**PR #216**, `docs(readme): show the live cost-adjusted research finding`, 12:20 +0800) → `5ad4ee3e` (**PR #218**, `feat(ui): add subordinate homepage brand sculpture`, 19:01 +0800). Both landed *after* yesterday's 09:30 run, so the 08-22 standup's "main static 11d / zero merges" was accurate at write time and is now superseded. **The 08-11 → 08-22 merge drought is over.**
- Open PRs: **8** — one new (`#217 chore/readme-refresh`, updated 08-22 03:57Z) plus the seven stale June/July lanes (#198, #184, #174, #170, #160, #149, #143). #217 is the only actionable one; it is **`CONFLICTING` with a failing Unit Tests check** (root cause diagnosed below).
- Stashes: **2 — unchanged** since 08-10 (`tc254-restack-after-tc253-final-main`, `tc253-restack-20260810`). Recovery stashes from the TC-253/TC-254 worktree sessions — do not drop until owner handoff acceptance. In-progress merge/rebase: **none** (`.git/MERGE_HEAD` / `rebase-merge` / `rebase-apply` all absent). Concurrent-writer check: HEAD re-verified `accb3ccb` on `feat/premium-ui-overhaul` immediately before the write and again before commit; 80 worktrees are registered against this repo and a parallel writer demonstrably touched this tree yesterday, so this check is load-bearing.
- Run window: scheduled unattended, 09:30 GMT+8 (2026-08-23). Third consecutive healthy daily run after the 08-14 → 08-19 dark period and the 08-20 triple FAIL(2). Orchestration: `ultracode` requested; holds as before (verified 07-23 → 08-23) — the dynamic-workflow review gate blocks headless Workflow fan-out for a daily standup, and status + safe sync over a tree shared with ~80 worktrees is correctly done inline (`continuous-improvement:reconcile` + `safety-guard`, direct git reads, single doc mutation in the main loop).

## DONE (last 24h)

- **Owner shipped two PRs to `main` — the drought broke.** `#216` (`e8b68e87`, 17 files) rebuilt the repo homepage on live cost-adjusted facts: `README.md` (339 lines rewritten), canonical-English notices on `README.ja/ko/zh`, `.gitattributes`, `.gitignore` (+6), `LANGUAGES.md`, `docs/github-social-preview.md`, six new brand assets, the `2026-08-22-repo-hygiene-readme-visibility.md` plan doc, and `scripts/build-readme-media.ps1`. `#218` (`5ad4ee3e`, 6 files) added the subordinate homepage brand sculpture: `hero-brand-art.tsx` (+30), `globals.css` (+103), `page.tsx`, a 74 KB webp, a Playwright spec, and a `DESIGN.md` line.
- **Diagnosed the whole 8-file drift increase as already-shipped content — not new unhomed cargo.** Tracked drift rose 108 → 116 and untracked 34 → 38 rows overnight. Byte-level verification (below) proves every one of those new rows is the `#216` payload sitting unclean in the primary tree, identical to what `main` now carries. Exactly one file in that lane is genuinely unshipped: `AGENTS.md`.
- **Root-caused PR #217's failing Unit Tests check to a single dropped truth-labeling phrase.** Precise, one-line fix identified (below). This is the only open PR with forward motion and both of its blockers are now named.
- **Corrected a factual error in the loop's own 08-22 standup** about where the LOCAL-vs-production disclosure lives. The correction shrinks weekly proposal 2 further and is recorded as a lesson, not buried.
- **Zero commits on this branch in the strict 24h window** (`git log --since="24 hours ago" --no-merges` = 0). The 08-22 pair landed 09:42 / 09:43 +0800, just outside. All real motion this cycle was owner-side on `main`.
- **TC-254 prospective D1 alpha ledger unchanged — monitoring only.** Gate thresholds re-verified from `origin/main` this run (`apps/web/lib/d1-alpha-protocol.ts:12-14`): `D1_ALPHA_MIN_CALENDAR_DAYS = 365`, `D1_ALPHA_MIN_SNAPSHOTS = 365`, `D1_ALPHA_MIN_CLOSED_TRADES = 12`. No new migration landed (`main` still stops at `054_d1_alpha_ledger.sql`). Progress figures (0/365 days, 1/365 snapshots, 0/12 closed trades) stand on the owner verification record, not a fresh probe — see the verification note.

Verification note: `origin/main` tip, both merge commits and their file stats, the open-PR set, PR #217's check results and mergeability, the blob hashes below, and the migration ledger were all confirmed this run from local git + `gh` after a live `git fetch`. A production HTTP re-probe (`/api/health`, `/track-record/alpha`) was **not** run — headless external `curl` is approval-gated in the unattended allowlist. TC-254 live counts therefore stand on the owner verification record (MemoryCore 2026-08-10 17:42 MPST), not a fresh measurement.

## PENDING (not finished)

- **The +8 tracked / +4 untracked drift is a stale mirror of shipped PR #216 — DO NOT commit it from this tree.** Verified two ways. (a) `git diff origin/main -- README.md README.ja.md README.ko.md README.zh.md .gitignore docs/github-social-preview.md docs/assets/demo.gif` returns **nothing for any of those paths** — the working copies are identical to `main`. (b) The untracked members match `main`'s blobs exactly: `docs/assets/demo.mp4` `832766af`, `social-preview.png` `f90d3545`, `hero-evidence-field.jpg` `1f28ed41`, `.gitattributes` `c57e0d1d`, `docs/plans/2026-08-22-repo-hygiene-readme-visibility.md` `dfbbf8e3`, `scripts/build-readme-media.ps1` `dd9ded60` — each equal to `git ls-tree origin/main` for the same path. They read as "drift" here only because this branch is 76 behind and never received `#216`. Committing them onto `feat/premium-ui-overhaul` would fork merged work, the same trap as the theme churn vs `#214`.
- **`AGENTS.md` (+37 / −9) is the one piece of the 08-22 hygiene lane that did NOT ship — genuinely homeless.** `git diff origin/main HEAD -- AGENTS.md` is **empty**, so this branch's committed `AGENTS.md` is byte-identical to `main`; the uncommitted edit is net-new on top of both. It is plan item 5 ("Align AGENTS.md key decisions with the live product. Do not revive TC-001-008 as current work") from `docs/plans/2026-08-22-repo-hygiene-readme-visibility.md`, which `#216` left out. Its correct home is a one-file branch cut from `origin/main` `5ad4ee3e` — **not** this 76-behind tree, where committing it would strand it further.
- **PR #217 has two named blockers, both fixable.** (1) **Failing Unit Tests** — `apps/web/lib/__tests__/truth-labeling.test.ts:173` asserts `expect(readme).toMatch(/gate-approved entry-like signals/i)`. That test is unchanged on `origin/chore/readme-refresh`, but the PR's rewritten `README.md` no longer contains the phrase (`git grep` on the branch matches the test file only). `main`'s README still carries it at `README.md:212`: *"Telegram, email, and Discord can carry gate-approved entry-like signals plus outcome and risk-exit notices once that gate is ready."* The CI log confirms the exact failure (`Expected pattern: /gate-approved entry-like signals/i`). (2) **`mergeable: CONFLICTING`** — #217 was cut from `055107ad` and pushed 03:57Z, then `#216` merged at 04:20Z rewriting the same 339-line `README.md`. Every other check on #217 passes: Build, Docker Build, Lint & Type Check, Playwright E2E, Strategy Backtests.
- **`main`'s repo homepage now serves a 4.42 MB inline GIF.** `#216` shipped `docs/assets/demo.gif` at 4,417,328 bytes (up from 1,366,200) and `README.md:23` embeds it inline, with the 783 KB mp4 only *linked* at line 25. This matches the recorded finding that the reel has a ~2.5 MB GIF floor (a full-frame 3D camera move defeats GIF inter-frame coding). `#217` is the fix — it swaps the inline GIF for the 159 KB `hero-evidence-field.jpg` — and `#217` is blocked. So the heaviest asset stays live on `main` until #217 lands.
- **Weekly proposal 2 (LOCAL-vs-production provenance) — re-verified, and yesterday's framing was wrong.** The 08-22 standup said the LOCAL-vs-production fact "still lives only in `scripts/compute-strategy-decay.py:46`". It does not. On `origin/main` it appears in **7 files / 10 occurrences**, including a rendered UI surface: `apps/web/app/admin/strategy-library/page.tsx:184` ("Numbers are LOCAL scanner/dev samples, not production-verified.") — but that page calls `requireAdmin()` at line 160, so it is **not public**. Three data files carry it as a machine-readable `provenance` field (`strategy_library.json`, `strategy-decay-metrics.json`, `satellite-allocations.json`), plus `apps/web/lib/strategy-library.ts`. `DataProvenanceBadge` remains mounted only on `/accuracy` surfaces, still absent from track-record. **Corrected scope: this is a publication gap, not a data gap** — the fact is already machine-readable and already rendered, just behind an admin gate.
- **Weekly proposal 1 (decay delivery-gate)** — open, unblocked, largest remaining lane. The `alert-channels.ts` ↔ `email-sender.ts` cycle is type-only one way and a dynamic `await import()` the other (verified 08-22); not a runtime prerequisite. **Weekly proposal 3 (calibration surface) stays retired** — verified 08-22 as fully shipped on `main`.
- **Loop-scheduler incident (08-14 → 08-20) — still undiagnosed.** Per `loops/state/tradeclaw.STATE.md`: a 6-day gap after 08-13, then three consecutive FAIL(2) runs on 08-20 (`claude exit 1`); `docs/automation/standups/2026-08-20.md` is still absent from both tracked and untracked trees (re-checked). Three healthy runs since (08-21, 08-22, 08-23) with zero repo damage. Root cause remains in run logs outside the unattended read scope.
- Untracked: **38 porcelain rows** (was 34). The pre-existing stranded lanes are unchanged: `product-i18n` (lib modules + tests + `product-locale-switcher.tsx` + e2e specs), the slow-gate research helper lane (`slow-gate-cli/assembly`, `recost-report` + tests + the 07-18 plan doc + the `f4.json` artifact), orphaned standups 07-13/14/15 (retired; double-commit guard holds), editorial art, `CLAUDE.md`. The `tmp/` row is gone — `#216`'s `.gitignore` now covers `/tmp/`, `.playwright-mcp/`, `playwright-report/`, and `test-results/`.
- Tracked drift split is **113 modified + 3 deleted = 116** per `git diff --name-status` (authoritative on this autocrlf tree). Deletions unchanged: `Dockerfile.fly`, `apps/web/app/fly/FlyClient.tsx`, `apps/web/app/replit/ReplitClient.tsx`.
- **`feat/premium-ui-overhaul` retire-or-integrate — case re-verified and now stronger.** 76 behind. `git ls-tree` on this branch stops at `apps/web/migrations/053_drop_monetization.sql`; `origin/main` carries `054_d1_alpha_ledger.sql`. The branch also now lacks both 08-22 merges. Unique unshipped cargo = `b061bef3` (one UI commit, near-certainly superseded by TC-244…TC-254, `#214` and `#218`) + the loop-doc ledger. The 116-file drift is **no longer** an argument for keeping the branch — the bulk of the new drift is provably already on `main`.
- graphify extraction is **48 days stale** (2026-07-06); `--update` skipped this run (see highlights).

## NEXT

- **Owner: unblock PR #217 with a two-step fix.** (1) Restore the pinned truth-labeling sentence into the rewritten README — copy `main`'s `README.md:212` line verbatim, or change `truth-labeling.test.ts:173` to a pattern matching equivalent honest wording. **Do not delete or weaken the assertion**: that test is the guard keeping the broadcast-gate claim honest on the repo homepage, and editing it to make CI green would be a transparency regression. (2) Rebase `chore/readme-refresh` onto `origin/main` `5ad4ee3e` to clear the `CONFLICTING` state — the conflict is `README.md` against `#216`, which rewrote the same file 20 minutes after #217 was pushed. Everything else on #217 is already green.
- **Owner: rescue `AGENTS.md` (+37 / −9) to its own branch off `origin/main` — it is the only unshipped work in the 08-22 hygiene lane.** Cut from `5ad4ee3e`, apply that one file, PR it. Doing this from `feat/premium-ui-overhaul` would bury it behind 76 commits of drift.
- **Owner: clean the primary tree's `#216` mirror, or accept it as permanent noise.** Fourteen paths in this checkout are byte-identical to `main` and will keep inflating every drift measurement until either this branch takes `main` or the working copies are reset. This is now the single largest source of false-positive drift signal in the daily loop.
- **Owner: re-scope weekly proposal 2 to a publication change, not a data change.** The LOCAL-vs-production fact is already a machine-readable `provenance` field on three data files and already rendered at `admin/strategy-library/page.tsx:184` behind `requireAdmin()`. Smallest honest version: read the existing `provenance` string and render it on `/track-record` so the public surface states which record it is showing. No new data plumbing and no new axis on `DataProvenanceBadge` — just publish what is already there. This remains the highest value-per-effort proposal.
- **Owner: weekly proposal 1 (decay delivery-gate) is the largest remaining lane and is unblocked.** Hook `alert-channels.ts:206` (`case 'email':`) without a prior cycle-break. It must compute on production Postgres, not the LOCAL sample, and must keep demoted strategies **visible-as-demoted** — auto-demote is a transparency feature, not a hiding mechanism. Branch from `origin/main` `5ad4ee3e`.
- **Owner: the 4.42 MB inline GIF on `main`'s README is live now.** If #217 will not land soon, the one-line interim fix is to point `README.md:23` at `docs/assets/hero-evidence-field.jpg` (159 KB, already on `main`) and keep the mp4 as the reel link. Do not re-attempt GIF compression — the ~2.5 MB floor on this reel is a settled measurement.
- **Owner: investigate the 08-14 → 08-20 scheduler gap + triple FAIL(2).** Three healthy runs since; the harness recovered on its own, which makes diagnosis time-sensitive because run logs may rotate. The repo is clean throughout — a scheduler/harness matter, not a code fix.
- **Owner / monitor: TC-254 is the live prospective candidate — monitoring only.** Verify a daily-cadence row lands for each closed D1 bar (epoch fixed at the 2026-08-09 bar). Investigate a missing row or integrity error **without rewriting history**. Do NOT tune from prospective observations, backfill/rewrite the ledger, auto-promote the candidate, delete the losing observed archive, or infer broker execution.
- **Owner: the `product-i18n` localization lane** (20+ untracked files with tests) is still the largest genuinely stranded feature — needs a branch-or-discard call.
- Parallel-writer watch: tracked **116** (up 8, fully attributed to `#216`), untracked **38 rows** (up 4, same cause). New flag thresholds: tracked **125** or untracked **45 rows** — raised from 110/40 because the `#216` mirror permanently shifted the baseline.
- Refresh graphify in an interactive session with `GEMINI_API_KEY` set — the extraction is 48 days stale and predates TC-24x, the D1 ledger, `#214`, `#216`, and `#218`.

## graphify highlights (from graphify-out/GRAPH_REPORT.md, 2026-07-06 extraction — 48 days stale)

- `--update` **skipped this run.** `GEMINI_API_KEY` presence remains unverifiable: a fourth distinct probe form was refused this run (`Test-Path Env:GEMINI_API_KEY` → rejected as a non-filesystem provider path; `$env:GEMINI_API_KEY` inside an `if/else` → rejected as unvalidatable control flow), on top of the three forms refused on 08-22. An unkeyed `--update` spawns heavy LLM subagents that gateguard blocks headless, so the skip is the safe branch either way. Per the loop contract: logged, continued, run not failed over graphify. The figures below were read directly from the report file this run, but the underlying extraction predates every 2026-08 change.
- **God Nodes (core abstractions), unchanged:** `readSessionFromRequest()` 114 edges, `execute()` 62, `query()` 60, `getUserById()` 49, `requireCronAuth()` 46, `getTrackedSignals()` 42, `queryOne()` 41, `getTrackedSignalsForRequest()` 34, `getUserTier()` 33, `getOHLCV()` 32. Auth/session + DB-query + signal-tracking remain the coupling hubs. `requireCronAuth()` at 46 stays relevant to the authenticated D1 ledger cron writer — a change there fans out widely.
- **Surprising connections:** no new ones (file unchanged since 07-06). Standing set: backtest `comparison-chart.tsx` and `metrics-table.tsx` → `packages/strategies/src/run-backtest.ts` (both via `BacktestResult`, EXTRACTED); `atr-calibration-cache.ts` → `packages/signals/src/atr-calibration.ts` (INFERRED); two INFERRED `pad()` edges into `packages/agent/src/cli/cli.ts` that read as extractor name-collision noise.
- **Import cycles: four, unchanged** — three in `packages/agent/src/channels/base.ts` ↔ `webhook` / `discord` / `telegram`, plus `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`. The product-side cycle is a **static artifact, not a runtime one** (verified 08-22: type-only one way, dynamic `await import()` the other), so it is not a prerequisite for the decay-gate lane. The three `channels/base.ts` cycles have still not been re-verified and may or may not be real.

## Sync result

- **Standup doc committed to `feat/premium-ui-overhaul` by explicit path** (`docs/automation/standups/2026-08-23.md`, single file). Nothing else staged — in particular the 116-file drift is left untouched, which this run proves is the correct call for 14 of those paths (identical to `main`) and the safe call for `AGENTS.md` (it needs a `main`-based branch, not this one).
- No `feature → main` PR opened: consistent with 30+ prior daily runs. `feature → main` is owner-gated, and a PR of 50 mostly-duplicate commits from a 76-behind branch would be noise. Recorded as owner-gated, not silently skipped. No open PR targets this branch head (checked via `gh pr list`).
- Protected `main` untouched. Commit SHA + push confirmation in the run trailer below.

## Lessons / Escalations (state file is machine-record-only; lessons recorded here per the sensitive-path pattern)

- **State-file note:** `loops/state/tradeclaw.STATE.md` is readable and holds only the launcher's machine run-records (no lessons/escalations headings). It confirms 08-21 and 08-22 daily = PASS and the 08-20 triple FAIL(2). STATE.md **writes** remain permission-denied (sensitive path), so lessons live in this committed doc. Continuity also holds via MemoryCore + the auto-memory index.
- **CORRECTION to the 08-22 standup (this loop's own error):** it claimed the LOCAL-vs-production fact "still lives only in `scripts/compute-strategy-decay.py:46`". Wrong — on `origin/main` it appears in 7 files / 10 occurrences, including a rendered (admin-gated) UI surface and three machine-readable `provenance` fields. The 08-22 claim came from re-reading the weekly proposal's own framing instead of grepping the tree. **Grep the tree, not the proposal, even when the proposal was written by this same loop.**
- **LESSON (new, measurement):** a drift *increase* is not automatically new unhomed work. Overnight growth of 108 → 116 tracked and 34 → 38 untracked looked like a parallel writer creating cargo; blob-hash comparison against `origin/main` proved 14 of those paths are byte-identical to freshly-merged `#216`. On a branch this far behind, "dirty" and "unshipped" are different questions. Cost: six `git hash-object` / `ls-tree` pairs plus one scoped `git diff origin/main -- <paths>`. Carry this check forward whenever drift moves.
- **LESSON (new, CI):** when a docs-only PR fails Unit Tests, suspect a copy-guard assertion before suspecting flake. `#217` fails solely because `truth-labeling.test.ts:173` pins a README phrase the rewrite dropped. The correct fix restores the *claim*, not the *test* — these guards exist so a README rewrite cannot quietly drop a transparency statement, and editing the assertion to match a weaker README would defeat their purpose.
- **LESSON (re-confirmed, methodological):** verifying carried-forward weekly proposals against live code paid out for the second consecutive run. 08-22 retired proposal 3 as already shipped; 08-23 corrected proposal 2's scope from "add an axis and plumb the data" to "publish a field that already exists behind an admin gate."
- **ESCALATION (operational, open):** daily loop dark 08-14 → 08-19 and all three 08-20 runs FAILED (gate FAIL(2), `claude exit 1`); `2026-08-20.md` never written. Three healthy runs since with no repo damage. Root cause undiagnosed and evidence may rotate — owner should pull the run logs.
- **ESCALATION (new, actionable):** PR #217 is `CONFLICTING` with a failing Unit Tests check. Both causes are diagnosed to the line in this doc. It is the only open PR with forward motion and it carries the fix for the 4.42 MB inline GIF now live on `main`.
- **ESCALATION (new):** `AGENTS.md` (+37 / −9) is unshipped work sitting in a shared dirty checkout with no branch. It is one file and it will be lost or mis-committed if it stays there.
- **ESCALATION (decision-ready, standing):** retire-or-integrate `feat/premium-ui-overhaul`. 50 ahead / 76 behind; provably lacks `054_d1_alpha_ledger.sql` and both 08-22 merges. Unique product cargo = `b061bef3` only, very likely fully superseded. The drift-based argument for keeping the branch is now weaker, not stronger.
- **ESCALATION (open):** Pro-funnel instrumentation seed (weekly 08-07 P4) — measure whether the checkout-resume fix recovers conversions — still open.
- **ESCALATION (standing):** the `product-i18n` localization lane (20+ untracked files with tests) needs an owner branch-or-discard call.
- **MONITOR (live boundary):** TC-254 prospective D1 ledger is `collecting-evidence` / `not-promoted`. Strict no-tune / no-backfill / no-promote / no-archive-deletion boundary. A missing daily row is a monitoring event, not a repair opportunity.
- **LESSON (standing, re-confirmed):** headless production re-probe is approval-gated in the unattended allowlist. Do not claim fresh live probes in a scheduled run; cite the owner verification record and independently confirm what git + `gh` can prove (main tip, merge stats, PR checks and mergeability, blob hashes, tree contents).
- **OBSERVATION (this run):** the quiet-cycle pattern broke — `main` moved twice and a parallel writer changed the tree. The value came from attributing every delta to a shipped commit rather than logging the counts, which turned an apparent 8-file drift regression into a clean "already on `main`" verdict plus one genuinely homeless file. Scope still held at status + a single doc commit.

---
Run trailer:
- Commit: _(filled after commit)_
- Push: _(filled after push)_
- feature → main: no PR opened (owner-gated; branch cargo redundant/stale). Protected `main` untouched at `5ad4ee3e`.
