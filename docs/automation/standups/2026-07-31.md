# Daily Standup — 2026-07-31

- Branch: `feat/premium-ui-overhaul` @ `b7908b23`
- Upstream: `origin/feat/premium-ui-overhaul` — local is **1 ahead / 0 behind** BEFORE this run. Remote tip re-verified via `git ls-remote` = `9266e0fe…` (the 07-30 standup). The 07-31 weekly commit `b7908b23` was committed by the weekly loop but **never pushed** — it is the one unpushed commit, carried by this run's sync.
- vs `main`: **14 ahead / 41 behind**. The 14-ahead set is 13 standup/research docs + one feature commit (`b061bef3 feat(ui): unify premium assets with emerald brand`, 2026-07-16). Re-verified: `b061bef3` is still the ONLY non-doc/non-research commit ahead of `main`.
- Stashes: none. In-progress merge/rebase: none (`.git/MERGE_HEAD` / `rebase-merge` / `rebase-apply` absent). Concurrent-writer check: HEAD unchanged across the run (re-verified immediately before commit). ~40 sibling/`.claude` worktrees exist on this host — none is this branch's tree; left untouched.
- `origin/main` tip static at `d87796cb` (2026-07-29 12:07 `chore(intel)`) — did not advance in the last 24h, so the 41-behind count held from 07-30.
- Run window: scheduled unattended, 09:30 GMT+8.
- Orchestration note: `ultracode` requested. Per the gate verified on 07-23/07-29/07-30, an unattended `ultracode`/Workflow fan-out for a daily standup cannot clear the dynamic-workflow manual-review gate headless, so I did the read-only investigation inline (reconcile skill + direct git reads) and kept the single git mutation in the main loop. A status+sync standup is single-context git work — the correct model when parallel writers share this one working tree.

## DONE (last 24h)
- Two doc commits landed in the window (`git log --since="24 hours ago" --no-merges`): `9266e0fe docs(standup): daily standup 2026-07-30` and `b7908b23 docs(research): weekly propose-only loop 2026-07-31`.
- The 07-31 **weekly propose-only loop ran** and committed `b7908b23`: 4 on-brand proposals superseding the 07-17 P1–P4 set — edge-decay suppression ledger, outcome-source integrity audit, per-strategy/band expectancy, published loss post-mortems — each verified against current code and cited, with 2 verify-before-propose kills. It followed the weekly contract (propose only, no product-code edits). It did NOT push; the commit sat local-only until this run.
- Write-path recovery holds: 07-28, 07-29, 07-30 standups and the 07-31 weekly all wrote + committed successfully. The double `FAIL(2)` on 07-27 stays confirmed intermittent, not a standing break.
- No product code advanced on `feat/premium-ui-overhaul` since `b061bef3` (2026-07-16). This remains a status branch the loop is not driving forward.
- Nothing merged to `main` in the last 24h attributable here. Newest owner merges on record remain PRs #177–#181 (2026-07-19).

## PENDING (not finished)
- **Unpushed weekly commit `b7908b23`** — the 07-31 weekly loop committed but its push did not land (remote tip was still `9266e0fe` at run start). It is a `docs(research)` commit on a feature branch, so this daily sync carries it forward along with the standup (feature-branch push, never `main`). Watch the weekly loop's push step for recurrence.
- Committed-standup GAP 07-24 → 07-27 — four missing days, a CLOSED hole (not reopened by the 07-28→07-31 successes). No scheduled runs wrote files for 07-24/07-25/07-26; 07-27 fired twice, both exited 1 / gate `FAIL(2)`, neither wrote `2026-07-27.md` (absent on disk and in history). Same class as the 07-19/07-20 permanent hole. Treated as permanent; no backfill (fabricating past-day status would violate the no-invented-data rule).
- Uncommitted drift, owned by parallel sessions — LEFT UNTOUCHED this run (not this loop's work): 99 tracked files modified/deleted (**+6095 / −2561**), 46 untracked. Vs the 07-30 record (+5892 / −2564, 46 untracked): **+203 net insertions, 3 fewer deletions**, untracked count identical at 46, tracked file count identical at 99. Drift stays frozen at the file level — 99 tracked / 46 untracked unchanged for 8 days, only line-level churn. Lanes (unchanged):
  - Premium UI overhaul (dominant tracked lane): `apps/web/app/components/`, `apps/web/components/`, `apps/web/components/landing/`, `apps/web/lib/`, locale pages (`ar`/`es`/`ms`/`zh`), `dashboard`, `screener`, `track-record`, `layout.tsx`.
  - Deploy / self-host cleanup: working-tree deletions `Dockerfile.fly`, `apps/web/app/fly/FlyClient.tsx`, `apps/web/app/replit/ReplitClient.tsx`; modified `fly.toml`, `docker-compose.yml`, `.replit`, `.github/workflows/docker-test.yml`, `.env.example`, `.env.docker.example`, `packages/create-tradeclaw/index.js`, `docs/FLYIO.md` / `docs/REPLIT.md` / `docs/QUICKSTART.md`.
  - WS / price lanes: `apps/web/lib/hooks/use-price-stream.ts`, `use-websocket-prices.ts`, `apps/web/lib/signals-live.ts`, `apps/web/middleware.ts`.
  - AI-improvement / recost lane: `STATE.yaml`, `docs/ai-improvement/*`.
- Untracked `product-i18n` localization layer — 21 files under `apps/web/lib/product-i18n/` (`app-shell`, `dashboard`, `dashboard-live`, `dashboard-evidence`, `screener`, `track-record`, `track-record-widgets`, `theme-state`, `marketing-nav`, `format`, `server-locale` + matching `__tests__` including `encoding`), plus `apps/web/app/components/product-locale-switcher.tsx` and `apps/web/tests/e2e/features/product-localization.spec.ts`. A substantial, test-carrying feature sitting entirely uncommitted with no branch home.
- Untracked slow-gate research lane: `scripts/research/slow-gate-{assembly,cli}.ts` + `recost-report.ts` and their `__tests__`, `docs/research/experiments/slow-gate-BTCUSD_ETHUSD-D1-2017-09-01-2026-07-16-f4.json`, and the plan doc `docs/plans/2026-07-18-slow-regime-gate-sandbox.md` (still untracked).
- Other untracked support files in the 46: root `.gitattributes` and root `CLAUDE.md`, `apps/web/app/__tests__/deployment-surface-integrity.test.ts`, `apps/web/app/api/health/route.test.ts`, `apps/web/lib/github-star-count.ts` (+ test), `apps/web/lib/hooks/websocket-config.ts` (+ test), `apps/web/lib/__tests__/translations.test.ts`, `apps/ws-server/src/__tests__/auth.test.ts`, `packages/create-tradeclaw/index.test.js`, plus the three orphaned standups below and `tmp/imagegen/tradeclaw-market-sculpture-source.png`.
- Unpushed-to-`main` feature commit: `b061bef3 feat(ui): unify premium assets with emerald brand` — still the only non-doc commit in the 14-ahead set.
- Orphaned standups on disk, untracked here — NOT rescue-committed (verified permanently retired): `2026-07-13.md`, `2026-07-14.md`, `2026-07-15.md` each already live on other branches. Committing them here would create divergent duplicates; the rescue double-commit guard holds.
- Open PRs for this branch: none (`gh pr list --head feat/premium-ui-overhaul --state open` empty).
- Newest plan doc: `docs/plans/2026-07-18-slow-regime-gate-sandbox.md` (still untracked) — the active slow-regime-gate sandbox workstream.

## NEXT
- Owner decision (gated): integrate `feat/premium-ui-overhaul` → `main`. Branch is 14-ahead / 41-behind with one unmerged feature commit (`b061bef3`) plus the 99-file uncommitted drift. A clean integration cherry-picks or rebases `b061bef3` onto current `main`, NOT a PR of the whole 41-behind status branch (stale-base-clobber risk), and does not fold unrelated parallel drift into a standup-branch PR. The rebase surface held at 41 behind this run but grows whenever `main` advances.
- Decide the home for the untracked `product-i18n` layer and slow-gate research lane. Both are coherent, test-carrying feature sets stranded in the working tree with no committed branch — they would make clean, independent PRs on their own and are the highest-value disposition targets after the UI drift.
- Advance the slow-regime-gate sandbox per `docs/plans/2026-07-18-slow-regime-gate-sandbox.md`. Research-surface + `currentVol` production wire-up already merged on `main`; keep observing the live vol scaler without retuning until a predeclared production sample is sufficient. No invented metrics — measured or "pending real data" only. LOCAL sandbox findings (EMA200 gate beats buy-and-hold on Calmar after costs in 3/4 folds; HMM sizing hurts) stay labelled research, never presented as the production verified record.
- Weekly loop push hygiene: the 07-31 weekly commit did not push itself. If the weekly loop keeps committing-without-pushing, its proposals stay stranded until a daily run carries them. De-prioritized (this daily sync closes the gap), but flag if it recurs.
- The 07-27 double-`FAIL(2)` root-cause stays DE-PRIORITIZED (not dropped): the write-path has been green four straight days. Re-open only if a fresh gap appears; do not schedule per-day catch-up re-runs (they would fabricate retroactive status).
- Refresh graphify: `GRAPH_REPORT.md` is the 2026-07-06 extraction — now 25 days stale. A fresh `--update` stays deferred on scheduled runs because it spawns LLM subagents whose writes gateguard blocks on this host (and the secret-env presence check is itself blocked by the shell guard, so headless auto-detection is not possible). Queue it for an interactive session with `GEMINI_API_KEY` set.

## graphify highlights (from graphify-out/GRAPH_REPORT.md, 2026-07-06 extraction — 25 days stale)
- Incremental `--update` intentionally skipped on this status run (spawns gateguard-blocked LLM subagents, and the drift it would ingest is other sessions' uncommitted work). Highlights embedded from the last committed extraction. Corpus at extraction: 11,497 nodes · 18,049 edges · 845 communities · 98% EXTRACTED.
- God Nodes (core abstractions to watch for coupling): `readSessionFromRequest()` 114 edges, `execute()` 62, `query()` 60, `getUserById()` 49, `requireCronAuth()` 46, `getTrackedSignals()` 42, `queryOne()` 41, `getTrackedSignalsForRequest()` 34, `getUserTier()` 33, `getOHLCV()` 32. Auth/session and DB access dominate the graph — a change to `readSessionFromRequest()` fans out across 114 edges.
- Surprising connections: `ComparisonChartProps` and `MetricsTableProps` (`apps/web/app/backtest`) both reference `BacktestResult` from `packages/strategies/src/run-backtest.ts` — backtest UI is tightly bound to the strategy runner's result shape, so a result-shape change breaks the UI. `refreshAtrCalibration()` → `calibrateAtrMultiplier()` couples `apps/web/app/lib/atr-calibration-cache.ts` to `packages/signals/src/atr-calibration.ts`.
- Import cycles (2-file, worth breaking): `packages/agent/src/channels/base.ts` ↔ `webhook.ts` / `discord.ts` / `telegram.ts` (three cycles through the channel base class); `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`.

## Sync result
- Committed this standup on `feat/premium-ui-overhaul` by explicit path (standup doc only; the 99-file drift + 46 untracked stay untouched — not this loop's work).
- Pushed to `origin/feat/premium-ui-overhaul` after re-verifying HEAD had not shifted. The push carries two commits the remote lacked: `b7908b23` (the stranded 07-31 weekly propose-only commit) and this standup. Confirmed the remote tip advanced via `git ls-remote`.
- No commit or merge to `main`; feature→main integration remains owner-gated (deliberately not auto-opening a PR from a 41-behind status branch carrying unrelated parallel drift).

## Lessons / escalations (this run)
- LESSON: the 07-31 weekly loop committed `b7908b23` but did not push it — the propose-only doc sat local-only until this daily run carried it. Sibling loops on the same branch can commit-without-pushing; the daily sync is the backstop that lands them. If the weekly loop's push step keeps failing, its proposals go unpublished for a day at a time — watch for a pattern before treating it as a real break.
- LESSON: the harness write-path is now green four consecutive days (07-28 → 07-31, counting the 07-31 weekly commit). The failure that produced the 07-24→07-27 hole was intermittent, not a standing break — fix priority stays "watch for recurrence", not "root-cause now".
- ESCALATION (owner): the uncommitted drift on `feat/premium-ui-overhaul` is frozen (99 tracked / 46 untracked, unchanged file counts for 8 days) but still has no committed home. It contains at least three separable, test-carrying feature sets — premium UI overhaul, the `product-i18n` localization layer (21 files), and the slow-gate research lane — plus deploy/self-host cleanup. Leaving them as ambient working-tree state against a 41-behind base risks a stale-base clobber. Needs a deliberate per-lane commit-and-PR or a documented discard.
- ESCALATION (owner): feature→`main` integration is still gated; the base held at 41 behind this run only because `origin/main` did not advance. The 14-ahead set carries exactly one product commit (`b061bef3`); the rest are loop docs. A clean integration cherry-picks or rebases `b061bef3` onto current `main`, not a PR of the whole status branch.
- LESSON: `ultracode` fan-out for a daily standup is blocked by the dynamic-workflow review gate under unattended permissions (verified 07-23/07-29/07-30, holds today). A status+sync standup is single-context git work; the safe pattern is inline read-only investigation with the one mutation kept in the main loop. Do not spend the turn re-authoring a workflow that cannot clear the gate headless.
- NOTE: loop STATE file (`C:\Users\User\.claude\loops\state\tradeclaw.STATE.md`) is outside the loop's allowed working dirs (`C:\Ai\tradeclaw`, `C:\Users\User\.claude\loops\skills\project-loops`) — reads/edits are blocked, so durable lessons/escalations live here in the committed standup doc (the launcher still appends its machine run-record automatically).
