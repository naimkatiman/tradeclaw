# TradeClaw Daily Standup — 2026-07-15

- Date: 2026-07-15 09:30 GMT+8 (scheduled unattended run)
- Primary tree: branch `feat/premium-ui-overhaul` @ `8512cf48` (== `origin/main`; a concurrent session checked this feature branch out in the primary tree since 07-14's detached `b6dd0ff4`)
- Standup lane: `loop/standup-2026-07-15` (dedicated lane off `origin/main` `8512cf48`, committed via a separate worktree so the primary tree's HEAD is never hijacked from the live premium-UI session)
- Ground truth: 0 ahead / 0 behind `origin/main`; no in-progress merge/rebase; 1 stash; ~19 worktrees

## DONE (last 24h)

Commits reachable from HEAD in the last 24h (grouped, outcome-first):

- **#167 (`8512cf48`) fix(test): stop Jest indexing `.codex` worktree copies; ignore local agent artifacts.** Removes the worktree-copy noise that was polluting the test index and the loop's gate signal.
- **(`e8b3016f`) chore(state): record first-layer production release.**
- **#166 (`b4b04388`) feat(web): first-layer simplicity — one-screen homepage, complexity moves to layer 2.** Layer 1 = one idea / one action / zero interruptions; MarketingChromeGate + Navbar minimal variant enforce it.
- **#165 (`71a8daaf`) fix(web): close motion polish follow-up gaps.**
- **#164 (`18531992`) chore(state): record motion polish release.**
- **#163 (`614ac819`) feat(web): ship audited motion polish.**

`origin/main` advanced `b6dd0ff4` (07-14) → `8512cf48`. The 07-14 gate FAIL(1) — `gate.ps1` crashing on a detached HEAD under PS 5.1 — was revalidated PASS at 07-14 23:13 after the detached-HEAD fix. No merges in the last 24h window.

## PENDING

- **Unpushed commits on the primary branch: 0** — `feat/premium-ui-overhaul` HEAD equals `origin/main`. All drift below is uncommitted working-tree state owned by concurrent sessions; this loop left the tree untouched.
- **Premium-UI overhaul (uncommitted, ~1,000 LOC, no committed plan doc):** `globals.css` +205, `proof-hero.tsx` (+345/−...), `navbar.tsx` +124, `PageNavBar.tsx` +123, `mobile-nav.tsx` +104, `explore-strip.tsx` +98, `site-footer.tsx` +70, `tradeclaw-logo.tsx`, `dashboard/layout.tsx`, plus one-line touches to 10 `page.tsx`/`*Client.tsx` files. No `docs/plans/*premium-ui-overhaul*` exists → work is at loss risk until committed behind a plan doc.
- **ai-improvement loop (uncommitted):** `implementation-log.md` +445, `README.md`, `source-review-metrics.md`, `verification-command-matrix.md`, `uncommitted-source-verification-handoff.md`, and `STATE.yaml` +86. `STATE.yaml` was deliberately NOT touched (known stale-base clobber risk — a stale-HEAD write can delete newer origin/main release records).
- **Research recost lane (uncommitted):** `scripts/research/recost-segment.ts` modified; new `scripts/research/recost-report.ts` + `__tests__/recost-report.test.ts` (untracked). Has a test — commit via PR after the test runs green.
- **New evidence guardrail (untracked):** `docs/audits/2026-07-15-linkedin-claim-verification.md`. Frozen prod snapshot 2026-07-15 01:26:40 UTC (cost-field / equity-summary / history responses SHA-256 fingerprinted) supports ONLY the narrow claim: *counted position-sized signals had negative average expectancy after the published fee+slippage model; this engine did not demonstrate a net edge under those assumptions.* Explicitly bars overclaims ("users lost money", "real broker execution results", "every trade unprofitable"). Consistent with the brand's modeled-vs-real distinction.
- **Standup residue (expected):** `standups/2026-07-13.md` and `standups/2026-07-14.md` are untracked in the primary tree — dual-write residue; the real commits live on lane `loop/standup-2026-07-13`. NOT re-committed here.
- **Open PRs (all stale, none merged in 24h):** #160 rescue of 5 stranded standups 07-05..07-11 · #149 standup 07-04 + stranded weekly 07-03 · #143 draft ADR (signal-worker extraction + unwired feature-flag scaffold). Still NO PR for `loop/standup-2026-07-13` (covers 07-13 + 07-14) — owner action outstanding.
- **Stash:** `stash@{0}: codex-preserve-overlap-before-premium-ui`.
- **Housekeeping debt grew:** ~19 worktrees (was 11 flagged 07-14), incl. stale `deploy-main`, `deploy-153`, `pr135-merge`, `pr144-fix` (detached) and retired standup branches.

## graphify highlights

Graph dated **2026-07-06 (~9 days stale)**; `--update` skipped this run (one line per contract): 20+ changed source files plus many changed docs would spawn LLM subagents whose writes gateguard blocks on this host, and `GEMINI_API_KEY` presence can't be verified without risking echoing the value. Highlights read from the seeded `graphify-out/GRAPH_REPORT.md`:

- **God Nodes (blast radius):** `readSessionFromRequest()` 114 · `execute()` 62 · `query()` 60 · `getUserById()` 49 · `requireCronAuth()` 46 · `getTrackedSignals()` 42 · `queryOne()` 41 · `getTrackedSignalsForRequest()` 34 · `getUserTier()` 33 · `getOHLCV()` 32. Auth/session + DB accessors dominate — treat any signals/track-record/auth change as high-blast-radius.
- **Surprising Connections:** `backtest/comparison-chart.tsx` and `backtest/metrics-table.tsx` both reference `packages/strategies` `BacktestResult` (UI directly coupled to the backtest-engine type); `app/lib/atr-calibration-cache.ts` → `packages/signals/src/atr-calibration.ts`.
- **Import Cycles:** `packages/agent/src/channels/base.ts` ↔ `webhook.ts` / `discord.ts` / `telegram.ts` (3 cycles through the channel base class); `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`. Both are in agent-channel + alert wiring, not the UI surface under active premium-UI edit — safe for today's work, worth untangling before extending channels.

## NEXT

- **This run:** commit 07-15 standup on lane `loop/standup-2026-07-15`, push, open PR to `main`.
- **Premium-UI session:** write a plan doc (mandatory for >150 LOC / >3 files), then commit the overhaul behind it and PR. ~1,000 LOC currently uncommitted with no plan → highest loss risk in the tree.
- **Standup backlog:** open the missing `loop/standup-2026-07-13` PR (rescues 07-13 + 07-14); land or close stale #160 / #149 / #143.
- **Research:** run `recost-report.test.ts` green, then PR `recost-report.ts` + `recost-segment.ts`.
- **Claims discipline:** keep any public LinkedIn / WEEX post inside the narrow supported claim from the 07-15 audit — do not upgrade "modeled fee+slippage" to "real broker execution."
- **Housekeeping:** prune stale worktrees (`deploy-main`, `deploy-153`, `pr135-merge`, `pr144-fix`) and retired standup branches once their PRs resolve.

## Sync result

- Standup committed on lane `loop/standup-2026-07-15` (base `origin/main` `8512cf48`), created via a separate worktree — the live `feat/premium-ui-overhaul` premium-UI session's working tree and HEAD were left untouched.
- Pushed to `origin`; PR opened to `main`. Commit SHA and PR URL reported in the run output.
- No product code, migrations, deploys, or `STATE.yaml`/Stripe/Postgres writes. No `git add -A`; staged by explicit path.
