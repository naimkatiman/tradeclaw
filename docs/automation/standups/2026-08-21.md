# Daily Standup — 2026-08-21

- Branch: `feat/premium-ui-overhaul` @ `c40f7184`.
- Upstream: `origin/feat/premium-ui-overhaul` — local is **1 ahead / 0 behind** at run start. Remote tip verified `e040cd9b3d67f4f2190af26df7eed7bc1c5ab247` (`git ls-remote`); the 1 unpushed commit is today's weekly-research doc `c40f7184` (the 08-21 weekly loop committed onto this branch — see DONE). Will become 2 ahead once this standup commits, until pushed.
- vs `main`: **46 ahead / 74 behind** (was 43 / 74 at the 08-13 run start). Ahead +3 = the three status-doc commits landed since 08-13 (`84a69178` 08-13 standup, `e040cd9b` 08-13 sync trailer, `c40f7184` 08-21 weekly research). Behind **unchanged at 74** — `main` did not move across the full 08-13 → 08-21 window.
- `origin/main` movement (since last standup): **none.** Tip held at `055107ad` (2026-08-11T04:20:04Z, PR #214 dark/light theme lane) for the entire 8-day window — now **10 days static**. Zero owner merges. Open PRs (`gh pr list`) are all stale July/early-Aug lanes (#198, #184, #174, #170, #160, #149, #143); none target this branch head, none are new.
- Stashes: **2 — unchanged** since 08-10: `stash@{0}` `tc254-restack-after-tc253-final-main`, `stash@{1}` `tc253-restack-20260810`. Documented recovery stashes from the TC-253/TC-254 worktree sessions — do not drop until owner handoff acceptance. In-progress merge/rebase: **none** (`.git/MERGE_HEAD` / `rebase-merge` / `rebase-apply` absent). Concurrent-writer check: HEAD re-verified `c40f7184` immediately before commit.
- Run window: scheduled unattended, 09:30 GMT+8 (2026-08-21). Orchestration: `ultracode` requested; holds as before (verified 07-23 → 08-21) — the dynamic-workflow review gate blocks headless Workflow fan-out for a daily standup, and status + safe sync over a tree shared with ~60 parallel worktrees is correctly done inline: `continuous-improvement:reconcile` + `safety-guard` skills, direct git reads, single doc mutation in the main loop. Fan-out adds no coverage here.

## DONE (last 24h)
- **Weekly research 2026-08-21 shipped to this branch** (`c40f7184`, `docs/research/weekly-research-2026-08-21.md`). Three grill-SURVIVED, propose-only proposals, each brand-anchored and file-cited: (1) **make "auto-demote" real** — promote the offline decay report (`compute-strategy-decay.py`) into a live delivery gate that suppresses/demotes a strategy's user-visible signals when `DECAYED` at n≥20, computed on production Postgres (not the LOCAL sample), keeping demoted strategies visible-as-demoted; (2) **LOCAL-vs-production provenance badge** on the track-record UI — a machine-set `production-verified` vs `local-sample` label, since today that distinction lives only in a script comment (`compute-strategy-decay.py:46`) while the UI shows observed-vs-unverified only; (3) **publish the existing confidence-calibration report** (`confidence-calibration.ts` → `/api/calibration`) as a public reliability view (band vs realized win-rate + Brier/ECE), model unchanged, with an honest "pending real data" state below the 20-sample floor. This is the only commit in the strict 24h window.
- **Quiet cycle on product surfaces.** `origin/main` held at `055107ad` (PR #214) for the full window; no PRs merged, opened, or closed against `main`. No product code advanced on this branch since `b061bef3` (2026-07-16) — the healthy pattern holds: real forward motion happens on `main` in isolated worktrees, never in this shared premium-UI checkout.
- **TC-254 prospective D1 alpha ledger remains live and collecting** (shipped 08-10). No new publication event — monitoring-only. Gate progress unchanged from the owner verification record: `collecting-evidence` / `not-promoted`, 0/365 calendar days, 1/365 snapshots, 0/12 prospectively closed sleeve trades, 0 unresolved gaps.

Verification note: `origin/main` tip `055107ad` (PR #214) and the empty merge window were confirmed from local git + `gh pr list` this run. A live production re-probe (`/api/health`, `/track-record/alpha`) was **not** run — headless external `curl` is approval-gated in the unattended allowlist (same class as the known Cloudflare headless block). TC-254 live facts stand on the owner-authorized verification record (MemoryCore 2026-08-10 17:42 MPST), not a fresh probe.

## PENDING (not finished)
- **Loop-scheduler incident (08-14 → 08-20) — needs owner attention.** Per `loops/state/tradeclaw.STATE.md`: **no daily standup ran 08-14 → 08-19** (a 6-day gap — last successful daily was 08-13), then **08-20 produced three consecutive FAIL(2) runs** (daily 06:06, weekly 06:12, daily 09:30; all `claude exit 1`). The `2026-08-20.md` standup file was never written (absent from both tracked and untracked trees), consistent with a gate FAIL-on-missing-output. The 08-21 weekly (`0838`) then PASSED. **No repo mutation resulted** from the failed runs — tracked drift is byte-identical to 08-13 (see below), so nothing was half-committed. Root cause of the gap + triple failure is not diagnosable from state records alone (run logs not in the unattended read scope); flagged as an operational escalation, not a code defect.
- **`c40f7184` (08-21 weekly research) is unpushed** — 1 commit ahead of `origin/feat/premium-ui-overhaul`. Will be pushed with today's standup (see Sync).
- **Uncommitted drift, owned by parallel sessions — LEFT UNTOUCHED: 108 tracked files (+7974 / −2726) — byte-identical to the last successful measurement (08-13).** No successful standup measured 08-14 → 08-20 (gap + failures), so this is the first re-confirmation since 08-13; still Δ0. Under the 110-file flag threshold. Porcelain split: **106 modified + 3 deleted** (`Dockerfile.fly`, `apps/web/app/fly/FlyClient.tsx`, `apps/web/app/replit/ReplitClient.tsx`) = the 108 total. File-level attribution is unrecoverable (standups record counts, not inventories).
- **Primary-tree theme churn overlaps SHIPPED code.** The in-place churn in the primary checkout (`theme-toggle.tsx`, `globals.css`, `layout.tsx`, navbar/mobile-nav) predates and is very likely redundant with PR #214, which merged that lane's intent to `main` on 08-11. **Do not commit it from the primary tree** — it would duplicate or fork merged #214 work. Reconcile against `origin/main` (which includes #214) before treating any of it as unhomed cargo.
- Untracked: **34 porcelain rows (93 files) — flat** vs 08-13. Every stranded lane re-confirmed: `product-i18n` (10 lib modules + 10 test files + `product-locale-switcher.tsx` + e2e specs — a large uncommitted localization feature), the slow-gate research helper lane (`slow-gate-cli/assembly`, `recost-report` + tests + the 07-18 plan doc + `f4.json` artifact), the two regime-study screenshots, orphaned standups 07-13/14/15 (retired; double-commit guard holds), editorial art assets (3 `editorial/*.webp` + `editorial-hero-art.tsx`), plus untracked `CLAUDE.md` / `.gitattributes` and a `tmp/` dir.
- **`feat/premium-ui-overhaul` retire-or-integrate — decision-ready, sharpened this cycle.** The 08-21 weekly research independently reconfirmed the hard fact: this branch is 74 behind and **does not contain the shipped TC-254 D1 ledger** — `apps/web/migrations/054_d1_alpha_ledger.sql` and `apps/web/app/track-record/alpha/` are absent here; migrations stop at `053`. They live on `main` via PR #211 (`25c30665`). Unique unshipped product cargo on this branch = `b061bef3` only (one UI commit, near-certainly superseded by TC-244…TC-254 + #214) + the loop-doc ledger + the 108-file unhomed drift. Do not rebase this branch onto a 74-ahead `main`.
- **2 recovery stashes retained** (tc254, tc253 restack) — do not drop until owner handoff acceptance.
- graphify extraction is **~46 days stale** (Jul 6); `--update` skipped this run (see highlights).

## NEXT
- **Owner: investigate the 08-14 → 08-20 loop-scheduler gap + 08-20 triple FAIL(2).** Determine why the daily loop did not fire for six days and why all three 08-20 attempts errored (`claude exit 1`) before writing output. This is the single new operational signal this cycle; the repo is clean (byte-flat drift proves no partial writes), so it is a scheduler/harness matter, not a code fix.
- **Owner / monitor: TC-254 is the live prospective candidate — monitoring only.** Verify a daily-cadence row lands for each closed D1 bar (epoch fixed at the 2026-08-09 bar). Investigate any missing row or integrity error **without rewriting history**. Do NOT tune from prospective observations, backfill/rewrite the ledger, auto-promote the candidate, delete the losing observed archive, or infer broker execution/performance. Gate progress: 0/365 days, 1/365 snapshots, 0/12 closed trades.
- **Owner: three propose-only weekly proposals are decision-ready (from `c40f7184`).** All three (decay delivery-gate, LOCAL-vs-production provenance badge, public calibration surface) reuse infrastructure already in the repo and convert stated brand claims into enforced/visible behavior. Any that touch track-record **must be branched from and reconciled against `origin/main`** (which holds migration 054 + `track-record/alpha`), never this 74-behind tree.
- **Owner: retire-or-integrate `feat/premium-ui-overhaul` — case unchanged, sharper each cycle.** 74 behind; research + theme cargo shipped to `main` (squash-duplicated here); only unique unshipped value = `b061bef3` (very likely superseded) + loop-doc ledger + 108-file drift needing per-lane homes. Either extract anything `b061bef3` holds that isn't already merged, or retire the branch.
- **Owner: the `product-i18n` localization lane is the largest stranded untracked feature** (20+ files with tests) — decide whether to branch + PR it or discard, so it stops riding as unhomed drift.
- Parallel-writer watch: tracked **108** (byte-flat vs 08-13), untracked **34 rows** (flat). Flag if tracked breaks **110** or untracked breaks **40 rows**.
- Refresh graphify in an interactive session with `GEMINI_API_KEY` set — the extraction is now ~46 days stale.

## graphify highlights (from graphify-out/GRAPH_REPORT.md, 2026-07-06 extraction — ~46 days stale)
- `--update` **skipped this run: `GEMINI_API_KEY` is not set** (standing config), so an incremental re-extract would spawn heavy LLM subagents that gateguard blocks headless. Per the loop contract, logged and continued — never fail the run over graphify. Treat the numbers below as indicative, not current; code has shifted (TC-24x, the D1 ledger, and #214's theme structure) since the extraction.
- **God Nodes (core abstractions):** `readSessionFromRequest()` 114 edges, `execute()` 62, `query()` 60, `getUserById()` 49, `requireCronAuth()` 46, `getTrackedSignals()` 42, `queryOne()` 41, `getTrackedSignalsForRequest()` 34, `getUserTier()` 33, `getOHLCV()` 32. Auth/session + DB-query + signal-tracking remain the coupling hubs; `requireCronAuth()` this high aligns with the authenticated D1 ledger cron writer — a change there fans out widely.
- **Surprising connections:** backtest `comparison-chart.tsx` / `metrics-table.tsx` → `packages/strategies/src/run-backtest.ts` (`BacktestResult` coupling); `atr-calibration-cache.ts` → `packages/signals/src/atr-calibration.ts`.
- **Import cycles:** three in `packages/agent/src/channels/base.ts` ↔ `webhook` / `discord` / `telegram`, plus one product-side cycle `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`. The alert-channels↔email-sender cycle is the one worth breaking on the product path — and it is exactly the delivery path Proposal 1 (decay gate) would hook into, so break the cycle before wiring a suppressor there.

## Sync result
- **Standup doc committed to `feat/premium-ui-overhaul` by explicit path.** Also pushed the pending `c40f7184` (08-21 weekly research) in the same push. No `feature → main` PR opened: consistent with 30+ prior daily runs — `feature → main` is owner-gated, the branch's unique cargo is redundant/stale (`b061bef3` + loop docs), and a PR of 46 mostly-duplicate commits would be noise, not signal. Recorded as owner-gated, not silently skipped. No open PR targets this branch head (checked this run via `gh pr list`).
- Commit SHA + push confirmation recorded in the run trailer below.

## Lessons / Escalations (state file is machine-record-only; lessons recorded here per the sensitive-path pattern)
- **State-file note:** `loops/state/tradeclaw.STATE.md` holds only the launcher's machine run-records (no lessons/escalations headings), and loop STATE.md edits are permission-denied here (sensitive path), so lessons are captured in this committed doc, matching established behavior. Continuity also held via MemoryCore + the auto-memory index.
- **ESCALATION (new, operational):** the daily loop was **dark 08-14 → 08-19 and FAILED all three 08-20 runs** (gate FAIL(2), `claude exit 1`). No repo damage (drift byte-flat), but six missed days + a triple failure is a scheduler/harness reliability issue the owner should diagnose. The `2026-08-20.md` output was never written.
- **ESCALATION (decision-ready, standing, sharpened):** retire-or-integrate `feat/premium-ui-overhaul`. 46 ahead / 74 behind; the branch provably lacks the shipped TC-254 ledger (migrations stop at 053; 054 + `track-record/alpha` are on `main` via #211). Unique product cargo = `b061bef3` only, very likely fully superseded.
- **ESCALATION (open):** Pro-funnel instrumentation seed (weekly 08-07 P4) — measure whether the checkout-resume fix recovers conversions — still open.
- **ESCALATION (standing):** the `product-i18n` localization lane (20+ untracked files with tests) is the single largest stranded feature in the dirty checkout — needs an owner branch-or-discard call.
- **MONITOR (live boundary):** TC-254 prospective D1 ledger is `collecting-evidence` in production. Strict no-tune / no-backfill / no-promote / no-archive-deletion boundary. Any missing daily row is a monitoring event, not a repair opportunity.
- **LESSON:** headless production re-probe is approval-gated (external `curl` not in the unattended allowlist) — same class as the Cloudflare headless block. Do not claim fresh live probes in a scheduled run; cite the owner verification record and independently confirm what git + `gh` can prove (main tip, open/merged PRs).
- **OBSERVATION (this run):** the branch state is otherwise identical to 08-13 (main static, drift byte-flat, untracked flat) — the only real delta is the 08-21 weekly proposals landing and the surfaced 08-14→08-20 scheduler incident. Status + a single doc commit is the correct scope; nothing more.

---
Run trailer:
- Commit: `7e4fa991` on `feat/premium-ui-overhaul` (1 file, +60). Parent `c40f7184`.
- Push: `e040cd9b..7e4fa991` → `origin/feat/premium-ui-overhaul` (landed the previously-unpushed `c40f7184` weekly-research doc in the same push); remote tip verified `7e4fa991db410c9f286e50525c62773a846649fa` == local HEAD (`git ls-remote`), upstream now 0/0.
- feature → main: no PR opened (owner-gated; branch cargo redundant/stale). Protected `main` untouched (static at `055107ad`).
