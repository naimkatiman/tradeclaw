# Daily Standup — 2026-07-28

- Branch: `feat/premium-ui-overhaul` @ `4ca71153`
- Upstream: `origin/feat/premium-ui-overhaul` — in sync (0 ahead / 0 behind local at read time; re-verified against remote tip via `git ls-remote` immediately before the commit below).
- vs `main`: 10 ahead / 39 behind. The 10-ahead set is 9 standup/research docs + one feature commit (`b061bef3 feat(ui): unify premium assets with emerald brand`, 2026-07-16).
- Stashes: none. In-progress merge/rebase: none (`.git/MERGE_HEAD` / `rebase-*` absent). Concurrent-writer check: HEAD unchanged across the run (re-verified immediately before commit).
- Run window: scheduled unattended, 09:30 GMT+8.
- Branch tip has NOT advanced since the 07-23 standup (`4ca71153`). The vs-`main` counts moved 9→10 ahead / 38→39 behind with a fixed HEAD, so the shift is entirely `origin/main` advancing plus a merge-base move — not new work on this branch.
- Orchestration note: `ultracode` requested. Per the established gate on this loop, an unattended `ultracode`/Workflow fan-out for a daily standup cannot clear the dynamic-workflow manual-review gate headless, so I did the read-only investigation inline and kept the single git mutation in the main loop. No coverage lost: ground truth was established twice (reconcile skill + direct git), and single-context git is the correct model when parallel writers share this one working tree.

## DONE (last 24h)
- Zero commits in the last 24h (`git log --since="24 hours ago" --no-merges` empty). The branch has been static at `4ca71153` for 5 days (last commit 2026-07-23).
- No product code advanced on `feat/premium-ui-overhaul` since `b061bef3` (2026-07-16). This remains a status branch the loop is not driving forward.
- Nothing merged to `main` in the last 24h attributable here; the newest owner merges are still PRs #177–#181 (2026-07-19). `origin/main` did advance by one commit since 07-23 (behind 38→39) — an owner/parallel-lane change, not this loop's.

## PENDING (not finished)
- Committed-standup GAP 07-24 → 07-27 — four missing days on record. No scheduled runs fired 07-24/07-25/07-26; 07-27 fired twice (`06:15` and `09:30`), both exited 1 / gate `FAIL(2)`, and neither wrote a `2026-07-27.md` file (none exists on disk or in history). This is a launcher/write-path failure, matching the 07-19/07-20 permanent-hole pattern — the standup content is not the blocker, the run harness is exiting before the write+commit. Treated as a permanent hole; no backfill (fabricating past-day status would violate the no-invented-data rule).
- Uncommitted drift, owned by parallel sessions — LEFT UNTOUCHED this run (not this loop's work): 99 tracked files modified/deleted (+5562 / −2564), 46 untracked. Vs the 07-23 record (+5312 / −2566, 46 untracked): +250 net insertions, 2 fewer deletions, untracked count identical at 46. Drift is effectively frozen — file counts unchanged for 5 days, only line-level churn. Lanes:
  - Premium UI overhaul (dominant tracked lane): `apps/web/app/components/`, `apps/web/components/`, `apps/web/components/landing/`, `apps/web/lib/`, locale pages (`ar`/`es`/`ms`/`zh`), `dashboard`, `screener`, `track-record`, `layout.tsx`.
  - Deploy / self-host cleanup: working-tree deletions `Dockerfile.fly`, `apps/web/app/fly/FlyClient.tsx`, `apps/web/app/replit/ReplitClient.tsx`; modified `fly.toml`, `docker-compose.yml`, `.replit`, `.github/workflows/docker-test.yml`, `.env.example`, `.env.docker.example`, `scripts/init-db.sh`, `scripts/test-docker.sh`, `packages/create-tradeclaw/index.js`, `docs/FLYIO.md` / `docs/REPLIT.md` / `docs/QUICKSTART.md`.
  - WS / price lanes: `apps/web/lib/hooks/use-price-stream.ts`, `use-websocket-prices.ts`, `apps/web/lib/signals-live.ts`, `apps/web/middleware.ts`.
  - AI-improvement / recost lane: `STATE.yaml` (+862), `docs/ai-improvement/implementation-log.md` (+1633), `docs/ai-improvement/*`, `scripts/research/recost-segment.ts` + its `__tests__/recost-segment-cli.test.ts`.
- Untracked `product-i18n` localization layer — ~24 files under `apps/web/lib/product-i18n/` (`app-shell`, `dashboard`, `dashboard-live`, `dashboard-evidence`, `screener`, `track-record`, `track-record-widgets`, `theme-state`, `marketing-nav`, `format`, `server-locale` + matching `__tests__`), plus `apps/web/app/components/product-locale-switcher.tsx` and `apps/web/tests/e2e/features/product-localization.spec.ts`. A substantial, test-carrying feature sitting entirely uncommitted with no branch home.
- Untracked slow-gate research lane: `scripts/research/slow-gate-{assembly,cli}.ts` + `recost-report.ts` and their `__tests__`, `docs/research/experiments/slow-gate-BTCUSD_ETHUSD-D1-2017-09-01-2026-07-16-f4.json`, and the plan doc `docs/plans/2026-07-18-slow-regime-gate-sandbox.md` (still untracked).
- Untracked infra/test additions: `apps/web/app/api/health/route.test.ts`, `apps/web/app/__tests__/deployment-surface-integrity.test.ts`, `apps/web/lib/hooks/websocket-config.ts` (+test), `apps/web/lib/github-star-count.ts` (+test), `apps/ws-server/src/__tests__/auth.test.ts`, `packages/create-tradeclaw/index.test.js`, plus untracked `.gitattributes`, `CLAUDE.md`, and `tmp/imagegen/*.png`.
- Unpushed-to-`main` feature commit: `b061bef3 feat(ui): unify premium assets with emerald brand` — still the only non-doc commit in the 10-ahead set.
- Orphaned standups on disk, untracked here — NOT rescue-committed (verified permanently retired): `2026-07-13.md`, `2026-07-14.md`, `2026-07-15.md` each already live on other branches. Committing them here would create divergent duplicates; the rescue double-commit guard holds.
- Open PRs for this branch: none (`gh pr list --head feat/premium-ui-overhaul --state open` empty).
- Newest plan doc: `docs/plans/2026-07-18-slow-regime-gate-sandbox.md` (still untracked) — the active slow-regime-gate sandbox workstream.

## NEXT
- ROOT-CAUSE the 07-27 double-`FAIL(2)`. Two runs the same day exited 1 without writing their standup file — the harness is failing before the write+commit step, not the content generation. This is the highest-value item: left unaddressed it produces exactly the 07-19/20 and 07-24→27 holes. Investigate the launcher write-path / gate, not a per-day re-run.
- Owner decision (gated): integrate `feat/premium-ui-overhaul` → `main`. Branch is 10-ahead / 39-behind with one unmerged feature commit (`b061bef3`) plus the 99-file uncommitted drift. Needs a rebase-or-supersede call AND a disposition for the drift before any PR. Do not auto-open a `main` PR against a 39-behind base (stale-base-clobber risk), and do not fold unrelated parallel drift into a standup-branch PR.
- Decide the home for the untracked `product-i18n` layer and slow-gate research lane. Both are coherent, test-carrying feature sets stranded in the working tree with no committed branch — they would make clean, independent PRs on their own and are the highest-value disposition targets after the UI drift.
- Advance the slow-regime-gate sandbox per `docs/plans/2026-07-18-slow-regime-gate-sandbox.md`. Research-surface + `currentVol` production wire-up already merged on `main`; keep observing the live vol scaler without retuning until a predeclared production sample is sufficient. No invented metrics — measured or "pending real data" only.
- Refresh graphify: `GRAPH_REPORT.md` is the 2026-07-06 extraction — 22 days stale. A fresh `--update` stays deferred on scheduled runs because it spawns LLM subagents whose writes gateguard blocks on this host; queue it for an interactive session.

## graphify highlights (from graphify-out/GRAPH_REPORT.md, 2026-07-06 extraction)
- Incremental `--update` intentionally skipped on this status run (spawns gateguard-blocked LLM subagents, and the drift it would ingest is other sessions' uncommitted work). Highlights embedded from the last committed extraction. Corpus at extraction: 11,497 nodes · 18,049 edges · 845 communities · 98% EXTRACTED.
- God Nodes (core abstractions to watch for coupling): `readSessionFromRequest()` 114 edges, `execute()` 62, `query()` 60, `getUserById()` 49, `requireCronAuth()` 46, `getTrackedSignals()` 42, `queryOne()` 41, `getTrackedSignalsForRequest()` 34, `getUserTier()` 33, `getOHLCV()` 32. Auth/session and DB access dominate the graph — a change to `readSessionFromRequest()` fans out across 114 edges.
- Surprising connections: `ComparisonChartProps` and `MetricsTableProps` (`apps/web/app/backtest`) both reference `BacktestResult` from `packages/strategies/src/run-backtest.ts` — backtest UI is tightly bound to the strategy runner's result shape, so a result-shape change breaks the UI. `refreshAtrCalibration()` → `calibrateAtrMultiplier()` couples `apps/web/app/lib/atr-calibration-cache.ts` to `packages/signals/src/atr-calibration.ts`.
- Import cycles (2-file, worth breaking): `packages/agent/src/channels/base.ts` ↔ `webhook.ts` / `discord.ts` / `telegram.ts` (three cycles through the channel base class); `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`.

## Sync result
- Committed this standup on `feat/premium-ui-overhaul` by explicit path (standup doc only; the 99-file drift + 46 untracked stay untouched — not this loop's work).
- Pushed to `origin/feat/premium-ui-overhaul` after re-verifying HEAD had not shifted, then confirmed the remote tip advanced to the new commit via `git ls-remote`.
- No commit or merge to `main`; feature→main integration remains owner-gated (deliberately not auto-opening a PR from a 39-behind status branch carrying unrelated parallel drift).

## Lessons / escalations (this run)
- ESCALATION (owner): the committed-standup gap 07-24 → 07-27 is a recurring harness failure, not a content failure. 07-27 ran twice and both runs exited 1 (`FAIL2`) before writing their file — same class as the 07-19/07-20 permanent hole. The standup body generates fine (this run proves it); the launcher write/commit or gate path is the failing surface. Fix the harness or accept the holes as permanent; do not schedule per-day catch-up re-runs (they would fabricate retroactive status).
- ESCALATION (owner): the uncommitted drift on `feat/premium-ui-overhaul` is frozen (99 tracked / 46 untracked, unchanged file counts for 5 days) but still has no committed home. It contains at least three separable, test-carrying feature sets — premium UI overhaul, the `product-i18n` localization layer, and the slow-gate research lane — plus deploy/self-host cleanup. Leaving them as ambient working-tree state against a 39-behind base risks a stale-base clobber. Needs a deliberate per-lane commit-and-PR or a documented discard.
- ESCALATION (owner): feature→`main` integration is still gated. The 10-ahead set carries exactly one product commit (`b061bef3`); the rest are loop docs. A clean integration cherry-picks or rebases `b061bef3` onto current `main`, not a PR of the whole status branch.
- LESSON: `ultracode` fan-out for a daily standup is blocked by the dynamic-workflow review gate under unattended permissions. A status+sync standup is single-context git work; the safe pattern is inline read-only investigation (git reads + seeded graphify report) with the one mutation kept in the main loop. Do not spend the turn re-authoring a workflow that cannot clear the gate headless.
- LESSON: shell guard on this host blocks `env`/`printenv` on a secret name, unquoted `@{u}`, `$VAR` expansion, and compound `&&` chains; `Glob` also times out on this repo (many `.claude/worktrees/*` + sibling `tradeclaw-*` worktrees). Use `git diff --name-only` / `git ls-files --others`, single-command git reads, and explicit refs.
