# Daily Standup — 2026-08-26

- **Branch:** `feat/premium-ui-overhaul`
- **HEAD:** `30d286fe`
- **Upstream:** `origin/feat/premium-ui-overhaul` — 0 ahead / 0 behind (in sync)
- **vs `origin/main` (`6342eb8c`):** 53 ahead / 77 behind
- **Working tree:** 116 tracked paths drifted (`+8193 / −2911`, 113 modified + 3 deleted), 60 untracked rows
- **Stashes:** 2 (`tc254-restack-after-tc253-final-main`, `tc253-restack-20260810`) — retained
- **In-progress git ops:** none (no `MERGE_HEAD`, no rebase state)
- **Protected-branch check:** HEAD is not `main`/`master`/`release/*` → SYNC allowed

## DONE (last 24h)

- **Nothing shipped anywhere.** `git log --since="24 hours ago" --all` returns zero commits across every ref. `origin/main` is unchanged at `6342eb8c` (2026-08-23 16:33 +0800 — **3 days static**). The three-merge burst of 08-22/08-23 has stopped; no owner activity since.
- **This loop did not run on 2026-08-25.** `docs/automation/standups/2026-08-25.md` does not exist and `loops/state/tradeclaw.STATE.md` has no `2026-08-25` record — its last entry is `Run 2026-08-24_0930 (daily) — exit 0, PASS`. Second scheduler gap in 12 days (the first was 08-14 → 08-19).
- **The 08-24 run also terminated early — after its commit, before its own trailer.** Commit `30d286fe` landed 2026-08-24 09:39:41 +0800 and is confirmed on the remote (`origin/feat/premium-ui-overhaul` = `30d286fe`), but `docs/automation/standups/2026-08-24.md` ends at a bare `---` with an empty trailer. On 08-22 and 08-23 the same run filled its trailer 46–60 seconds after the doc commit (`abe6a244` → `accb3ccb`, `6beab0b6` → `98ec2947`). The 08-24 gate still recorded PASS, so **the gate does not observe trailer completion** — the run was cut short and nothing caught it. *(Deliberately not backfilling that trailer here: it would mix two days into one commit. The push it was meant to record is verified above.)*
- **Only true DONE item: the 08-24 standup itself** — one docs-only commit, pushed, remote-tip-verified. That is the entire 48-hour output of this repository.

## PENDING (not finished)

- **`Docker Publish` is still RED on `main` — day 3, no re-run attempted.** Run `32628564441` on `6342eb8c` remains the newest Docker Publish run in the log. No retry, no fix, no follow-up run. Job/step pinned this run:
  - Job **`Build & Push :latest`**, step 6 **`Build & push`** — started `08:33:27Z`, failed `08:40:08Z` (6m41s). Steps 1–5 (checkout, `setup-qemu-action@v3`, `setup-buildx-action@v3`, `login-action@v3`) all **green**. The failure is inside the buildx multi-platform build itself, not in setup.
  - Workflow config re-verified by blob (`6f20f8cf`): `docker-publish.yml:34` `runs-on: ubuntu-latest`, `:39` `docker/setup-qemu-action@v3`, `:54` `platforms: linux/amd64,linux/arm64`.
  - Consequence, stated precisely: **the job that failed is the one that pushes `:latest`**. `tradeclaw/tradeclaw:latest` on Docker Hub is therefore still the `5ad4ee3e` build (run `32569177227`, success, 2026-08-22T11:01:37Z) — **4 days old and one commit behind `main`**. That one commit is docs-only, so the image *content* is materially current; the *pipeline* is not.
  - Honest limit: **one failure, one attempt.** Flake-vs-deterministic is undetermined because nobody re-ran it.
- **CORRECTION — the multi-arch claim is public, but not where the 08-24 standup said it was.** That run escalated "the self-hosting docs advertise multi-arch images." **Wrong.** `git grep -i arm64` across all of `origin/main` matches **no documentation file at all** (only the workflow, `STATE.yaml`, two lockfiles, `scripts/dockerhub-publish.sh`, and two app files). The real claim is stronger and sits on a **rendered, sitemap-published product page**:

  | surface | line | rendered claim |
  |---|---|---|
  | `apps/web/app/hub/page.tsx` | 6 | page `description` metadata — "Supports linux/amd64 and linux/arm64 (Apple Silicon)." |
  | `apps/web/app/hub/HubClient.tsx` | 106 | spec row — `Platforms: linux/amd64, linux/arm64` |
  | `apps/web/app/hub/HubClient.tsx` | 145–147 | three badges — `linux/amd64`, `linux/arm64`, **`Apple Silicon ✓`** |

  `/hub` is in the sitemap at `apps/web/app/sitemap.ts:71` (priority 0.7), so this is indexed public copy carrying a checkmark. `scripts/dockerhub-publish.sh:24` also defaults `PLATFORMS="linux/amd64,linux/arm64"`. **This upgrades the escalation:** dropping arm64 would not be a docs edit, it would falsify a rendered product claim on a live page.
- **CORRECTION — one of the six "homeless" untracked files is a superseded draft, not unshipped work.** The 08-24 run flagged `docs/plans/2026-08-10-dark-light-theme-craft.md` (102 lines) as existing "in no ref at all" and asked for a branch-or-discard call. Path-existence said homeless; **content says shipped**:

  | | local untracked | `origin/main` |
  |---|---|---|
  | path | `docs/plans/2026-08-10-dark-light-theme-craft.md` | `docs/plans/2026-08-11-dark-light-theme-craft.md` |
  | lines | 102 | 97 |
  | blob | `38a25e88` | `01b658fe` |
  | title | `# Dark + light theme craft pass` | `# Dark + light theme craft pass` |
  | scope line | "marketing layer-1 (homepage) via the shared token layer" | "the shared token layer … (all routes inherit)" |

  Main's copy arrived in `055107ad` (**PR #214**, 08-11). The local file is the pre-merge draft under its drafting date, narrower in scope. Action is **discard-local**, not branch-it.
- **Untracked drift re-classified from scratch (60 rows) — 51 / 3 / 5 / 1.** Every row resolved against `git ls-tree -r origin/main` this run, not carried forward:
  - **51 rows exist on `origin/main`** — all `product-i18n` (21), the three editorial `.webp`, both e2e specs, all six `docs/assets` files, `.gitattributes`, `scripts/build-readme-media.ps1`, the `slow-gate` research trio (`slow-gate-cli.ts`, `slow-gate-assembly.ts`, `__tests__/slow-gate-assembly.test.ts`), the `slow-gate-…f4.json` experiment, and both `docs/plans` entries for 07-18 / 08-22. They read as untracked only because this branch is 77 behind.
  - **3 rows are standups already committed on loop branches** (07-13, 07-14, 07-15 → `8dadd644`, `8ec65a38`, `25420b5e`). Absent from `main`. Double-commit guard applies.
  - **5 rows are genuinely unshipped:** `scripts/research/recost-report.ts` (219) + `__tests__/recost-report.test.ts` (310) + `__tests__/regime-expectancy-study-cli.test.ts` (163) + `__tests__/slow-gate-cli.test.ts` (747) = **1,439 lines with tests, in no ref anywhere**, plus `CLAUDE.md` — which is **7 lines** (a MemoryCore import stub), not substantive work.
  - **1 row is the superseded theme-plan draft** above.
- **`STATE.yaml` clobber unchanged — still a live data-loss hazard.** Re-measured this run, identical to the 08-24 reading:

  | source | lines | `TC-254` occurrences |
  |---|---|---|
  | working tree (uncommitted) | 5843 | **0** |
  | branch `HEAD` | 3606 | **0** |
  | `origin/main` | 4601 | 3 |

  Working tree vs `origin/main` = `+2017 / −775`. Committing it from this 77-behind base still erases **TC-250 … TC-254**. Third consecutive day flagged, zero movement.
- **README/media lane is still a revert of #219.** Re-verified by blob, unchanged:

  | path | worktree blob | `origin/main` blob | direction |
  |---|---|---|---|
  | `README.md` | `2a13048f` | `b91e6256` | `+8 / −14` — worktree is **older** |
  | `docs/assets/demo.gif` | `584529f4` (4,417,328 B) | **absent** — deleted by #219 | committing resurrects the 4.42 MB GIF |
  | `docs/assets/social-preview.png` | `f90d3545` | `5a7a8c38` | worktree is the heavier pre-#219 file |
  | `scripts/build-readme-media.ps1` | `dd9ded60` | `35bc1801` | worktree is **46 lines shorter** than main |
- **`AGENTS.md` (+37 / −9) — homeless for a fourth day.** Re-verified: worktree vs `origin/main` = `+37 / −9`; branch `HEAD` vs `origin/main` = **empty diff**. Net-new edit, no branch, sitting in a checkout shared by concurrent writers.
- **Open PRs: 7, all stale, zero movement.** Newest `#198` (08-05) is now **21 days** untouched; oldest `#143` (06-28). Three `CONFLICTING` (`#198`, `#184`, `#170`), two drafts (`#174`, `#143`). `#174` targets `release/premium-green-assets-20260716`, not `main`. No PR targets this branch head.

## NEXT

- **Owner: re-run `32628564441` before doing anything else.** It is free, it is the only red check on the default branch, and it collapses the whole question. Green → emulation flake, fix is a retry policy. Red → the runner image or QEMU moved, and the decision becomes real. Three days of not-knowing has cost more than the re-run would have.
- **Owner: if arm64 must be dropped, `/hub` changes in the same PR.** Not the docs — there are none. `apps/web/app/hub/HubClient.tsx:106`, `:145–147` (including the `Apple Silicon ✓` badge) and `page.tsx:6`'s description all assert arm64 support on an indexed page. Shipping an amd64-only image while `/hub` renders a checkmark is an unmeasured public claim, which is exactly what this project's positioning forbids. Alternative that keeps the claim true: build the arm64 leg on a native arm runner instead of QEMU.
- **Owner: `STATE.yaml` — decide, or delete.** Third day as a named-casualty hazard (TC-250…TC-254). Two honest options: re-anchor the edit onto `6342eb8c` keeping only genuinely new records, or `git checkout -- STATE.yaml`. Leaving it is the only option that can still lose data.
- **Owner: `AGENTS.md` is still the single cleanest merge in the repo.** 46 changed lines, one file, branch from `6342eb8c`, PR it. Fourth day of identical advice.
- **Owner: branch-or-discard the 1,439-line research lane** (`recost-report.ts` + three CLI/assembly test files). Unchanged from 08-24 and now the *only* genuinely stranded work of size — the theme-plan draft dropped out of that set today. It has tests and exists in no ref.
- **Owner: reset the README/media lane and drop the theme-plan draft.** `git checkout -- README.md docs/assets/demo.gif`, then delete the two stale untracked media copies and `docs/plans/2026-08-10-dark-light-theme-craft.md`. All five are older-than-`main` or superseded; the operation is loss-free and removes the sharpest foot-gun in this tree.
- **Owner: weekly proposal 2, re-scoped to publication** — carried, still the best value-per-effort. The LOCAL-vs-production fact already exists as a machine-readable `provenance` field on three data files and already renders at `admin/strategy-library/page.tsx:184` behind `requireAdmin()`. Smallest honest version: surface that existing string on `/track-record`. No new plumbing.
- **Owner: weekly proposal 1 (decay delivery-gate) remains the largest unblocked lane.** Hook `alert-channels.ts:206` (`case 'email':`). Must compute on production Postgres, not the LOCAL sample, and must keep demoted strategies **visible-as-demoted** — auto-demote is a transparency feature, not a hiding mechanism. Branch from `6342eb8c`.
- **Owner / monitor: TC-254 stays `collecting-evidence` / `not-promoted`.** Thresholds re-verified on `main` this run — `d1-alpha-protocol.ts:12-14`: `MIN_CALENDAR_DAYS = 365`, `MIN_SNAPSHOTS = 365`, `MIN_CLOSED_TRADES = 12`. No tuning from prospective observations, no backfill, no ledger rewrite, no auto-promotion, no deletion of the losing observed archive, no inferred broker execution. A missing daily row is a monitoring event, not a repair opportunity. This candidate exists **only on `main`** and is invisible from this branch.
- **Owner: the scheduler gap is now a pattern, not an incident.** 08-14 → 08-19 dark, all three 08-20 runs FAIL(2), 08-24 truncated mid-run, 08-25 dark. Four disruptions in 13 days. Worth one look at the launcher before the logs rotate.
- Parallel-writer watch: tracked **116** (flat, third consecutive day), untracked **60 rows** (flat). Against the 08-24 flag rule (tracked > 125, or any untracked row resolving to no ref): tracked is under threshold; untracked-no-ref is **5**, down from 6 — the reduction is a reclassification, not a cleanup.

## graphify highlights (from `graphify-out/GRAPH_REPORT.md`, 2026-07-06 extraction — **51 days stale**)

- **`--update` refused again — 6th consecutive run.** `npx graphify . --update` was denied at the Bash-permission layer by the unattended allowlist, same as 08-24; four distinct `GEMINI_API_KEY` probe forms were refused on 08-22/08-23 before that. An unkeyed `--update` would spawn LLM subagents that gateguard blocks headless, so the skip is the safe branch either way. Per the loop contract: logged, continued, run **not** failed over graphify. Report mtime confirms staleness: `Jul 6 09:35`.
- **God Nodes (re-read from the report, unchanged):** `readSessionFromRequest()` 114 edges, `execute()` 62, `query()` 60, `getUserById()` 49, `requireCronAuth()` 46, `getTrackedSignals()` 42, `queryOne()` 41, `getTrackedSignalsForRequest()` 34, `getUserTier()` 33, `getOHLCV()` 32. Auth/session, DB-query, and signal-tracking remain the coupling hubs.
- **Surprising connections: none new** — file unchanged since 07-06. Standing set: backtest `comparison-chart.tsx` and `metrics-table.tsx` → `packages/strategies/src/run-backtest.ts` (both via `BacktestResult`, EXTRACTED); `atr-calibration-cache.ts` → `packages/signals/src/atr-calibration.ts` (INFERRED); two INFERRED `pad()` edges into `packages/agent/src/cli/cli.ts` (`scripts/diag-equity-inflection.ts` is the caller) that read as extractor name-collision noise.
- **Import cycles: four, unchanged** — three in `packages/agent/src/channels/base.ts` ↔ `webhook` / `discord` / `telegram`, plus `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`. The product-side cycle is a **static artifact, not a runtime one** (verified 08-22), so it does not block the decay-gate lane. The three `channels/base.ts` cycles remain un-re-verified.
- The extraction predates TC-24x, the D1 ledger, `#214`, `#216`, `#218`, and `#219` — and now the entire `/hub` surface this standup cites. Refresh needs an interactive session with `GEMINI_API_KEY` set.

## Sync result

- **Standup doc committed to `feat/premium-ui-overhaul` by explicit path** (`docs/automation/standups/2026-08-26.md`, single file). Nothing else staged. The 116-file drift still contains a revert of #219, a `STATE.yaml` that would delete five release records, and one homeless file that needs a `main`-based branch — none of which belongs in a standup commit.
- Branch + HEAD re-verified immediately before staging — unchanged from the STEP 0 snapshot, so no concurrent writer moved the base mid-run.
- No `feature → main` PR opened: owner-gated, consistent with 30+ prior daily runs. 53 mostly-superseded commits from a 77-behind branch would be noise, not a review.
- Protected `main` untouched. Commit SHA + push confirmation in the run trailer below.

## Lessons / Escalations (state file is machine-record-only; lessons recorded here per the sensitive-path pattern)

- **State-file note:** `loops/state/tradeclaw.STATE.md` is readable and holds only the launcher's machine run-records — no lessons/escalations headings exist in it. Confirmed: 08-21 → 08-24 daily all PASS, 08-20 triple FAIL(2), **no 08-25 entry**. STATE.md *writes* remain permission-denied (sensitive path), so lessons live in this committed doc.
- **LESSON (new, load-bearing — the deepest cut of the "untracked ≠ unshipped" family):** *path*-identity is not *content*-identity. `docs/plans/2026-08-10-dark-light-theme-craft.md` exists at that exact path in no ref anywhere, which is why 08-24's `git ls-tree` sweep classified it homeless — correctly, by its own test. But `main` carries the same document, same title, five lines shorter, at `2026-08-11-…`, merged in #214. A file renamed before merge defeats path-existence checks completely. **Before calling anything unshipped, grep `main` for the file's title or a distinctive first line, not just its path.** This is the third consecutive refinement of the same error class (08-23: product-i18n; 08-24: untracked rows generally; today: renamed siblings) and each round cost one wrong owner-escalation.
- **LESSON (new, verify the *location* of a claim, not just its existence):** the 08-24 run escalated that "the self-hosting docs advertise multi-arch images." The claim direction was right and the location was wrong — **no doc in the repo mentions arm64 at all.** The claim lives in rendered JSX on a sitemap-published page, complete with an `Apple Silicon ✓` badge. Had the owner acted on the 08-24 wording, they would have edited docs that say nothing and left the actual public assertion untouched. Cost of getting it right: one `git grep -l -i arm64 origin/main`. **Locate the surface before escalating a copy problem.**
- **LESSON (new, gate blind spot):** the 08-24 run recorded `exit 0 / PASS` while leaving its own trailer unwritten. `gate.ps1` checks that the output file exists, is non-empty, meets `minLines`, and is committed on an allowed branch — **it cannot see that the run died between the commit and the trailer.** A PASS verdict is evidence the artifact exists, not that the run completed. Read the artifact's tail before trusting the verdict.
- **LESSON (re-confirmed, 4th consecutive run):** re-deriving carried claims from the tree beats carrying them forward. Today that produced two corrections to 08-24 (theme-plan homeless → shipped-as-renamed; arm64 claim in docs → in rendered JSX) and confirmed five findings byte-for-byte. The loop's own prior output remains the least reliable input available to it.
- **ESCALATION (highest priority, day 3, unmoved):** `Docker Publish` red on `main` at `6342eb8c`. `Build & Push :latest` step 6 failed after 6m41s with QEMU setup green. **`tradeclaw/tradeclaw:latest` has not been rebuilt since `5ad4ee3e` on 08-22.** Nobody has re-run it. One free re-run resolves flake-vs-deterministic.
- **ESCALATION (upgraded today):** `/hub` renders `linux/arm64` and `Apple Silicon ✓` on a sitemap-indexed page (`HubClient.tsx:106,145-147`, `page.tsx:6`) while the workflow leg that produces that image is the one failing. If arm64 is dropped, **that page is the change**, not a doc. Framed as a transparency obligation, not a housekeeping note.
- **ESCALATION (carried, day 3, unmoved):** uncommitted `STATE.yaml` would delete `TC-250 … TC-254` from the release ledger if it ever merged. Named casualties, re-measured today, identical.
- **ESCALATION (carried, day 3, unmoved):** the primary checkout's README/media lane is a revert of #219, including resurrecting a 4.42 MB GIF the owner deliberately deleted. Loss-free to reset.
- **ESCALATION (carried, day 4, unmoved):** `AGENTS.md` (+37 / −9) — unshipped work, no branch, shared dirty checkout. One file.
- **ESCALATION (carried, re-scoped):** the **1,439-line research lane** (`recost-report.ts` + three test files) is now the *sole* genuinely stranded work of size, after the theme-plan draft was reclassified as shipped. `CLAUDE.md`, the other "homeless" row, is a 7-line import stub and should not be counted alongside it.
- **ESCALATION (decision-ready, standing — strengthened again):** retire-or-integrate `feat/premium-ui-overhaul`. 53 ahead / 77 behind; provably lacks `054_d1_alpha_ledger.sql`, the entire `product-i18n` lane, the `/hub` surface this standup had to read from `origin/main`, all three 08-22/08-23 merges, and the TC-250…TC-254 records. Every drift number this loop reports is dominated by the branch's own staleness rather than by real work.
- **ESCALATION (operational, upgraded from incident to pattern):** four scheduler disruptions in 13 days — 08-14 → 08-19 dark, 08-20 triple FAIL(2), 08-24 truncated after commit, 08-25 dark. Root cause still undiagnosed; runner logs may rotate.
- **ESCALATION (open, unchanged):** Pro-funnel instrumentation seed (weekly 08-07 P4) — measure whether the checkout-resume fix recovers conversions.
- **MONITOR (live boundary, re-verified from code today):** TC-254 prospective D1 ledger stays `collecting-evidence` / `not-promoted`. `d1-alpha-protocol.ts:12-14` on `main` = 365 days / 365 snapshots / 12 closed trades. No tuning, no backfill, no promotion, no archive deletion, no inferred execution.
- **LESSON (standing, re-confirmed):** headless production re-probe is approval-gated in the unattended allowlist. **No live-probe claims appear in this doc** — the `/hub` findings above are read from `origin/main` source, not from a rendered page, and the Docker Hub `:latest` staleness is inferred from the failed job's own name and the last successful run, not from a registry query.
- **OBSERVATION (this run):** the repository produced zero commits in 48 hours, and this loop authored one of the two most recent commits in it. Every item in NEXT is owner-gated; nothing in the backlog is blocked on this loop. Scope held at status + a single doc commit.

---
