# Daily Standup — 2026-08-07

- Branch: `feat/premium-ui-overhaul` @ `25fd9a8a`
- Upstream: `origin/feat/premium-ui-overhaul` — local is **1 ahead / 0 behind**. The 1-ahead is today's weekly propose-only doc (`25fd9a8a`), committed by the weekly research loop at 0847 and **not yet pushed** — a real push-hygiene item this cycle (resolved in Sync below). The prior 13-commit regime study + the 08-06 standup are already on the remote (pushed last cycle).
- vs `main`: **35 ahead / 59 behind** (was 33 / 59 on 08-06). Ahead rose 33→35 (+2) = the 08-06 standup doc (`15c53a1a`) + today's weekly doc (`25fd9a8a`); still exactly one product commit (`b061bef3 feat(ui): unify premium assets with emerald brand`, 2026-07-16) — the other 34 are loop docs + the committed research lane. Behind flat at 59.
- `origin/main` movement (last 24h): **none.** Tip is still `81a5f3f3 chore(intel): append 2026-08-05 daily intel ideas` — unchanged since 08-06. No owner merge, no product release this window. Quiet main.
- Stashes: none. In-progress merge/rebase: none (`.git/MERGE_HEAD` / `rebase-merge` / `rebase-apply` absent). Concurrent-writer check: HEAD re-verified `25fd9a8a` immediately before commit. ~60 sibling / `.claude` / `.codex` / `.portfolio-worktrees` worktrees exist on this host — none is this branch's tree; left untouched.
- Run window: scheduled unattended, 09:30 GMT+8. Provenance stamp: 2026-08-07 09:34 MPST (GMT+0800).
- Orchestration note: `ultracode` requested. The daily loop runs under `acceptEdits` + a narrow allowlist (Bash git/gh/date/graphify, Read/Write/Edit/Glob/Grep/Skill — no Task/Workflow tool), and the dynamic-workflow review gate (verified 07-23 → 08-06, holds today) blocks a headless `ultracode` / Workflow fan-out for a daily standup. Status+sync is single-context git work over a working tree shared with parallel writers — correct done inline (reconcile + safety-guard skills + direct git reads) with the single git mutation kept in the main loop. Fan-out would add no coverage here.

## DONE (last 24h)
- **Weekly propose-only research doc landed (`25fd9a8a`).** The Friday weekly loop ran at 0847 and produced `docs/research/weekly-research-2026-08-07.md` — 4 study-triggered transparency proposals that harvest the 2026-08-05 regime-expectancy study into user-facing honesty moves: (1) publish the regime-filter kill to the `/research` kill-ledger, (2) disclose reconstruction coverage (37% of trades classifiable; 1,995 non-crypto signals excluded for lack of a candle feed), (3) publish the QA/reconciliation gates as a trust signal, (4) position the pre-registered study as a registered-report standard. Each proposal carries a `Brand-alignment:` line and a `Grill verdict: SURVIVED`. Propose-only — no product code, no invented metrics; findings labelled research, not production record.
- **08-06 daily standup committed (`15c53a1a`).** The prior cycle's status doc landed inside this 24h window and was pushed cleanly to `origin/feat/premium-ui-overhaul` (it is at/below upstream now — not stranded). This 08-07 write makes **eleven consecutive clean daily writes** (07-28 → 08-07 + the 07-31 and 08-07 weeklies).
- No product code advanced on this branch since `b061bef3` (2026-07-16). The regime-expectancy study (13 commits, landed in the prior 24h window) is now backed up on the remote; no new study commits this cycle. The only forward motion in the last 24h is the two loop docs above.

## PENDING (not finished)
- **1 unpushed commit** — today's weekly doc `25fd9a8a` exists only in the local tree at run start. The weekly loop committed but did not push it. Pushed this run (see Sync) — feature-branch backup, categorically distinct from owner-gated feature→main integration.
- **The 4 weekly proposals (`25fd9a8a`) are actionable but owner-gated.** Publishing the regime kill to `/research`, the reconstruction-coverage disclosure, the QA-gates trust surface, and the registered-report positioning are all product/content changes — propose-only by contract. They need an owner call to become work items. Highest-leverage because they convert an already-committed internal study into public transparency the brand is built on.
- Uncommitted drift, owned by parallel sessions — LEFT UNTOUCHED (not this loop's work): **105 tracked files modified/deleted (+7710 / −2691)**, 57 untracked. Vs the 08-06 record (105 tracked +7710/−2691, 57 untracked): **byte-for-byte flat** — same file count, same insertion/deletion totals. The parallel writer was idle on the tracked tree this cycle (untracked **flat at 57 for a fourth consecutive cycle**; tracked line-delta now flat too). Lanes unchanged:
  - Premium UI overhaul (dominant tracked lane): `apps/web/app/components/`, `apps/web/components/`, `apps/web/components/landing/`, `apps/web/lib/`, locale pages (`ar`/`es`/`ms`/`zh`), `dashboard`, `screener`, `track-record`, `layout.tsx`, `globals.css`.
  - Deploy / self-host cleanup: working-tree deletions `Dockerfile.fly`, `apps/web/app/fly/FlyClient.tsx`, `apps/web/app/replit/ReplitClient.tsx`; modified `fly.toml`, `docker-compose.yml`, `.replit`, `.github/workflows/docker-test.yml`, `.env.example`, `.env.docker.example`, `packages/create-tradeclaw/index.js`, `docs/FLYIO.md` / `docs/REPLIT.md` / `docs/QUICKSTART.md`.
  - WS / price lanes: `apps/web/lib/hooks/use-price-stream.ts`, `use-websocket-prices.ts`, `apps/web/lib/signals-live.ts`, `apps/web/middleware.ts`.
  - AI-improvement / recost / regime lane: `STATE.yaml`, `docs/ai-improvement/*`, `scripts/research/recost-segment.ts`, `scripts/research/regime-study-assembly.ts` (+ its test), `scripts/test-docker.sh`.
- Untracked `product-i18n` localization layer — a substantial, test-carrying feature (≈26 files) still entirely uncommitted with no branch home. Re-confirmed present.
- Untracked slow-gate research lane (`scripts/research`: `slow-gate-cli.ts`, `slow-gate-assembly.ts`, `recost-report.ts` + 3 tests) and plan doc `docs/plans/2026-07-18-slow-regime-gate-sandbox.md` — the D1 slow-gate build the regime study commissions; still no committed home.
- Unpushed-to-`main` feature commit `b061bef3` — still the only non-doc commit in the 35-ahead set. Its premium-UI intent shipped via merged PR #168 and `main` has since moved seven artwork releases (TC-244..249) past it. Redundancy increasingly likely; owner call.
- Orphaned standups 07-13/14/15 on disk, untracked — NOT rescue-committed (verified retired: each already lives on other branches; the double-commit guard holds).
- Committed-standup GAP 07-24 → 07-27 — a CLOSED hole, treated permanent; no backfill (fabricating past-day status would violate the no-invented-data rule).
- Open PRs (**7**, none target this branch; all → `main` except #174): `#198 agent/ia-consolidation-20260805 → main` "make the front door match the product"; `#184` daily-intel; `#174` remotion draft (→ `release/premium-green-assets-20260716`); `#170` daily-intel; `#160` rescue-stranded-standups; `#149` loop standup 07-04; `#143` feature-flags ADR. Set unchanged vs 08-06.

## NEXT
- **Owner call on the 4 weekly transparency proposals (`25fd9a8a`).** These are the cleanest next value: the regime study is already committed and reproducible; publishing its kill + coverage caveats to `/research` and the QA gates as a trust surface is exactly the "radical transparency, wins AND losses" brand posture. Keep every published number measured or labelled `pending real data`; keep sandbox/EMA200 findings tagged LOCAL/research, never the production verified record.
- **The regime study's own verdict still sets the next build:** H1 (regime filter) and H2 (inversion) refuted; horizon is the only surviving lever → the **D1 slow-gate**. Advance `docs/plans/2026-07-18-slow-regime-gate-sandbox.md`. Keep it LOCAL research.
- Give the committed regime study + today's weekly proposals a home toward `main` via a focused `scripts/research` + `docs/research` PR — independent of the premium-UI drift. Do not rebase a 59-behind status branch. Owner-gated.
- Owner decision (gated), still top: integrate or retire `feat/premium-ui-overhaul`. Now **35-ahead / 59-behind** with one possibly-redundant feature commit (`b061bef3`) + 105-file uncommitted drift; `main` has shipped seven artwork releases past its intent. Confirm `b061bef3` still adds value vs current `main`, else retire.
- Decide the home for the untracked `product-i18n` layer (≈26 files) and the untracked slow-gate research lane. Both are coherent, test-carrying sets stranded with no committed branch — clean independent PRs.
- Watch parallel-writer churn: tracked flat at 105, untracked flat at 57 (4th cycle), line-delta flat this cycle. Flag if tracked breaks 110 or untracked breaks 70.
- Refresh graphify: `GRAPH_REPORT.md` is the 2026-07-06 extraction — now **32 days stale**. A fresh `--update` stays deferred on scheduled runs (spawns gateguard-blocked LLM subagents, would ingest other sessions' uncommitted drift); env-var presence probes remain permission-blocked from the loop. Queue for an interactive session.

## graphify highlights (from graphify-out/GRAPH_REPORT.md, 2026-07-06 extraction — 32 days stale)
- Incremental `--update` intentionally skipped on this status run (gateguard-blocked LLM subagents + would ingest other sessions' uncommitted drift; `GEMINI_API_KEY` presence not probeable from the loop's permission layer). Highlights embedded from the last committed extraction (11,497 nodes · 18,049 edges · 845 communities).
- God Nodes (core abstractions to watch for coupling): `readSessionFromRequest()` 114 edges, `execute()` 62, `query()` 60, `getUserById()` 49, `requireCronAuth()` 46, `getTrackedSignals()` 42, `queryOne()` 41, `getTrackedSignalsForRequest()` 34, `getUserTier()` 33, `getOHLCV()` 32. Auth/session and DB access dominate — a change to `readSessionFromRequest()` fans out across 114 edges.
- Surprising connections: `ComparisonChartProps` / `MetricsTableProps` (`apps/web/app/backtest`) both reference `BacktestResult` from `packages/strategies/src/run-backtest.ts` — backtest UI is tightly bound to the strategy runner's result shape. `refreshAtrCalibration()` → `calibrateAtrMultiplier()` couples `apps/web/app/lib/atr-calibration-cache.ts` to `packages/signals/src/atr-calibration.ts`.
- Import cycles (2-file, worth breaking): `packages/agent/src/channels/base.ts` ↔ `webhook.ts` / `discord.ts` / `telegram.ts` (three cycles through the channel base class); `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`.

## Sync result
- Committed this standup on `feat/premium-ui-overhaul` by explicit path (standup doc only; the 105-file drift + 57 untracked stay untouched — not this loop's work).
- **Pushed the branch to `origin/feat/premium-ui-overhaul`** after re-verifying HEAD had not shifted — this backs up today's weekly propose-only doc (`25fd9a8a`, previously local-only) plus this standup. Push to the feature branch's own remote is safe hygiene, not a deploy (Railway ships only from `main`). Remote-tip advance confirmed via `git ls-remote`.
- No commit or merge to `main`; feature→main integration and the regime-study / weekly-proposal PR disposition remain owner-gated (deliberately not auto-opening a PR from a 59-behind status branch carrying unrelated parallel drift — a junk PR of 35 commits + 105-file drift is worse than none).

## Lessons / escalations (this run)
- LESSON: two consecutive loop docs (08-06 standup, 08-07 weekly) both committed but the weekly left itself unpushed — the weekly loop's gate checks commit, not push. The daily loop's safe-sync scope correctly catches and backs up that unpushed weekly doc. Push hygiene is a shared responsibility; the daily standup is the natural backstop for an unpushed loop doc on the same feature branch.
- LESSON: working-tree drift was byte-for-byte flat vs 08-06 (105 tracked, +7710/−2691, 57 untracked — all identical). A fully idle parallel-writer cycle is itself signal: the premium-UI / i18n / recost lanes are paused, not churning. The stranded-drift escalation is stable, not growing — strengthens the case that these are finished-but-unhomed sets awaiting a commit decision, not live work.
- ESCALATION (owner, standing): the 4 weekly transparency proposals (`docs/research/weekly-research-2026-08-07.md`) convert an already-committed, reproducible regime study into public honesty (kill-ledger entry, reconstruction-coverage disclosure, QA-gate trust signal, registered-report positioning). This is core brand work sitting behind an owner gate. Pick it up or explicitly defer.
- ESCALATION (owner, standing): the committed regime study is a clean, self-contained, test-carrying research lane stranded on a 59-behind status branch. Best home is a focused `scripts/research` + `docs/research` PR to `main`, independent of the premium-UI drift. Needs an owner call to cut it loose.
- ESCALATION (owner, standing): `feat/premium-ui-overhaul` is very likely redundant — `b061bef3`'s intent shipped via merged PR #168 and `main` moved seven artwork releases (TC-244..249) past it; the branch is 59 behind. Confirm value vs current `main` or retire — do not rebase.
- ESCALATION (owner, standing): uncommitted drift on this branch still has no committed home — 105 tracked / 57 untracked across ≥3 separable, test-carrying feature sets (premium UI, `product-i18n` ≈26 files, slow-gate research) plus deploy/self-host cleanup and artwork assets. Open 15 days; now size-stable AND churn-stable (flat this cycle). Needs deliberate per-lane commit-and-PR or a documented discard.
- LESSON: `ultracode` fan-out for a daily standup remains blocked by the dynamic-workflow review gate under unattended permissions (verified 07-23 → 08-06, holds 08-07); the daily loop's allowlist also excludes Task/Workflow tools. Inline read-only investigation with the single mutation in the main loop stays the safe pattern for status+sync over a shared tree.
- NOTE: the launcher-expected loop STATE file at `C:\Users\User\.claude\loops\state\tradeclaw.STATE.md` holds only the launcher-appended run log; edits to that global path are sensitive-path-blocked from the loop, so durable lessons/escalations live here in the committed standup doc.
