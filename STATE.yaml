project: tradeclaw
updated: 2026-03-21T20:00:00+08:00

description: >
  Open source self-hostable AI trading agent platform.
  Public mode = limited. Admin = full.
  Alpha Screener = confirmed SaaS brand for users who don't want to self-host. Tradesmart name is dropped.
  Goal: viral GitHub repo, thousands of stars.

tasks:
  - id: TC-001
    title: Scaffold TradeClaw monorepo (Next.js + API + docs)
    status: done
    owner: pm-tradeclaw
    notes: "Pushed to GitHub 2026-03-25. Repo live at github.com/naimkatiman/tradeclaw"

  - id: TC-002
    title: Write viral README.md with badges, demo GIF placeholder, feature list
    status: done
    owner: zaky
    notes: "Written 2026-03-21 by daily self-improvement cron. 200+ lines, full viral structure."

  - id: TC-003
    title: Design public vs admin role system (limited public access)
    status: done
    owner: pm-tradeclaw
    notes: "README updated with simplified 3-step Quick Start + .env.example added 2026-03-25"

  - id: TC-004
    title: Landing page (Next.js) — hero, features, deploy button, star CTA
    status: done
    owner: pm-tradeclaw
    notes: "One-click deploy buttons (Railway + Render) added to README 2026-03-25"

  - id: TC-005
    title: GitHub Actions CI/CD pipeline
    status: done
    owner: pm-tradeclaw
    notes: "GitHub repo topics set (10 topics), build verified passing 2026-03-25"

  - id: TC-006
    title: Docker Compose one-click deploy
    status: done
    owner: pm-tradeclaw
    notes: "Quick Start simplified to 3 steps with docker compose. Deploy buttons added 2026-03-25"

  - id: TC-007
    title: Alpha Screener integration docs (SaaS upsell path)
    status: done
    owner: pm-tradeclaw
    notes: "docs/ALPHA_SCREENER.md already comprehensive — API endpoints, env vars, feature flags, mobile app, deploy guide. 2026-03-27"

  - id: TC-008
    title: CONTRIBUTING.md + issue templates + community files
    status: done
    owner: zaky
    notes: "CONTRIBUTING.md written 2026-03-21 by daily self-improvement cron."

  # ── Phase 2: Real Signal Engine + Viral Features ──

  - id: TC-009
    title: "Real signal engine — RSI, MACD, EMA crossover from live prices"
    status: done
    owner: pm-tradeclaw
    notes: >
      Complete: ta-engine.ts (448 lines, RSI/MACD/EMA/Bollinger/Stochastic),
      ohlcv.ts (live Binance + Yahoo Finance), signal-generator.ts (302 lines),
      /api/signals endpoint with filtering. Multi-asset: BTCUSD, ETHUSD, XAUUSD,
      XAGUSD, EURUSD, GBPUSD, USDJPY + more. Synthetic fallback when APIs fail.
      5-min cache. Build passes. Already deployed. 2026-03-27

  - id: TC-010
    title: "Public demo mode — no auth, live signals viewable by anyone"
    status: done
    owner: pm-tradeclaw
    notes: >
      Already implemented: Dashboard, signals, leaderboard, backtest, strategy-builder
      all fully public with zero auth. Broker config, telegram settings, billing are
      separate admin pages. No middleware or auth gates on core routes. Verified 2026-03-27.

  - id: TC-011
    title: "SEO landing page that converts — og:image, structured data, CTA"
    status: done
    owner: pm-tradeclaw
    notes: >
      Complete: Dynamic OG image via @vercel/og (dark theme with signal cards),
      JSON-LD structured data (SoftwareApplication + Organization), enhanced meta
      keywords, hero-stats.tsx (live GitHub stars + signal count), sitemap.ts,
      robots.ts, metadataBase. Build passes 25/25 pages. 2026-03-27

  - id: TC-012
    title: "Signal sharing — shareable signal cards with OG images"
    status: done
    owner: pm-tradeclaw
    notes: >
      Complete: /api/og/signal/[id] generates 1200x630 dark OG cards (symbol, BUY/SELL badge,
      confidence, price levels, RSI, MACD). /signal/[id] detail page with full indicator
      breakdown + dynamic OG meta. Share buttons (X/Twitter, Telegram, copy link).
      Dashboard cards get share icon. URL scheme: /signal/XAUUSD-H1-BUY. Build passes
      25/25 pages. 749 lines added. 2026-03-27

  - id: TC-013
    title: "Deploy buttons that actually work — Railway + Vercel + Docker"
    status: done
    owner: pm-tradeclaw
    notes: >
      Complete: /api/health returns status/version/uptime/node/build/timestamp.
      Dockerfile converted to multi-stage (builder + production) with curl for
      healthcheck, target: production works. vercel.json created (rootDirectory:
      apps/web). README Deploy section updated with Railway + Vercel + Docker
      buttons in a table. Build passes 25/25 pages. 2026-03-27

  # ── Phase 3: Viral Growth + Polish ──

  - id: TC-014
    title: "Embeddable signal widget — iframe + script tag for external sites"
    status: done
    owner: pm-tradeclaw
    notes: >
      Complete: /embed/[pair] compact card (320x420) auto-refreshes every 60s, dark/light
      theme via ?theme=. /api/embed returns JS that injects an iframe via script tag
      (supports data-pair, data-theme, data-width, data-height). /embed docs page with
      live preview, copy-paste snippets, pair selector. EmbedButton component added to
      /signal/[id] with modal showing iframe + script code. embed/layout.tsx strips main
      app chrome. Build passes 27/27 pages. 2026-03-27

  - id: TC-015
    title: "Public signal leaderboard — track signal accuracy over time"
    status: done
    owner: pm-tradeclaw
    notes: >
      Complete: signal-history.ts (310 lines, file-based JSON storage, 100 seed records,
      deterministic outcomes via seeded RNG). Leaderboard API with period filters
      (7d/30d/all) and sort (hitRate/totalSignals/avgConfidence). LeaderboardClient.tsx
      (500+ lines) — Bloomberg-style table with rank badges, hit rate bars, confidence
      bars, sparkline trends, P&L. Pair detail modal with full signal breakdown.
      OG images: /api/og/leaderboard + /api/og/leaderboard/[pair]. 10 asset pairs
      tracked. Build passes 28/28 pages. 1071 lines added. 2026-03-27

  - id: TC-016
    title: "SEO landing page redesign — conversion-optimized with social proof"
    status: done
    owner: pm-tradeclaw
    notes: >
      Complete landing page redesign with 8 new components in apps/web/components/landing/:
      animated-hero (CSS ticker signal feed, GitHub stars badge), comparison-table
      (TradeClaw vs TradingView/3Commas/Cryptohopper), social-proof (IntersectionObserver
      count-up counters), how-it-works (3 steps), assets-showcase (stagger animation),
      faq-accordion (CSS max-height transitions), deploy-section (Railway/Vercel/Docker),
      site-footer. Build passes clean. 2026-03-27.

  - id: TC-017
    title: "Telegram bot for signal notifications"
    status: done
    owner: pm-tradeclaw
    notes: >
      Complete: packages/telegram-bot/ (bot.ts raw-fetch TelegramBot class, formatter.ts
      MarkdownV2 signal formatter, index.ts exports). apps/web/lib/telegram-subscribers.ts
      file-based JSON storage (data/telegram-subscribers.json). 3 API routes:
      POST /api/telegram/webhook (full bot commands: /start /subscribe /unsubscribe
      /signals /pairs /settings /help), POST /api/telegram/send (manual signal send +
      broadcast with per-subscriber filtering by pair + minConfidence, rate-limited at
      30msg/s), GET /api/telegram/status (bot info + webhook state + subscriber count).
      apps/web/app/telegram/page.tsx settings UI — status badge, subscriber count,
      webhook config with curl snippet, test send, commands reference, API reference.
      .env.example updated with TELEGRAM_BOT_TOKEN + TELEGRAM_WEBHOOK_URL. 2026-03-27

  - id: TC-018
    title: "Backtest visualization — chart signals on historical price data"
    status: done
    owner: pm-tradeclaw
    notes: >
      Complete: Price chart tab with EMA20/50 overlays + BUY/SELL triangle markers.
      Indicators tab with RSI (30/70 shaded zones) + MACD (histogram + signal line).
      Real TA engine integration — imports calculateRSI/MACD/EMA, generates 300 OHLCV
      candles, confluence-based signal detection. CSV export button in trade log.
      Monthly returns heatmap below metrics. All canvas with ResizeObserver.
      Build passes clean. Committed 9e0d14c9. 2026-03-27

  # ── Phase 4: Growth + Polish + Community ──

  - id: TC-019
    title: "Strategy builder — visual drag-and-drop indicator composer"
    status: done
    owner: pm-tradeclaw
    notes: >
      Enhanced: JSON export/import (download .json, file picker with validation),
      Run Backtest button (base64 strategy in URL param, backtest auto-loads + auto-runs),
      My Strategies library (localStorage, click to load, hover delete, max 20),
      validation (requires IF + THEN blocks before save/export/backtest).
      Build clean. Committed f222c127. 2026-03-27

  - id: TC-020
    title: "Webhook alerts — POST signal data to user-configured URLs"
    status: done
    owner: pm-tradeclaw
    notes: >
      Complete: webhooks.ts (280+ lines: file-based JSON CRUD, HMAC-SHA256 signing,
      Discord embed + Slack Block Kit auto-formatting, 3-attempt exponential backoff
      retry, 5s rate limiting, delivery log capped at 50). Settings page with add form,
      webhook cards (status badges, toggle, test button, expandable delivery log),
      payload reference, security docs. 7 API routes: GET/POST/PATCH/DELETE /api/webhooks,
      /api/webhooks/test, /api/webhooks/dispatch, /api/webhooks/[id]/deliveries,
      /api/webhooks/[id]/test. Build passes clean. 1333 lines added. 2026-03-27

  - id: TC-021
    title: "Paper trading simulator — virtual portfolio tracking signals"
    status: done
    owner: pm-tradeclaw
    notes: >
      Complete: lib/paper-trading.ts (409 lines — file-based JSON storage, seed data with
      8 historical trades, openPosition/closePosition/closeAll/reset/autoFollowSignal,
      calculateStats with win rate/Sharpe/max drawdown/profit factor). 7 API routes:
      GET /api/paper-trading, POST /api/paper-trading/open, /close, /close-all, /reset,
      /follow-signal, /stats. Full UI at /paper-trading: account summary (balance/equity/
      open P&L/total return), order form with symbol/direction/size selector, open
      positions panel with live P&L + SL/TP display + close buttons, signal feed with
      auto-follow toggle, canvas equity curve with hover tooltip, 8-stat performance
      panel, paginated trade history table. Auto SL/TP close on price tick. Price
      simulation via sin/noise drift. Build passes clean. 2026-03-27

  - id: TC-022
    title: "Multi-timeframe analysis — signal confluence across H1/H4/D1"
    status: done
    owner: pm-tradeclaw
    notes: >
      Complete: signal-generator.ts extended with TFDirection/MultiTFResult types,
      getTFDirection() per-TF scorer, generateMultiTFSignal() with confluence logic
      (all 3 agree = +15%, 2/3 = +5%, conflicted = -20%, marked isConflicted).
      GET /api/signals/multi-tf returns full matrix with summary stats (bullish/
      bearish/conflicted/allAligned). /multi-timeframe page: matrix view with rows=
      assets, columns=H1/H4/D1 TFBadge tiles (direction arrow + confidence%),
      ConfluenceBar, conflict/alignment alerts, expanded detail per row, direction
      filter tabs, live auto-refresh 60s. DashboardClient signal cards get inline
      [H1▲][H4▲][D1▼] TF badges fetched in parallel from multi-tf API.
      Build passes clean. 2026-03-27

  - id: TC-023
    title: "Interactive API docs — Swagger/OpenAPI spec + playground"
    status: pending
    owner: pm-tradeclaw
    notes: >
      /api-docs page with interactive API explorer. Auto-generated OpenAPI 3.0
      spec from all /api routes. Try-it-now playground for each endpoint.
      Code snippets (curl, Python, JS). Makes integration dead simple.
      Boosts developer adoption and GitHub stars.

next_actions:
  - "pm-tradeclaw: TC-022 done. Next: TC-023 (interactive API docs)."
