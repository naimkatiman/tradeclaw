# Daily Standup — 2026-08-24

- **Branch:** `feat/premium-ui-overhaul`
- **HEAD:** `98ec2947`
- **Upstream:** `origin/feat/premium-ui-overhaul` — 0 ahead / 0 behind (in sync)
- **vs `origin/main` (`6342eb8c`):** 52 ahead / 77 behind
- **Working tree:** 116 tracked paths drifted (`+8193 / −2911`, 113 modified + 3 deleted), 60 untracked rows
- **Stashes:** 2 (`tc254-restack-after-tc253-final-main`, `tc253-restack-20260810`) — retained
- **In-progress git ops:** none (no `MERGE_HEAD`, no rebase state)
- **Protected-branch check:** HEAD is not `main`/`master`/`release/*` → SYNC allowed

## DONE (last 24h)

- **`main` moved again — third owner merge in three days.** PR **#219** `docs(readme): correct five code-contradicted claims and cut the homepage to a third of its weight` merged `2026-08-23T08:33:07Z` (16:33 +0800) → tip `6342eb8c`. Six files, `+18 / −13`. The 11-day drought that dominated the 08-21/08-22 standups is now fully broken (`e8b68e87` 08-22, `5ad4ee3e` 08-22, `6342eb8c` 08-23).
- **The 4.42 MB inline GIF is gone from `main`.** #219 **deleted** `docs/assets/demo.gif` (`Bin 4417328 → 0`), shrank `docs/assets/social-preview.png` from 348,960 → 127,887 bytes, added `docs/assets/hero-dark.png` (105,893 B) and `hero-light.png` (146,934 B), and updated `scripts/build-readme-media.ps1`. The 08-23 escalation on README media weight is **CLOSED by evidence**, not by assumption.
- **PR #217 is CLOSED, unmerged** (`closedAt 2026-08-23T07:02:26Z`, `mergedAt: null`). Both blockers this loop diagnosed on 08-23 — the `truth-labeling.test.ts:173` copy-guard and the `CONFLICTING` README rebase — were resolved by **abandoning the branch, not fixing it**: #219 shipped from a fresh branch `chore/readme-facts-media` 91 minutes later. Net outcome achieved; the specific remediation this loop recommended was not the path taken.
- **On this branch:** 2 commits in the strict 24h window — `6beab0b6` (the 08-23 standup) and `98ec2947` (its run trailer). Both docs-only, both already pushed.

## PENDING (not finished)

- **RED CHECK ON `main` — `Docker Publish` failed on `6342eb8c`** (run `32628564441`, `2026-08-23T08:40Z`). First failure in the last 8 sampled runs of that workflow; the `CI` workflow on the same SHA is **green**. Root cause pinned to the line:
  - `qemu: uncaught target signal 4 (Illegal instruction) - core dumped` (×3) → `⨯ Next.js build worker exited with code: null and signal: SIGILL`, during `Generating static pages using 3 workers (0/325)`.
  - `.github/workflows/docker-publish.yml:39` uses `docker/setup-qemu-action@v3`; **line 54** sets `platforms: linux/amd64,linux/arm64`. The crash is the **arm64 leg running Next.js static generation under QEMU user-mode emulation**.
  - **Not attributable to #219's content.** #219 touched only README prose and image assets. Nothing in that diff reaches the static-generation worker. Treat as an emulation/runner-image failure, not a code regression.
- **`STATE.yaml` — live stale-base clobber, now with named casualties.** Three versions disagree:

  | source | lines | highest task id |
  |---|---|---|
  | working tree (uncommitted) | 5843 | **TC-249** |
  | branch `HEAD` | 3606 | TC-242 |
  | `origin/main` | 4601 | **TC-254** |

  The uncommitted edit is `+2017 / −775` against `origin/main` and **contains no `TC-254` record at all** (`grep -c` → 0 in both the worktree and branch HEAD; present on `main` at line 4513). Committing and merging the working-tree `STATE.yaml` from this 77-behind base would **erase TC-250 through TC-254** from the release ledger, including the D1 alpha ledger record. Do not stage this file from this tree.
- **README/media lane in this checkout is now a revert, not a mirror.** On 08-23 these paths were byte-identical to `main`. #219 changed that overnight — verified by blob hash this run:

  | path | working tree | `origin/main` | meaning |
  |---|---|---|---|
  | `README.md` | pre-#219 | `6342eb8c` | `+8 / −14` vs main — worktree is the **older** copy |
  | `docs/assets/demo.gif` | `584529f4`, 4,417,328 B | **absent (deleted by #219)** | committing resurrects the 4.42 MB GIF |
  | `docs/assets/social-preview.png` | `f90d3545`, 348,960 B | `5a7a8c38`, 127,887 B | worktree is 221 KB heavier / stale |
  | `scripts/build-readme-media.ps1` | `dd9ded60` | `35bc1801` | worktree is pre-#219 |

  Staging this lane would undo five fact corrections and re-add the GIF the owner just removed.
- **Untracked drift fully classified (60 rows) — the headline count is almost entirely noise:**
  - **51 rows exist on `origin/main`.** They read as "untracked" only because this branch is 77 behind, so files `main` gained after the branch point have no index entry here. Confirmed by `git ls-tree -r origin/main` against every path.
  - **3 rows are standups (07-13, 07-14, 07-15) already committed on loop branches** — `8dadd644`, `8ec65a38`, `25420b5e`. Present in history, absent from `main`. The double-commit guard applies: do not "rescue" these blindly.
  - **6 rows exist in no ref at all** — the only genuinely homeless untracked work: `CLAUDE.md`, `docs/plans/2026-08-10-dark-light-theme-craft.md` (102 lines), and a research lane of **1,439 lines**: `scripts/research/recost-report.ts` (219) + `__tests__/recost-report.test.ts` (310) + `__tests__/regime-expectancy-study-cli.test.ts` (163) + `__tests__/slow-gate-cli.test.ts` (747).
- **`AGENTS.md` (+37 / −9) is still homeless — unchanged from 08-23.** Re-verified this run: worktree vs `origin/main` = `+37 / −9`; branch `HEAD` vs `origin/main` = **empty**. The edit is net-new and belongs to no branch. Second consecutive day sitting in a shared dirty checkout.
- **Open PRs: 7, all stale.** Oldest `#143` (06-28), newest `#198` (08-05). Three are `CONFLICTING` (`#198`, `#184`, `#170`), two are drafts (`#174`, `#143`). No open PR has moved in 19 days. No PR targets this branch head.

## NEXT

- **Owner: triage the red `Docker Publish` on `main` first — it is the only red check on the default branch.** The failure is `SIGILL` inside QEMU during Next.js static generation of 325 pages on the `linux/arm64` leg. Cheapest diagnostic is a re-run of `32628564441` with no code change: if it goes green, it is an emulation flake and the fix is a retry policy; if it stays red, the runner image or QEMU version moved and the honest options are (a) drop `linux/arm64` from `docker-publish.yml:54` and say so in the release notes, or (b) build arm64 on a native arm runner. **Do not silently drop the arm64 platform** — the self-hosting docs advertise multi-arch images, and quietly narrowing that is exactly the kind of undisclosed change this project's positioning forbids.
- **Owner: `STATE.yaml` in the primary tree is a live data-loss hazard, not a housekeeping item.** It is one uncommitted file that would delete five release records (TC-250…TC-254) if it ever rode a merge. Either re-anchor the edit onto `origin/main` `6342eb8c` and keep only the genuinely new records, or discard the working copy. This is the second time this exact failure mode has been caught before it landed.
- **Owner: `AGENTS.md` — cut a branch from `6342eb8c`, apply that one file, PR it.** Unchanged advice, now two days old. It is 46 changed lines and it is the single cleanest merge available today.
- **Owner: decide on the 1,439-line research lane** (`recost-report` + three CLI/assembly test files). This is the **largest genuinely unshipped work in the repo** — it exists in no ref, has tests, and is sitting untracked in a tree shared by concurrent writers. Branch-or-discard.
- **Owner: reset the README/media lane in the primary checkout.** Four paths are now *older* than `main` and would revert #219 if staged. Since they carry no local work, restoring `README.md` and `docs/assets/demo.gif` from `HEAD` and deleting the two stale untracked copies is loss-free and removes the sharpest foot-gun in this tree.
- **Owner: re-scope weekly proposal 2 to a publication change** — carried forward, still the best value-per-effort item. The LOCAL-vs-production fact is already a machine-readable `provenance` field on three data files and already rendered at `admin/strategy-library/page.tsx:184` behind `requireAdmin()`. Smallest honest version: render that existing string on `/track-record`. No new data plumbing.
- **Owner: weekly proposal 1 (decay delivery-gate) remains the largest unblocked lane.** Hook `alert-channels.ts:206` (`case 'email':`). Must compute on production Postgres, not the LOCAL sample, and must keep demoted strategies **visible-as-demoted** — auto-demote is a transparency feature, not a hiding mechanism. Branch from `6342eb8c`.
- **Owner / monitor: TC-254 stays `collecting-evidence` / `not-promoted`.** Strict boundary holds: no tuning from prospective observations, no backfill or ledger rewrite, no auto-promotion, no deletion of the losing observed archive, no inferred broker execution. A missing daily row is a monitoring event, not a repair opportunity. Note this candidate exists **only on `main`** — it is invisible from this branch, which is itself an argument for retiring the branch.
- **Owner: investigate the 08-14 → 08-19 scheduler gap + 08-20 triple FAIL(2).** Four healthy runs since. Evidence may rotate.
- Parallel-writer watch: tracked **116** (flat vs 08-23), untracked **60 rows** (up from 38 — **entirely explained**: 51 are `main` content this branch lacks, and the count rises mechanically as `main` advances). Because the untracked count now tracks `main`'s progress rather than local activity, the old row-count threshold is meaningless. **New flag rule: tracked > 125, or any untracked row that resolves to no ref (today: 6).**

## graphify highlights (from `graphify-out/GRAPH_REPORT.md`, 2026-07-06 extraction — 49 days stale)

- **`--update` skipped again — 5th consecutive run.** `npx graphify . --update` was refused outright by the unattended allowlist this run (permission denied on the Bash invocation itself), after four distinct `GEMINI_API_KEY` probe forms were refused on 08-22/08-23. An unkeyed `--update` spawns LLM subagents that gateguard blocks headless, so the skip is the safe branch regardless. Per the loop contract: logged, continued, run **not** failed over graphify.
- **God Nodes (core abstractions), unchanged:** `readSessionFromRequest()` 114 edges, `execute()` 62, `query()` 60, `getUserById()` 49, `requireCronAuth()` 46, `getTrackedSignals()` 42, `queryOne()` 41, `getTrackedSignalsForRequest()` 34, `getUserTier()` 33, `getOHLCV()` 32. Auth/session, DB-query, and signal-tracking remain the coupling hubs.
- **Surprising connections:** none new — the report file has not changed since 07-06. Standing set: backtest `comparison-chart.tsx` and `metrics-table.tsx` → `packages/strategies/src/run-backtest.ts` (both via `BacktestResult`, EXTRACTED); `atr-calibration-cache.ts` → `packages/signals/src/atr-calibration.ts` (INFERRED); two INFERRED `pad()` edges into `packages/agent/src/cli/cli.ts` that read as extractor name-collision noise.
- **Import cycles: four, unchanged** — three in `packages/agent/src/channels/base.ts` ↔ `webhook` / `discord` / `telegram`, plus `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`. The product-side cycle is a **static artifact, not a runtime one** (verified 08-22), so it is not a prerequisite for the decay-gate lane. The three `channels/base.ts` cycles remain un-re-verified.
- The extraction predates TC-24x, the D1 ledger, `#214`, `#216`, `#218`, and `#219`. Refresh needs an interactive session with `GEMINI_API_KEY` set.

## Sync result

- **Standup doc committed to `feat/premium-ui-overhaul` by explicit path** (`docs/automation/standups/2026-08-24.md`, single file). Nothing else staged. This run proves that is the correct call three times over: the 116-file drift now contains a **revert of #219**, a `STATE.yaml` that would **delete five release records**, and one genuinely homeless file (`AGENTS.md`) that needs a `main`-based branch rather than this one.
- Branch + HEAD re-verified immediately before staging (`feat/premium-ui-overhaul` @ `98ec2947`, upstream 0/0) — unchanged from the STEP 0 snapshot, so no concurrent writer moved the base mid-run.
- No `feature → main` PR opened: owner-gated, consistent with 30+ prior daily runs. A PR of 52 mostly-superseded commits from a 77-behind branch would be noise. Recorded as owner-gated, not silently skipped.
- Protected `main` untouched. Commit SHA + push confirmation in the run trailer below.

## Lessons / Escalations (state file is machine-record-only; lessons recorded here per the sensitive-path pattern)

- **State-file note:** `loops/state/tradeclaw.STATE.md` is readable and holds only the launcher's machine run-records (no lessons/escalations headings exist in it). It confirms 08-21/08-22/08-23 daily = PASS and the 08-20 triple FAIL(2). STATE.md **writes** remain permission-denied (sensitive path), so lessons live in this committed doc.
- **CORRECTION to the 08-23 standup (this loop's own error):** it called the `product-i18n` localization lane "the largest genuinely stranded feature (20+ untracked files with tests)" and asked for a branch-or-discard call. **Wrong — it shipped.** All 22 `product-i18n` files are on `origin/main` (merged via `#207`, `0c069e51`); they appear untracked here only because this branch predates that merge. Worse, the local copies are *older* than the shipped ones (`dashboard.ts` 663 lines local vs 701 on main; `track-record.ts` 475 vs 523; `editorial-hero-art.tsx` 30 vs 32), so the correct action is **discard the local drafts**, not branch them. Branching them would have re-proposed a regression as a feature.
- **LESSON (new, load-bearing — generalizes the 08-23 lesson):** on a branch that is far behind, `git ls-files --others` is **not** a list of unshipped work. It is "files with no index entry", which conflates (a) new local work, (b) files `main` gained after the branch point, and (c) files committed on sibling branches. Today the split was 6 / 51 / 3. The 08-23 run learned this for *tracked* drift via blob hashing; it did not apply the same discipline to *untracked* rows and shipped a wrong escalation as a result. **Resolve every untracked row against `origin/main`'s tree and `git log --all` before calling anything stranded.** Cost: two `git ls-tree` batches and six `git log --all` probes.
- **LESSON (new, direction matters):** "differs from `main`" is only half a finding — the *direction* decides the action. `README.md`, `demo.gif`, `social-preview.png`, and `build-readme-media.ps1` all differ from `main` today, and in every case the working tree holds the **older** copy, so committing them would be a revert. Line counts and blob sizes disambiguate direction for a few tokens; a bare "modified" verdict would have pointed the exact wrong way.
- **LESSON (new, CI):** a red check on a docs-only merge commit is not evidence the docs broke something. `Docker Publish` failed on `6342eb8c` with `SIGILL` under QEMU during static generation, on a PR that changed only prose and images. Read the failing step before attributing the failure to the diff. The 08-23 lesson said "suspect a copy-guard before flake"; today's is the mirror — **suspect infrastructure when the failing step has no causal path to the diff.**
- **LESSON (re-confirmed):** verifying carried-forward proposals against live code paid out a third consecutive run. 08-22 retired weekly proposal 3 (already shipped); 08-23 rescoped proposal 2; 08-24 retired the `product-i18n` escalation entirely.
- **ESCALATION (new, highest priority):** `Docker Publish` is **red on `main`** at `6342eb8c`. The default branch is not publishing images. Root cause diagnosed to the workflow line; remediation is an owner call between retry-policy and dropping/relocating the arm64 build. **If arm64 is dropped, the self-hosting docs must say so.**
- **ESCALATION (upgraded from housekeeping):** the uncommitted `STATE.yaml` would **delete TC-250…TC-254** from the release ledger if it ever merged. Named casualties, not a theoretical risk. Second catch of this exact stale-base pattern.
- **ESCALATION (new):** the primary checkout's README/media lane is now a **revert of #219**, including resurrecting a 4.42 MB GIF the owner deliberately deleted. Loss-free to reset.
- **ESCALATION (carried, unchanged 2 days):** `AGENTS.md` (+37 / −9) is unshipped work in a shared dirty checkout with no branch. One file. It will be lost or mis-committed if it stays there.
- **ESCALATION (re-based on today's evidence):** the **1,439-line research lane** (`recost-report.ts` + three test files, in no ref anywhere) replaces `product-i18n` as the largest genuinely stranded work. Needs a branch-or-discard call.
- **ESCALATION (decision-ready, standing — argument strengthened):** retire-or-integrate `feat/premium-ui-overhaul`. 52 ahead / 77 behind; provably lacks `054_d1_alpha_ledger.sql`, the entire `product-i18n` lane, all three 08-22/08-23 merges, and the TC-250…TC-254 ledger records. Every drift measurement this loop reports is now dominated by the branch's own staleness rather than by real work.
- **ESCALATION (operational, open):** daily loop dark 08-14 → 08-19; all three 08-20 runs FAILED (gate FAIL(2), `claude exit 1`); `2026-08-20.md` never written. Four healthy runs since, no repo damage, root cause undiagnosed, logs may rotate.
- **ESCALATION (open):** Pro-funnel instrumentation seed (weekly 08-07 P4) — measure whether the checkout-resume fix recovers conversions — still open.
- **MONITOR (live boundary):** TC-254 prospective D1 ledger stays `collecting-evidence` / `not-promoted`. No tuning, no backfill, no promotion, no archive deletion, no inferred execution.
- **LESSON (standing, re-confirmed):** headless production re-probe is approval-gated in the unattended allowlist. No live-probe claims are made in this doc. Everything above is proven by `git`, `gh run`, blob hashes, and tree contents.
- **OBSERVATION (this run):** the loop's own prior output was the least reliable input available. Two of today's findings are corrections to yesterday's standup (`product-i18n` stranded → shipped; media lane mirrors `main` → now reverts `main`), and both were caught by re-deriving from the tree rather than carrying the claim forward. Scope held at status + a single doc commit.

---
