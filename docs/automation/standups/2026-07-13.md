# TradeClaw Daily Standup — 2026-07-13

- Date: 2026-07-13 09:30 GMT+8 (scheduled unattended run)
- Primary tree: DETACHED HEAD @ `b6dd0ff4` (== `origin/main` tip; `main` is checked out in the `oss-pivot-1` worktree)
- Standup lane: fresh branch `loop/standup-2026-07-13` off `b6dd0ff4` — executes the standing "re-anchor off main" recommendation; old lanes `loop/standup-2026-06-26` / `loop/standup-2026-07-04` retired from active use
- Note: no 2026-07-12 standup exists anywhere in git — yesterday's run died before writing (gate FAIL(2)); this is a recorded one-day hole, not hidden

## DONE (since last successful standup, 2026-07-11)

**Top escalation CLOSED — deploy gap resolved.** Production `tradeclaw.win` now serves the free/open-source pivot. Verified live on 2026-07-13 via page fetch:
- Homepage: "Everything is free — there are no tiers", "Nothing is for sale". No Pro $29 / Elite $99 / trial funnel remains.
- `/pricing`: zero monetization copy; MIT-licensed / open-source framing only.
- Cost Field hero live with the honest production record as displayed on prod: net expectancy −0.43R per trade, total return after costs −100%, win rate 36.3% vs 36% break-even, 4,036 recorded trades open for public inspection. Losses shown, not hidden — brand promise ("Every trade verified", radical transparency) is now what production actually says.

**Merged to main (07-11 → 07-12):**
- #154 fix(ci): restore daily track-record cron; stop scheduling the 4 Cloudflare-blocked dispatchers
- #155 ci: park daily-track-record schedule until the Cloudflare WAF rule covers /track-record
- #156 Impeccable 3D makeover: Cost Field hero + transparency data product (pivot Phase 5)
- #157/#158/#159 Railway deployment records (Cost Field, websocket isolation)
- #161 fix(nav,seo): repair 4 dead links, index the transparency pages, un-orphan /today
- Direct on main: recost diagnostic lane — `scripts/research/recost-segment.ts` CLI + tests (103004bc), parser hardening (bd93f60f), JSON output-path validation (135b9439), docs finalized 07-12 23:59 (b6dd0ff4)

**CI:** green on 07-12 pushes (CI, Docker Publish, Docker Smoke Test all success).

**Cron dispatchers:** no longer silently red. Last scheduled runs (07-11) all failed, then #154/#155 deliberately unscheduled the 4 Cloudflare-blocked dispatchers and parked Daily Track Record Post. Public auto-posting is dark by explicit parking pending the WAF rule — a decision on record, not a silent failure.

## PENDING

- **Loop reliability:** 3 gate-FAIL runs in 4 days (07-09 daily, 07-10 weekly, 07-12 daily) — each died before writing output. Launcher/--max-turns needs inspection; this run recovered the lane.
- **Another writer's recost lane (NOT staged by this loop):** primary tree carries 7 modified files (+138/−68: `STATE.yaml`, 5 `docs/ai-improvement/*` docs, `scripts/research/recost-segment.ts`) plus untracked `scripts/research/recost-report.ts` + `__tests__/recost-report.test.ts`. Continues the recost lane last committed 07-12 23:59. Tree frozen during this run (t0==t1). Left for its owner session.
- **Open PRs:** #160 rescue of 5 stranded standups 07-05..07-11 (MERGEABLE) · #149 loop lane, standup 07-04 + weekly research 07-03 (MERGEABLE, superseded for future standups by today's fresh branch) · #143 draft ADR (signal-worker extraction).
- **Primary tree detached** at main tip with `main` held by the `oss-pivot-1` worktree; 10 worktrees total incl. stale `pr135-merge`, `pr144-fix`, `deploy-main`, `deploy-153`.
- **Workflow fan-out still blocked non-interactively** — ultracode attempt re-verified today ("Review dynamic workflow before running"); no-op in unattended runs.
- **graphify --update skipped** (one line per contract): session cwd is pinned outside the repo and GEMINI_API_KEY is unverifiable here; graph is 7 days stale (2026-07-06).

## NEXT

1. Owner: merge #160 to restore standup history on main; then reconcile/close #149 (verify its 07-04 standup + weekly research aren't lost) and delete retired branches `loop/standup-2026-06-26`, `loop/standup-2026-07-04`.
2. Owner: add the Cloudflare WAF rule for `/track-record`, then reverse #155 so Daily Track Record Post resumes — the public verified record should not stay dark longer than necessary.
3. Fix the loop launcher (3 dead runs in 4 days wrote nothing; the gate correctly FAILed them).
4. Regenerate the knowledge graph supervised (repo cwd + GEMINI_API_KEY): the 07-06 graph predates the detier (#152/#153) and the 57-component purge — e.g. god node `getUserTier()` (33 edges) should now be gone; confirm no vestigial tier code survives.
5. Leave the recost-report.ts lane to its owner session; do not adopt or stage it from the loop.

## graphify highlights (standing 2026-07-06 report — 11,497 nodes / 18,049 edges / 845 communities)

- God Nodes: `readSessionFromRequest()` 114 edges · `execute()` 62 · `query()` 60 · `getUserById()` 49 · `requireCronAuth()` 46 · `getTrackedSignals()` 42 · `queryOne()` 41 · `getTrackedSignalsForRequest()` 34 · `getUserTier()` 33 (stale — tiers deleted since) · `getOHLCV()` 32
- Surprising connections: web UI components call `pad()` from `packages/agent/src/cli/cli.ts` [INFERRED]; backtest UI props (`ComparisonChartProps`, `MetricsTableProps`) reference `BacktestResult` from `packages/strategies` [EXTRACTED]
- Import cycles (coupling to watch): `channels/base.ts` ↔ each of `webhook.ts` / `discord.ts` / `telegram.ts` (3 two-file cycles) · `apps/web/lib/alert-channels.ts` ↔ `email-sender.ts`

## Sync result

- Branch: `loop/standup-2026-07-13` (fresh off `b6dd0ff4`, created in an isolated worktree; primary tree and the concurrent writer's files untouched)
- Commit: `9da234a6` pushed and verified via ls-remote (remote tip == local HEAD)
- PR: BLOCKED — `gh pr create` and `gh api` PR creation are denied in this unattended session (write-scope gh not allowlisted). Owner action: open the PR at https://github.com/naimkatiman/tradeclaw/pull/new/loop/standup-2026-07-13 (base `main`). Never merge directly.
