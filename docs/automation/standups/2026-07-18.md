# TradeClaw Daily Standup — 2026-07-18

- Date: 2026-07-18 (scheduled 09:30 GMT+8, unattended)
- Working branch: `feat/premium-ui-overhaul` (base HEAD `805afd48`, advances to the standup commit below)
- Upstream: `origin/feat/premium-ui-overhaul` @ `805afd48` — 0 ahead / 0 behind at start (branch fully pushed)
- Vs `origin/main` @ `2506a795` — 5 ahead / 22 behind (branch fell 5 further behind the merge train since 07-17's 3/17)
- Ground truth: `continuous-improvement:reconcile` + `safety-guard` + `git diff --stat` (autocrlf-aware). No stash, no in-progress merge/rebase. 32 worktrees registered.

## DONE (last 24h)

Two streams: this branch's standup-record commits, plus 2 production-track PRs concurrent sessions merged to `origin/main`.

Branch-local (already pushed, on `feat/premium-ui-overhaul`):
- `805afd48` `docs(standup): record 07-17 final sync` — verified push `524fc44a`→`a35e1b22`; feature→main PR left owner-gated.
- `a35e1b22` `docs(standup): daily standup 2026-07-17`.

Merged to `origin/main` (production-track, concurrent sessions — outcome-first):
- Position sizing now scales with live volatility: `479b1df1` `fix(risk): wire live volatility into the allocator` (PR #176). Closes the gap where the vol scaler was computed but never fed the allocator.
- Slow regime-gate evidence published: `20bdb9e7` `feat(research): slow regime-gate sandbox evidence` (PR #175). EMA200 gate beats buy-and-hold on Calmar after costs (3/4 folds); HMM sizing hurts (~90% cost drag from 995 flips); nothing beats buy-and-hold on raw CAGR. Measured, not marketed — matches the brand's "prove it after costs" stance.

## PENDING (uncommitted / unpushed — not this loop's to land)

Real content drift grew sharply since 07-17: **54 tracked files, +1714/-1365** (up from 8 files/+995), plus 20 untracked. This is not phantom CRLF — it is at least four distinct concurrent-writer lanes sitting unstaged in one tree. A daily standup does not sweep this into a commit; it belongs to its owning lanes, split by concern.

- 5 branch commits sit ahead of `origin/main` and 22 behind it → the branch needs a rebase before any clean integration PR. No open PR from this branch (#168 already merged).
- Lane A — deployment / self-host surface (NEW and largest this run):
  - Deletions: `Dockerfile.fly`, `apps/web/app/fly/FlyClient.tsx` (-279), `apps/web/app/replit/ReplitClient.tsx` (-264).
  - Config churn: `fly.toml`, `docker-compose.yml`, `docker-entrypoint.sh`, `.replit`, `.env.example`, `.env.docker.example`, `scripts/init-db.sh`, `scripts/test-docker.sh`, `.github/workflows/docker-test.yml`, `next.config.ts`, `packages/create-tradeclaw/index.js`.
  - Docs: `docs/FLYIO.md` (-189), `docs/REPLIT.md`, `docs/QUICKSTART.md`, `docs/self-host-smoke-checklist.md`.
  - New tests + `.gitattributes` (`* text=auto`, `*.sh eol=lf` — keeps container entrypoints LF-safe after Windows clones): `apps/web/app/__tests__/deployment-surface-integrity.test.ts`, `apps/web/app/api/health/route.test.ts`, `packages/create-tradeclaw/index.test.js`, plus `apps/web/app/api/health/route.ts` reworked. Coherent lane — ships on its own branch.
- Lane B — premium UI / i18n: `navbar.tsx`, `site-footer.tsx`, `locale-provider.tsx`, `marketing-chrome-gate.tsx`, `translations.ts` (+ new `lib/__tests__/translations.test.ts`), `hero-brand-art.tsx`, locale pages `ar/es/ms/zh/page.tsx`, `layout.tsx`, `sitemap.ts` (+ test), `marketplace/providers/page.tsx`, docs pages.
- Lane C — websocket / price stream: `use-price-stream.ts`, `use-websocket-prices.ts`, new `lib/hooks/websocket-config.ts` (+ test), `apps/ws-server/src/__tests__/auth.test.ts`, `signals-live.ts`, `openapi.ts`.
- Lane D — ai-improvement / recost (carried from 07-17, still active):
  - `STATE.yaml` now **+151** (was +86) — the known stale-base clobber trap. Do NOT blind-commit; diff against `origin/main` and re-anchor first (branch is 22 behind, so a stale-base write would delete newer main release records).
  - `docs/ai-improvement/implementation-log.md` (+883) plus `README.md`, `source-review-metrics.md`, `verification-command-matrix.md`, `uncommitted-source-verification-handoff.md`.
  - `scripts/research/recost-segment.ts`, `recost-segment-cli.test.ts`; untracked `recost-report.ts` + test; untracked `slow-gate-assembly.ts` + `slow-gate-cli.ts` + tests; `docs/plans/2026-07-18-slow-regime-gate-sandbox.md`; `slow-gate-BTCUSD_ETHUSD-D1-...-f4.json` experiment output.
- Untracked prior standups `2026-07-13.md`, `2026-07-14.md`, `2026-07-15.md` are already committed on their own `loop/standup-*` branches (`9da234a6`, `d9b55496`, `25420b5e`) — not lost, deliberately not folded here (would create divergent duplicates on the wrong branch).
- Open PRs unmerged: #174 remotion claw backgrounds, #170 daily-intel 07-16, #160 rescue stranded standups (07-05..07-11), #149 loop standup 07-04, #143 draft ADR (signal-worker extraction + unwired feature-flag scaffold).
- `tmp/imagegen/*.png` untracked scratch.

## NEXT

- Owner: the drift is now four separable lanes (A deployment-surface, B premium-UI/i18n, C websocket, D ai-improvement/recost). Split by concern into distinct branches/PRs — never one 54-file blob. Lane A (Fly/Replit removal + self-host hardening) is the cleanest to peel off first.
- ai-improvement loop: reconcile `STATE.yaml` against `origin/main` before any commit (stale-base clobber guard; it now carries +151 lines of unverified drift on a base 22 behind main). Land `recost-report.ts` / `slow-gate-*` on the ai-improvement branch, never on a standup commit.
- Owner: rebase `feat/premium-ui-overhaul` onto `origin/main` (+22) before opening its integration PR. The branch bundles UI + weekly research + standup commits — split by concern for a clean merge.
- Worktree hygiene: 32 worktrees registered (up from 27). Stale detached-HEAD deploy/release trees (`deploy-153`, `deploy-main`, `pr135-merge`, `pr144-fix`, `verify-40dd669c`, `main-release-b4b04388`, `pr166-release`, `tradeclaw-deploy-176`) are cleanup candidates once their PRs are confirmed merged — owner-gated, never remove dirty worktrees.
- Standing infra gap (from memory, unchanged): owner must add the Cloudflare WAF rule covering `/api/cron/*`, `/api/digest/*`, and `/track-record` so headless cron dispatchers stop 403-ing.

## graphify highlights

Source: seeded `graphify-out/GRAPH_REPORT.md` (snapshot 2026-07-06). Incremental `--update` skipped — see Blockers. God Nodes, cycles, and surprising edges unchanged from the seed.

- God Nodes (coupling by edge count): `readSessionFromRequest()` 114, `execute()` 62, `query()` 60, `getUserById()` 49, `requireCronAuth()` 46, `getTrackedSignals()` 42, `queryOne()` 41, `getTrackedSignalsForRequest()` 34, `getUserTier()` 33, `getOHLCV()` 32. Session/auth resolution, signal-fetch, and raw DB access dominate — the blast-radius surfaces. Lane C (websocket/price-stream) sits near `getTrackedSignals*`; Lane A reworks the health route — both touch high-coupling neighborhoods, so verify broadly before those land.
- Surprising connections: `backtest/comparison-chart.tsx` and `backtest/metrics-table.tsx` both reference `BacktestResult` from `packages/strategies/src/run-backtest.ts` (UI reaching into the engine's result type); `app/lib/atr-calibration-cache.ts` → `packages/signals/src/atr-calibration.ts`.
- Import cycles (4, watch-on-touch): `packages/agent/src/channels/base.ts` ↔ `webhook.ts` / `discord.ts` / `telegram.ts` (three cycles through the channel base), and `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`.

## Blockers (logged, non-fatal — run continued)

- graphify `. --update` skipped deliberately: `GEMINI_API_KEY` confirmed NOT set this run (checked via `process.env`). `--update` on changed docs would spawn gateguard-blocked LLM subagents. Read the seeded 2026-07-06 report per the contract's "never fail the run over graphify." Report is 12 days stale; it predates all four current drift lanes, so treat its module map as approximate.
- State file `C:\Users\User\.claude\loops\state\tradeclaw.STATE.md` was readable this run (resumed from it) — write-back attempt result recorded in Sync.

## Sync result

- Branch: `feat/premium-ui-overhaul` (current working branch). Committed in place — no `git checkout` — because Lanes A–D are actively writing this same tree; switching branches mid-write would disrupt concurrent writers (concurrent-loop-safety rule).
- Commit SHA: recorded in the follow-up run-record. Staged `docs/automation/standups/2026-07-18.md` only, by explicit path (pathspec-limited commit). No `git add -A`. All 54 drift files + 20 untracked scratch/test/lane files left untouched and uncommitted.
- Push: to `origin/feat/premium-ui-overhaul` only, verified by `git ls-remote` (remote tip == local HEAD after commit). No merge to main, no force, single tree owns the branch.
- PR: feature→main integration PR NOT opened — owner-gated and out of scope for a daily standup. The branch is 22 behind `origin/main` and bundles four in-flight lanes; owner rebases and splits by concern first.
