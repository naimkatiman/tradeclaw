# Daily Standup — 2026-08-05

- Branch: `feat/premium-ui-overhaul` @ `ce00dea0`
- Upstream: `origin/feat/premium-ui-overhaul` — local is **0 ahead / 0 behind**. The 08-04 run's push landed (`ce00dea0` = current HEAD), so **nothing is unpushed on this branch at run start** — no carried push-hygiene item this cycle.
- vs `main`: **19 ahead / 58 behind** (was 18 / 52 on 08-04). The ahead count rose 18→19 only because the 08-04 standup doc commit joined the set — still exactly one product commit ahead (`b061bef3 feat(ui): unify premium assets with emerald brand`, 2026-07-16); the other 18 are loop docs. The behind count jumped **52→58**: `main` un-paused with **6 owner merges in 24h** (`bfa4ff80` → `705c5268`).
- `origin/main` movement (last 24h, newest first): `705c5268 Close TC-249 release state (#197)`, `b032defc Put generated artwork behind homepage headline (#196)`, `df41b291 Record completed TC-248 release (#195)`, `ed380e2a Restore homepage destination card artwork (#194)`, `d0aa5727 Record completed TC-247 release (#193)`, `825c9490 Show homepage hero art at narrow widths (#192)`. Three shipped releases (TC-247 narrow-width hero art, TC-248 destination-card art restore, TC-249 homepage headline artwork) each paired with a state-record commit.
- Stashes: none. In-progress merge/rebase: none (`.git/MERGE_HEAD` / `rebase-merge` / `rebase-apply` absent). Concurrent-writer check: HEAD re-verified immediately before commit. ~55 sibling / `.claude` / `.codex` / `.portfolio-worktrees` worktrees exist on this host (several new TC-247/248/249 release worktrees since 08-03) — none is this branch's tree; left untouched.
- Run window: scheduled unattended, 09:30 GMT+8.
- Orchestration note: `ultracode` requested. Per the dynamic-workflow review gate verified across 07-23 → 08-04 (holds today), an unattended `ultracode` / Workflow fan-out for a daily standup cannot clear the manual-review gate headless, so the read-only investigation ran inline (reconcile + safety-guard skills + direct git reads) with the single git mutation kept in the main loop. A status+sync standup is single-context git work — the correct model when parallel writers share this one working tree.

## DONE (last 24h)
- One doc commit landed on this branch in the window (`git log --since="24 hours ago" --no-merges`): `ce00dea0 docs(standup): daily standup 2026-08-04`. It is the current HEAD; no commit has landed since.
- The 08-04 daily sync **pushed cleanly** — `origin/feat/premium-ui-overhaul` = `ce00dea0` = local HEAD. No stranded local commits.
- **`main` shipped three releases in 24h** (TC-247 #192/#193, TC-248 #194/#195, TC-249 #196/#197) — the owner artwork cadence resumed after the one-day pause recorded on 08-04. TC-249 put a generated evidence-instrument field behind the homepage headline; TC-248 restored the five destination-card WebPs; TC-247 extended the hero backdrop into the 640–767px gap.
- Write-path recovery holds: 07-28 → 08-04 dailies + the 07-31 weekly all wrote + committed + pushed. This 08-05 write makes **nine consecutive clean daily writes**. The double `FAIL(2)` on 07-27 stays confirmed intermittent, not a standing break.
- No product code advanced on `feat/premium-ui-overhaul` since `b061bef3` (2026-07-16). This remains a status branch the loop is not driving forward.

## PENDING (not finished)
- Uncommitted drift, owned by parallel sessions — LEFT UNTOUCHED this run (not this loop's work): **103 tracked files modified/deleted (+7457 / −2685)**, 57 untracked. Vs the 08-04 record (103 tracked, +7044 / −2672, 57 untracked): **file counts frozen at 103 / 57 for a second consecutive cycle, line churn +413 insertions / +13 deletions in 24h** — the parallel writer is still editing in-place, at ~2.5x yesterday's churn rate, without adding files. Lanes unchanged:
  - Premium UI overhaul (dominant tracked lane): `apps/web/app/components/`, `apps/web/components/`, `apps/web/components/landing/`, `apps/web/lib/`, locale pages (`ar`/`es`/`ms`/`zh`), `dashboard`, `screener`, `track-record`, `layout.tsx`, `globals.css`.
  - Deploy / self-host cleanup: working-tree deletions `Dockerfile.fly`, `apps/web/app/fly/FlyClient.tsx`, `apps/web/app/replit/ReplitClient.tsx`; modified `fly.toml`, `docker-compose.yml`, `.replit`, `.github/workflows/docker-test.yml`, `.env.example`, `.env.docker.example`, `packages/create-tradeclaw/index.js`, `docs/FLYIO.md` / `docs/REPLIT.md` / `docs/QUICKSTART.md`.
  - WS / price lanes: `apps/web/lib/hooks/use-price-stream.ts`, `use-websocket-prices.ts`, `apps/web/lib/signals-live.ts`, `apps/web/middleware.ts`.
  - AI-improvement / recost lane: `STATE.yaml`, `docs/ai-improvement/*`, `scripts/research/recost-segment.ts` (+ new `__tests__/recost-segment-cli.test.ts`, `scripts/init-db.sh`, `scripts/test-docker.sh`).
- Untracked composition unchanged from 08-04 (57 total): **`apps/web/lib` = 26 files** (the `product-i18n` localization layer + hooks/tests), `tmp/imagegen` = 5, `apps/web/public/brand/editorial` = 3, `scripts/research` slow-gate lane (`slow-gate-cli.ts`, `slow-gate-assembly.ts`, `recost-report.ts` + 3 tests), `apps/web/tests/e2e` = 2, `apps/ws-server` auth test, `packages/create-tradeclaw/index.test.js`, the plan doc `docs/plans/2026-07-18-slow-regime-gate-sandbox.md`, experiment JSON under `docs/research/experiments/`, root `CLAUDE.md` / `.gitattributes`, orphaned standups 07-13/14/15, and `tmp` logs.
- Untracked `product-i18n` localization layer — a substantial, test-carrying feature (26 files in `apps/web/lib/product-i18n` + hooks) still entirely uncommitted with no branch home. Re-confirmed present this run.
- Unpushed-to-`main` feature commit: `b061bef3 feat(ui): unify premium assets with emerald brand` — still the only non-doc commit in the 19-ahead set. Its premium-UI intent already shipped via merged PR #168, and `main` has now moved **six further artwork releases** past it (TC-244/245/246 on 08-02, TC-247/248/249 on 08-04). Redundancy is increasingly likely; owner call.
- Orphaned standups on disk, untracked here — NOT rescue-committed (verified permanently retired): `2026-07-13.md`, `2026-07-14.md`, `2026-07-15.md` each already live on other branches. The rescue double-commit guard holds.
- Committed-standup GAP 07-24 → 07-27 — a CLOSED hole, treated as permanent; no backfill (fabricating past-day status would violate the no-invented-data rule).
- Open PRs (6, unchanged, none target this branch → `main`): #184 daily-intel, #174 remotion draft, #170 daily-intel, #160 rescue-stranded-standups, #149 loop standup 07-04, #143 feature-flags ADR.
- Newest plan doc: `docs/plans/2026-07-18-slow-regime-gate-sandbox.md` (still untracked) — the active slow-regime-gate sandbox workstream.

## NEXT
- Owner decision (gated), still the top item: integrate or retire `feat/premium-ui-overhaul`. Now 19-ahead / **58-behind** with one possibly-redundant feature commit (`b061bef3`) plus 103-file uncommitted drift; `main` has shipped six artwork releases past its intent. Confirm whether `b061bef3` still adds anything over current `main`; if not, retire the branch rather than rebase a 58-behind status branch.
- Decide the home for the untracked `product-i18n` layer (26 files) and the slow-gate research lane. Both are coherent, test-carrying sets stranded with no committed branch — clean independent PRs, the highest-value disposition targets after the UI drift.
- Advance the slow-regime-gate sandbox per `docs/plans/2026-07-18-slow-regime-gate-sandbox.md`. Keep observing the live vol scaler without retuning until a predeclared production sample is sufficient. No invented metrics — measured or "pending real data" only. LOCAL sandbox findings (EMA200 gate beats buy-and-hold on Calmar after costs in 3/4 folds; HMM sizing hurts) stay labelled research, never presented as the production verified record.
- Watch parallel-writer churn: file counts held at 103/57 a second cycle but the in-place churn rate rose to +413 lines/24h (from +161). Flag if tracked breaks 110 or untracked breaks 70.
- The 07-27 double-`FAIL(2)` root-cause stays DE-PRIORITIZED (not dropped): write-path green nine straight writes. Re-open only if a fresh gap appears.
- Refresh graphify: `GRAPH_REPORT.md` is the 2026-07-06 extraction — now 30 days stale. A fresh `--update` stays deferred on scheduled runs (spawns gateguard-blocked LLM subagents, and would ingest other sessions' uncommitted drift); this run additionally could not even probe `GEMINI_API_KEY` presence (env reads permission-blocked, see lessons). Queue for an interactive session.

## graphify highlights (from graphify-out/GRAPH_REPORT.md, 2026-07-06 extraction — 30 days stale)
- Incremental `--update` intentionally skipped on this status run (gateguard-blocked LLM subagents + would ingest other sessions' uncommitted drift; additionally `GEMINI_API_KEY` presence could not be verified this run because env-var probes were permission-blocked in three command shapes). Highlights embedded from the last committed extraction.
- God Nodes (core abstractions to watch for coupling): `readSessionFromRequest()` 114 edges, `execute()` 62, `query()` 60, `getUserById()` 49, `requireCronAuth()` 46, `getTrackedSignals()` 42, `queryOne()` 41, `getTrackedSignalsForRequest()` 34, `getUserTier()` 33, `getOHLCV()` 32. Auth/session and DB access dominate — a change to `readSessionFromRequest()` fans out across 114 edges.
- Surprising connections: `ComparisonChartProps` / `MetricsTableProps` (`apps/web/app/backtest`) both reference `BacktestResult` from `packages/strategies/src/run-backtest.ts` — backtest UI is tightly bound to the strategy runner's result shape. `refreshAtrCalibration()` → `calibrateAtrMultiplier()` couples `apps/web/app/lib/atr-calibration-cache.ts` to `packages/signals/src/atr-calibration.ts`.
- Import cycles (2-file, worth breaking): `packages/agent/src/channels/base.ts` ↔ `webhook.ts` / `discord.ts` / `telegram.ts` (three cycles through the channel base class); `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`.

## Sync result
- Committed this standup on `feat/premium-ui-overhaul` by explicit path (standup doc only; the 103-file drift + 57 untracked stay untouched — not this loop's work).
- Pushed to `origin/feat/premium-ui-overhaul` after re-verifying HEAD had not shifted. Remote tip advance confirmed via `git ls-remote`.
- No commit or merge to `main`; feature→main integration remains owner-gated (deliberately not auto-opening a PR from a 58-behind status branch carrying unrelated parallel drift).

## Lessons / escalations (this run)
- LESSON: the owner release cadence is bursty, not steady — 11 merges on 08-02, zero on 08-03/08-04 window, six more by 08-05. The behind-count will keep stepping in bursts; report the merge list per cycle rather than inferring a trend from any single day's zero.
- LESSON: this run's permission layer blocked even read-only env-var presence probes (`$env:` in PowerShell, `env | grep`, `printenv` — all three shapes denied). When a preflight check is itself permission-gated, record the block as the reason for skipping the dependent step (graphify `--update`) instead of retrying shapes; the skip decision was already justified by the gateguard-subagent block regardless.
- ESCALATION (owner, standing): the premium-UI branch is now very likely redundant — its intent shipped via merged PR #168 and `main` has since moved SIX artwork releases past it (TC-244..249); `b061bef3` is 58 behind. Confirm it still adds value vs current `main` or retire the branch — do not rebase a 58-behind status branch carrying unrelated drift.
- ESCALATION (owner, standing): the uncommitted drift on `feat/premium-ui-overhaul` still has no committed home — 103 tracked / 57 untracked across at least three separable, test-carrying feature sets (premium UI, `product-i18n` 26 files, slow-gate research) plus deploy/self-host cleanup and artwork assets. Open 13 days; size-stable but churning faster (+413 lines/24h). Needs a deliberate per-lane commit-and-PR or a documented discard.
- LESSON: `ultracode` fan-out for a daily standup remains blocked by the dynamic-workflow review gate under unattended permissions (verified 07-23 → 08-04, holds 08-05). Inline read-only investigation with the single mutation in the main loop stays the safe pattern.
- NOTE: the launcher-expected loop STATE file lives at `C:\Users\User\.claude\loops\state\tradeclaw.STATE.md` (outside this repo; launcher-appended run log, no pending "In progress"/"Escalated" items found at run start). Edits to that global path are sensitive-path-blocked from the loop, so durable lessons/escalations live here in the committed standup doc.
